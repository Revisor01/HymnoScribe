---
phase: 03-wysiwyg-controls-element-reihenfolge
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/js/liedblattManagement.js
  - frontend/dashboard.html
  - frontend/css/dashboard.css
autonomous: true
requirements: [ELEM-01, ELEM-02]

must_haves:
  truths:
    - "Strophen und Refrain eines Liedes lassen sich per Drag per SortableJS in beliebige Reihenfolge bringen"
    - "Refrain kann als erstes Element (vor Strophe 1) gesetzt werden"
    - "Refrain kann dupliziert werden (mehrfach in der Liste vorhanden)"
    - "Strophen-Selektion per Checkboxen wählt welche Strophen gedruckt werden"
    - "Refrain-Verweis-Toggle: 'Vollständig' oder 'Nur Verweis' (druckt nur 'Refrain')"
    - "Reihenfolge und Selektion werden in objekt.elementOrder serialisiert"
    - "Backward-Compat: fehlende elementOrder fällt auf altes Rendering zurück"
  artifacts:
    - path: "frontend/js/liedblattManagement.js"
      provides: "createLiedOptions() mit SortableJS-Strophenliste und elementOrder-Serialisierung"
      contains: "elementOrder"
    - path: "frontend/dashboard.html"
      provides: "SortableJS CDN-Einbindung"
      contains: "sortablejs"
  key_links:
    - from: "createLiedOptions() Sortable-Liste"
      to: "updateLiedblatt() elementOrder"
      via: "Sortable.toArray() → objekt.elementOrder beim Change-Event"
      pattern: "elementOrder"
    - from: "updateLiedblatt() elementOrder"
      to: "engine.js Item-Traversal"
      via: "Strophen/Refrain-Blöcke werden in elementOrder-Reihenfolge in content eingefügt"
      pattern: "elementOrder"
---

<objective>
createLiedOptions() auf eine SortableJS-basierte Strophen/Refrain-Reihenfolge-Liste umbauen. Refrain als eigenes duplizierbares Element mit Toggle (Vollständig/Nur Verweis). Strophenauswahl per Checkboxen. updateLiedblatt() rendert in elementOrder-Reihenfolge.

Purpose: Implementiert ELEM-01 (Refrain als erstes Element) und ELEM-02 (freie Reihenfolge) mit SortableJS gemäß D-05, D-06, D-07, D-08.
Output: Seitenpanel mit sortierbarer Strophen/Refrain-Liste, Reihenfolge beeinflusst Vorschau sofort.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-wysiwyg-controls-element-reihenfolge/03-01-SUMMARY.md

<interfaces>
<!-- Bestehende Interfaces — direkt aus Codebase extrahiert -->

Aus frontend/js/liedblattManagement.js:

```javascript
// createLiedOptions(lied) — aktuell (Zeile 149-267)
// Erzeugt: .lied-options div mit Strophen als .strophe-option (Checkbox + Label + refrainSelect)
// AKTUELLES MODEL: Pro Strophe eine Zeile mit nachgeschaltetem Refrain-Select
//   strophe-option[0]: [Checkbox Strophe 1] [Label] [Select: kein/voll/verweis]
//   strophe-option[1]: [Checkbox Strophe 2] [Label] [Select: kein/voll/verweis]
//
// NEUES MODEL: Sortierbare Liste mit eigenständigen Elementen
//   sortable-item[data-type="strophe" data-index="0"]: [☑ Strophe 1] [⠿ Handle]
//   sortable-item[data-type="refrain"]: [Refrain: ○Vollständig ○Verweis] [⠿ Handle] [+ Duplizieren]
//   sortable-item[data-type="strophe" data-index="1"]: [☑ Strophe 2] [⠿ Handle]

export function createLiedOptions(lied) { /* muss umgebaut werden */ }

// updateLiedblatt() (Zeile 269-570) — renderiert content in liedblatt-content
// AKTUELL: iteriert selectedStrophen (Array von Strophen-Indizes) und schreibt
//   strophe-div → refrain-div (falls refrainOptions[index] != 'none')
// NEU: muss objekt.elementOrder konsumieren, falls vorhanden
//   elementOrder = ['strophe-0', 'refrain', 'strophe-1', 'refrain', 'strophe-2']
//   Backward-Compat: falls elementOrder fehlt → altes Verhalten (selectedStrophen + refrainOptions)

// Refrain-Rendering (Zeile 432-481):
//   refrainType === 'full' → voller Text mit "Refrain:"-Prefix
//   refrainType === 'short' → nur <p>Refrain</p> (bereits implementiert!)
// Diese Logik bleibt ERHALTEN — nur die Trigger-Logik ändert sich

// sessionManagement.js liest (Zeile 172-178):
//   objekt.selectedStrophen = Array.from(... '.strophen-container input:checked') ...
//   objekt.refrainOptions = Array.from(... '.strophe-option') ...
// NEU: Diese Auslese muss auf elementOrder umgestellt werden
```

SortableJS CDN (aus RESEARCH.md):
```html
<!-- SortableJS 1.15.7 — aktiv gewartet (Feb 2026) -->
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.7/Sortable.min.js"></script>
```

Serialisierungs-Zielformat:
```javascript
// objekt.elementOrder = ['strophe-0', 'strophe-1', 'refrain', 'strophe-2']
// objekt.elementConfig = {
//   'strophe-0': { active: true },
//   'strophe-1': { active: false },
//   'refrain':   { mode: 'full' },        // 'full' oder 'short'
//   'refrain-2': { mode: 'short' }        // Duplikate mit Suffix
// }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: createLiedOptions() auf SortableJS-Reihenfolge-Liste umbauen</name>
  <files>frontend/js/liedblattManagement.js, frontend/dashboard.html, frontend/css/dashboard.css</files>
  <action>
**SortableJS in dashboard.html einbinden** — Im `<head>` nach den bestehenden CDN-Imports:

```html
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.7/Sortable.min.js"></script>
```

**createLiedOptions() komplett ersetzen** mit neuer Implementierung:

```javascript
export function createLiedOptions(lied) {
    const liedOptions = document.createElement('div');
    liedOptions.classList.add('lied-options');

    // Toggle-Button (bleibt)
    const toggleButton = document.createElement('button');
    toggleButton.textContent = 'Optionen anzeigen/ausblenden';
    toggleButton.classList.add('toggle-options');
    liedOptions.appendChild(toggleButton);

    const optionsContent = document.createElement('div');
    optionsContent.classList.add('options-content');
    optionsContent.style.display = 'none';
    liedOptions.appendChild(optionsContent);

    // --- Noten-Option (wie bisher) ---
    const showNotesDiv = document.createElement('div');
    const showNotesCheckbox = document.createElement('input');
    showNotesCheckbox.type = 'checkbox';
    showNotesCheckbox.id = `showNotes-${lied.id}`;
    const showNotesLabel = document.createElement('label');
    showNotesLabel.htmlFor = showNotesCheckbox.id;
    showNotesLabel.textContent = 'Noten anzeigen';
    showNotesDiv.appendChild(showNotesCheckbox);
    showNotesDiv.appendChild(showNotesLabel);
    optionsContent.appendChild(showNotesDiv);

    const noteTypeDiv = document.createElement('div');
    noteTypeDiv.style.display = 'none';
    const noteTypeRadio1 = document.createElement('input');
    noteTypeRadio1.type = 'radio';
    noteTypeRadio1.name = `noteType-${lied.id}`;
    noteTypeRadio1.value = 'without';
    noteTypeRadio1.id = `notesWithoutText-${lied.id}`;
    const noteTypeLabel1 = document.createElement('label');
    noteTypeLabel1.htmlFor = noteTypeRadio1.id;
    noteTypeLabel1.textContent = 'Noten ohne Text';
    noteTypeDiv.appendChild(noteTypeRadio1);
    noteTypeDiv.appendChild(noteTypeLabel1);
    const noteTypeRadio2 = document.createElement('input');
    noteTypeRadio2.type = 'radio';
    noteTypeRadio2.name = `noteType-${lied.id}`;
    noteTypeRadio2.value = 'with';
    noteTypeRadio2.id = `notesWithText-${lied.id}`;
    const noteTypeLabel2 = document.createElement('label');
    noteTypeLabel2.htmlFor = noteTypeRadio2.id;
    noteTypeLabel2.textContent = 'Noten mit Text';
    noteTypeDiv.appendChild(noteTypeRadio2);
    noteTypeDiv.appendChild(noteTypeLabel2);
    optionsContent.appendChild(noteTypeDiv);

    showNotesCheckbox.addEventListener('change', function() {
        noteTypeDiv.style.display = this.checked ? 'block' : 'none';
        if (!this.checked) { noteTypeRadio1.checked = false; noteTypeRadio2.checked = false; }
        updateLiedblatt();
    });
    [noteTypeRadio1, noteTypeRadio2].forEach(r => r.addEventListener('change', updateLiedblatt));

    // --- Sortierbare Element-Liste (NEU: D-05, D-06, D-07, D-08) ---
    const sortableContainer = document.createElement('div');
    sortableContainer.classList.add('element-order-list');
    sortableContainer.id = `elementOrder-${lied.id}`;
    optionsContent.appendChild(sortableContainer);

    // Strophen parsen
    let strophen = lied.strophen;
    if (typeof strophen === 'string') {
        try { strophen = JSON.parse(strophen); }
        catch (e) { strophen = strophen.split('\n').filter(s => s.trim() !== ''); }
    }
    if (!Array.isArray(strophen)) strophen = [strophen];

    // elementOrder aus gespeichertem Zustand ODER Default aufbauen
    // Default: [strophe-0, refrain(falls vorhanden), strophe-1, refrain, ...]
    let elementOrder = lied.elementOrder || null;
    let elementConfig = lied.elementConfig || {};

    if (!elementOrder) {
        // Default-Reihenfolge: Strophe, dann optionaler Refrain nach jeder Strophe
        elementOrder = [];
        strophen.forEach((_, i) => {
            elementOrder.push(`strophe-${i}`);
            if (lied.refrain) elementOrder.push(`refrain`); // Initial: Refrain nach jeder Strophe
        });
        // Backward-Compat: refrainOptions anwenden falls vorhanden
        // (elementOrder wird beim ersten Öffnen aus altem State generiert)
    }

    // Duplikate von 'refrain' eindeutig machen: refrain, refrain-2, refrain-3 ...
    const refrainCounts = {};
    const normalizedOrder = elementOrder.map(key => {
        if (key.startsWith('refrain')) {
            const base = 'refrain';
            refrainCounts[base] = (refrainCounts[base] || 0) + 1;
            return refrainCounts[base] === 1 ? base : `${base}-${refrainCounts[base]}`;
        }
        return key;
    });

    // Sortierbare Items erstellen
    function createStropheItem(stropheIndex, key) {
        const item = document.createElement('div');
        item.classList.add('sortable-item', 'strophe-item');
        item.dataset.key = key;
        item.dataset.type = 'strophe';
        item.dataset.stropheIndex = stropheIndex;

        const checkbox = document.createElement('input');
        checkbox.type = 'checkbox';
        checkbox.classList.add('strophe-active-check');
        checkbox.checked = elementConfig[key]?.active !== false; // default: aktiv
        checkbox.addEventListener('change', updateLiedblatt);

        const label = document.createElement('span');
        // Ersten 30 Zeichen des Strophen-Texts als Preview
        const stropheText = typeof strophen[stropheIndex] === 'string'
            ? strophen[stropheIndex].replace(/<[^>]+>/g, '').trim().slice(0, 30)
            : `Strophe ${stropheIndex + 1}`;
        label.textContent = `${stropheIndex + 1}. ${stropheText}${stropheText.length >= 30 ? '…' : ''}`;
        label.classList.add('strophe-label');

        const handle = document.createElement('span');
        handle.classList.add('drag-handle');
        handle.textContent = '⠿';
        handle.title = 'Ziehen zum Sortieren';

        item.appendChild(handle);
        item.appendChild(checkbox);
        item.appendChild(label);
        return item;
    }

    function createRefrainItem(key, mode) {
        const item = document.createElement('div');
        item.classList.add('sortable-item', 'refrain-item');
        item.dataset.key = key;
        item.dataset.type = 'refrain';

        const handle = document.createElement('span');
        handle.classList.add('drag-handle');
        handle.textContent = '⠿';
        handle.title = 'Ziehen zum Sortieren';

        const labelSpan = document.createElement('span');
        labelSpan.textContent = 'Refrain';
        labelSpan.classList.add('refrain-label');

        // Toggle Vollständig / Nur Verweis (D-08)
        const toggleFull = document.createElement('input');
        toggleFull.type = 'radio';
        toggleFull.name = `refrainMode-${lied.id}-${key}`;
        toggleFull.value = 'full';
        toggleFull.checked = (mode !== 'short');
        const toggleFullLabel = document.createElement('label');
        toggleFullLabel.textContent = 'Vollständig';

        const toggleShort = document.createElement('input');
        toggleShort.type = 'radio';
        toggleShort.name = `refrainMode-${lied.id}-${key}`;
        toggleShort.value = 'short';
        toggleShort.checked = (mode === 'short');
        const toggleShortLabel = document.createElement('label');
        toggleShortLabel.textContent = 'Verweis';

        // Refrain duplizieren (D-06)
        const dupButton = document.createElement('button');
        dupButton.textContent = '+';
        dupButton.title = 'Refrain duplizieren';
        dupButton.classList.add('refrain-dup-btn');
        dupButton.type = 'button';
        dupButton.addEventListener('click', (e) => {
            e.stopPropagation();
            // Neues Refrain-Item nach diesem einfügen
            const newKey = `refrain-${Date.now()}`;
            const newItem = createRefrainItem(newKey, 'full');
            item.insertAdjacentElement('afterend', newItem);
            updateLiedblatt();
        });

        // Remove-Button (für Duplikate)
        const removeButton = document.createElement('button');
        removeButton.textContent = '×';
        removeButton.title = 'Refrain entfernen';
        removeButton.classList.add('refrain-remove-btn');
        removeButton.type = 'button';
        removeButton.addEventListener('click', (e) => {
            e.stopPropagation();
            item.remove();
            updateLiedblatt();
        });

        [toggleFull, toggleShort].forEach(r => r.addEventListener('change', updateLiedblatt));

        item.appendChild(handle);
        item.appendChild(labelSpan);
        item.appendChild(toggleFull);
        item.appendChild(toggleFullLabel);
        item.appendChild(toggleShort);
        item.appendChild(toggleShortLabel);
        item.appendChild(dupButton);
        item.appendChild(removeButton);
        return item;
    }

    // Items aus normalizedOrder erstellen
    normalizedOrder.forEach(key => {
        if (key.startsWith('strophe-')) {
            const idx = parseInt(key.replace('strophe-', ''));
            if (idx >= 0 && idx < strophen.length) {
                sortableContainer.appendChild(createStropheItem(idx, key));
            }
        } else if (key.startsWith('refrain') && lied.refrain) {
            const cfg = elementConfig[key] || {};
            sortableContainer.appendChild(createRefrainItem(key, cfg.mode || 'full'));
        }
    });

    // SortableJS initialisieren (D-05)
    if (typeof Sortable !== 'undefined') {
        Sortable.create(sortableContainer, {
            handle: '.drag-handle',
            animation: 150,
            onEnd: updateLiedblatt
        });
    }

    // Toggle-Button Event
    toggleButton.addEventListener('click', function(e) {
        e.preventDefault();
        optionsContent.style.display = optionsContent.style.display === 'none' ? 'block' : 'none';
    });

    return liedOptions;
}
```

**CSS** in `dashboard.css` hinzufügen:

```css
.element-order-list {
    margin-top: 8px;
    border: 1px solid #e0e0e0;
    border-radius: 4px;
    overflow: hidden;
}

.sortable-item {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 5px 8px;
    background: #fafafa;
    border-bottom: 1px solid #eeeeee;
    font-size: 12px;
    cursor: default;
}

.sortable-item:last-child {
    border-bottom: none;
}

.sortable-item.refrain-item {
    background: #f5f0ff;
}

.drag-handle {
    cursor: grab;
    color: #aaa;
    font-size: 14px;
    padding: 0 2px;
    user-select: none;
}

.drag-handle:active {
    cursor: grabbing;
}

.strophe-label {
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    max-width: 120px;
}

.refrain-label {
    font-style: italic;
    margin-right: 4px;
}

.refrain-dup-btn,
.refrain-remove-btn {
    padding: 1px 5px;
    font-size: 11px;
    cursor: pointer;
    border: 1px solid #ccc;
    border-radius: 3px;
    background: #fff;
    line-height: 1.2;
}

.refrain-remove-btn {
    color: #e74c3c;
    border-color: #e74c3c;
}
```
  </action>
  <verify>
    <automated>grep -n "elementOrder\|Sortable.create\|sortable-item" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | grep -q "Sortable.create" && echo "PASS: SortableJS in createLiedOptions" || echo "FAIL: SortableJS fehlt"</automated>
    <automated>grep -n "sortablejs" /Users/simonluthe/Documents/HymnoScribe/frontend/dashboard.html | grep -q "sortablejs" && echo "PASS: SortableJS CDN in HTML" || echo "FAIL: CDN fehlt"</automated>
    <automated>grep -n "elementOrder\|elementConfig" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | grep -q "elementOrder" && echo "PASS: elementOrder in liedblattManagement" || echo "FAIL: fehlt"</automated>
  </verify>
  <done>
    - SortableJS 1.15.7 per CDN in dashboard.html eingebunden (per D-05)
    - createLiedOptions() erzeugt sortierbare Liste mit Strophen-Checkboxen und Refrain-Items
    - Refrain hat Radio-Toggle Vollständig/Verweis (per D-08)
    - Refrain hat Duplizieren-Button (per D-06)
    - Drag-Handle ermöglicht freie Reihenfolge inkl. Refrain vor Strophe 1 (per ELEM-01)
    - Backward-Compat: fehlende elementOrder → Default-Reihenfolge
  </done>
</task>

<task type="auto">
  <name>Task 2: updateLiedblatt() und sessionManagement.js auf elementOrder umstellen</name>
  <files>frontend/js/liedblattManagement.js, frontend/js/sessionManagement.js</files>
  <action>
**updateLiedblatt() — elementOrder-Serialisierung lesen**: In der `forEach`-Schleife von `updateLiedblatt()` (Zeile 277ff), NACHDEM `objekt` gelesen wird, für Lieder mit `.lied-options` den elementOrder-State aus dem DOM auslesen:

```javascript
// In updateLiedblatt(), im forEach, nach `const objekt = JSON.parse(selected.getAttribute('data-object'))`:
// Nur für Lieder/Liturgien mit Strophen-Optionen:
if (objekt.typ === 'Lied' || objekt.typ === 'Liturgie') {
    const liedOptions = selected.querySelector('.lied-options');
    if (liedOptions) {
        const sortableContainer = liedOptions.querySelector('.element-order-list');
        if (sortableContainer) {
            // elementOrder aus DOM-Reihenfolge lesen
            const sortableItems = Array.from(sortableContainer.querySelectorAll('.sortable-item'));
            objekt.elementOrder = sortableItems.map(item => item.dataset.key);

            // elementConfig aus Item-States lesen
            objekt.elementConfig = {};
            sortableItems.forEach(item => {
                const key = item.dataset.key;
                if (item.dataset.type === 'strophe') {
                    const checkbox = item.querySelector('.strophe-active-check');
                    objekt.elementConfig[key] = { active: checkbox ? checkbox.checked : true };
                } else if (item.dataset.type === 'refrain') {
                    const modeRadio = item.querySelector('input[type="radio"]:checked');
                    objekt.elementConfig[key] = { mode: modeRadio ? modeRadio.value : 'full' };
                }
            });
        }
    }
}
```

**updateLiedblatt() — Content-Rendering nach elementOrder**: Der bestehende Block der Strophen/Refrain-Renders (Zeile 392-487) muss auf elementOrder-Basis umgestellt werden. BACKWARD-COMPAT: Wenn `objekt.elementOrder` nicht vorhanden, altes Verhalten (selectedStrophen + refrainOptions) beibehalten.

Ersetze den Block ab `const selectedStrophen = ...` (Zeile 392) bis zum Ende des `forEach` (Zeile 484) mit:

```javascript
// Strophen parsen
let strophen = objekt.strophen;
if (typeof strophen === 'string') {
    try { strophen = JSON.parse(strophen); }
    catch (e) { strophen = strophen.split('\n').filter(s => s.trim() !== ''); }
}
if (!Array.isArray(strophen)) strophen = [strophen];

// NEUES RENDERING: elementOrder vorhanden
if (objekt.elementOrder && objekt.elementOrder.length > 0) {
    const elementConfig = objekt.elementConfig || {};

    objekt.elementOrder.forEach(key => {
        if (key.startsWith('strophe-')) {
            const stropheIndex = parseInt(key.replace('strophe-', ''));
            const cfg = elementConfig[key] || {};
            // Nur rendern wenn aktiv (Checkbox gesetzt) — per D-07
            if (cfg.active === false) return;
            if (stropheIndex < 0 || stropheIndex >= strophen.length) return;

            const stropheDiv = document.createElement('div');
            stropheDiv.classList.add('strophe');
            stropheDiv.style.marginBottom = `${globalConfig.fontSize * 0.5}px`;

            let strophenText = strophen[stropheIndex].replace(/<p>/g, '').replace(/<\/p>/g, '<br>');
            let strophenTextArray = strophenText.split('<br>');

            const pElementWithNumber = document.createElement('p');
            pElementWithNumber.classList.add('strophe');
            pElementWithNumber.innerHTML = `<span class="strophenum">${stropheIndex + 1}.</span> ${strophenTextArray[0]}`;
            stropheDiv.appendChild(pElementWithNumber);

            for (let i = 1; i < strophenTextArray.length; i++) {
                if (strophenTextArray[i].trim() !== '') {
                    const pElement = document.createElement('p');
                    pElement.textContent = strophenTextArray[i];
                    stropheDiv.appendChild(pElement);
                }
            }
            content.appendChild(stropheDiv);

        } else if (key.startsWith('refrain') && objekt.refrain) {
            const cfg = elementConfig[key] || {};
            const refrainType = cfg.mode || 'full'; // 'full' oder 'short'

            const refrainDiv = document.createElement('div');
            refrainDiv.classList.add('refrain');

            if (refrainType === 'full') {
                // Volles Refrain-Rendering (bestehende Logik aus Zeile 436-473, unverändert)
                const tempDiv = document.createElement('div');
                tempDiv.innerHTML = objekt.refrain;
                const firstP = document.createElement('p');
                firstP.style.fontStyle = 'italic';
                firstP.classList.add('refrain');
                if (tempDiv.childNodes.length > 0) {
                    if (tempDiv.firstChild.nodeType === Node.TEXT_NODE) {
                        firstP.textContent = 'Refrain: ' + tempDiv.firstChild.textContent.trim();
                        tempDiv.removeChild(tempDiv.firstChild);
                    } else {
                        firstP.innerHTML = 'Refrain: ' + tempDiv.firstChild.innerHTML;
                        tempDiv.removeChild(tempDiv.firstChild);
                    }
                } else {
                    firstP.textContent = 'Refrain:';
                }
                refrainDiv.appendChild(firstP);
                Array.from(tempDiv.children).forEach(child => {
                    const newP = document.createElement('p');
                    newP.style.fontStyle = 'italic';
                    newP.classList.add('refrain');
                    newP.innerHTML = child.innerHTML;
                    refrainDiv.appendChild(newP);
                });
            } else {
                // Kurz-Verweis (D-08)
                const shortRefrain = document.createElement('p');
                shortRefrain.style.fontStyle = 'italic';
                shortRefrain.textContent = 'Refrain';
                refrainDiv.appendChild(shortRefrain);
            }
            content.appendChild(refrainDiv);
        }
    });
} else {
    // BACKWARD-COMPAT: altes selectedStrophen + refrainOptions Rendering
    const selectedStrophen = Array.from(selected.querySelectorAll('.strophen-container input:checked')).map(cb => parseInt(cb.value));
    if (selectedStrophen.length > 0) {
        selectedStrophen.forEach((index) => {
            // [BESTEHENDER CODE aus Zeile 406-482 unverändert hier einfügen]
            // Wichtig: Dieser Block muss 1:1 aus dem Original kopiert werden
        });
    }
}
```

**sessionManagement.js — elementOrder serialisieren**: In `saveSessionToLocalStorage()` (Zeile 165-179), den Branch für `Lied`/`Liturgie` erweitern:

```javascript
// Nach objekt.refrainOptions = ... (Zeile 174-178), ergänzen:
// elementOrder aus DOM lesen (falls neue createLiedOptions verwendet)
const sortableContainer = liedOptions.querySelector('.element-order-list');
if (sortableContainer) {
    const sortableItems = Array.from(sortableContainer.querySelectorAll('.sortable-item'));
    objekt.elementOrder = sortableItems.map(item => item.dataset.key);
    objekt.elementConfig = {};
    sortableItems.forEach(item => {
        const key = item.dataset.key;
        if (item.dataset.type === 'strophe') {
            const checkbox = item.querySelector('.strophe-active-check');
            objekt.elementConfig[key] = { active: checkbox ? checkbox.checked : true };
        } else if (item.dataset.type === 'refrain') {
            const modeRadio = item.querySelector('input[type="radio"]:checked');
            objekt.elementConfig[key] = { mode: modeRadio ? modeRadio.value : 'full' };
        }
    });
}
```

In `applySessionData()` (Zeile 222): `elementOrder` und `elementConfig` werden via `addToSelected(objekt)` → `createLiedOptions(objekt)` automatisch wiederhergestellt, da `createLiedOptions` `lied.elementOrder` und `lied.elementConfig` bereits liest.
  </action>
  <verify>
    <automated>grep -n "elementOrder\|elementConfig" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | wc -l | awk '{print ($1 >= 4) ? "PASS: elementOrder mehrf. in updateLiedblatt" : "FAIL: zu wenige Vorkommen"}'</automated>
    <automated>grep -n "elementOrder\|elementConfig" /Users/simonluthe/Documents/HymnoScribe/frontend/js/sessionManagement.js | grep -q "elementOrder" && echo "PASS: elementOrder in sessionManagement serialisiert" || echo "FAIL: fehlt"</automated>
    <automated>grep -n "BACKWARD\|elementOrder.*length.*0\|!.*elementOrder" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | grep -q "elementOrder" && echo "PASS: Backward-Compat-Zweig vorhanden" || echo "FAIL: Backward-Compat fehlt"</automated>
  </verify>
  <done>
    - updateLiedblatt() rendert Strophen/Refrain in elementOrder-Reihenfolge (per D-05)
    - Strophen mit active=false werden nicht gerendert (Checkbox-Selektion per D-07)
    - Refrain-Modus 'full'/'short' steuert Volltext oder "Refrain"-Verweis (per D-08)
    - Backward-Compat: Lieder ohne elementOrder nutzen altes Rendering
    - sessionManagement.js serialisiert elementOrder + elementConfig in Session-Daten
    - applySessionData() stellt Reihenfolge beim Laden wieder her
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| objekt.refrain → innerHTML | Refrain-HTML aus Datenbank — potenziell unsanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | SortableJS CDN — externe Ressource | accept | SortableJS ist read-only UI-Library, kein Datenzugriff. CDN-Integrity-Hash optional für v2. |
| T-03-03-02 | Tampering | objekt.refrain wird als innerHTML gesetzt (Zeile 438) | accept | Bereits bestehendes Verhalten — nicht neue Angriffsfläche. Refrain kommt aus eigener Datenbank, nicht aus User-Input im Liedblatt-Editor. Pattern beibehalten (Änderung würde Formatierung brechen). |
| T-03-03-03 | Denial of Service | Unendliche Refrain-Duplikation | mitigate | Max 10 Refrain-Items pro Lied: In dupButton-Handler prüfen `if (sortableContainer.querySelectorAll('.refrain-item').length >= 10) return;` |
</threat_model>

<verification>
1. `grep -n "Sortable.create" frontend/js/liedblattManagement.js` — zeigt SortableJS-Initialisierung
2. `grep -n "sortablejs" frontend/dashboard.html` — zeigt CDN-Einbindung
3. `grep -n "elementOrder" frontend/js/sessionManagement.js` — zeigt Serialisierung
4. `grep -n "BACKWARD\|!.*elementOrder" frontend/js/liedblattManagement.js` — zeigt Backward-Compat
5. `grep -n "refrain-dup-btn\|refrain-remove-btn" frontend/js/liedblattManagement.js` — zeigt Duplikations-UI
</verification>

<success_criteria>
- Strophen/Refrain-Liste per SortableJS sortierbar — Reihenfolge beeinflusst Vorschau sofort
- Refrain kann an erste Position gesetzt werden (ELEM-01)
- Refrain kann mehrfach eingefügt werden via Duplizieren-Button (D-06)
- Strophen-Checkboxen steuern welche Strophen gedruckt werden (D-07)
- Refrain-Toggle Vollständig/Verweis funktioniert pro Refrain-Instanz (D-08)
- Sessions aus Phase 2 ohne elementOrder rendieren korrekt (Backward-Compat)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-wysiwyg-controls-element-reihenfolge/03-03-SUMMARY.md` erstellen.
</output>
