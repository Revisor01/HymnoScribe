---
phase: 03-wysiwyg-controls-element-reihenfolge
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/js/liedblattManagement.js
  - frontend/js/layout/domRenderer.js
  - frontend/js/previewPageBreaks.js
  - frontend/css/dashboard.css
autonomous: true
requirements: [WYSI-01, WYSI-02]

must_haves:
  truths:
    - "Jedes liedblatt-content-Kind-Element trägt ein stabiles data-override-key Attribut"
    - "Spacing-Handle erscheint als horizontaler Balken zwischen Elementen in der Vorschau"
    - "Drag am Spacing-Handle ändert spacingOverrides[key].after und triggert Re-Layout nach 150ms"
    - "Bild-Resize-Handle erscheint an der Unterkante von Bild-Blöcken in der Vorschau"
    - "Drag am Bild-Handle ändert imageSizeOverrides[key].widthFraction, Seitenverhältnis bleibt"
    - "Event-Delegation auf liedblatt-content verhindert Listener-Verlust beim Re-Render"
  artifacts:
    - path: "frontend/js/liedblattManagement.js"
      provides: "Stabile data-override-key Attribute auf jedem liedblatt-content-Kind"
      contains: "data-override-key"
    - path: "frontend/js/layout/domRenderer.js"
      provides: "Spacing-Handles und Bild-Resize-Handles nach jedem Block"
      contains: "spacing-handle"
    - path: "frontend/js/previewPageBreaks.js"
      provides: "Event-Delegation für Drag-Interaktion auf liedblatt-content"
      contains: "initDragHandles"
  key_links:
    - from: "domRenderer.js spacing-handle"
      to: "overrideState.js setSpacingOverride"
      via: "Event-Delegation pointerdown auf .spacing-handle, blockId → override-key"
      pattern: "spacing-handle"
    - from: "domRenderer.js image-resize-handle"
      to: "overrideState.js setImageSizeOverride"
      via: "Event-Delegation pointerdown auf .image-resize-handle, widthFraction berechnen"
      pattern: "image-resize-handle"
---

<objective>
Stabile Override-Keys in updateLiedblatt() setzen, Drag-Handles in domRenderer.js rendern und Pointer-Event-Handler via Event-Delegation auf dem Container initialisieren. Spacing- und Bild-Handles ermöglichen Live-Override direkt in der Vorschau.

Purpose: Implementiert WYSI-01 (Bild-Drag-Handle) und WYSI-02 (Spacing-Drag-Handle) mit Pointer-Events und setPointerCapture.
Output: Funktionierende Drag-Handles in der Vorschau — Spacing und Bildbreite per Ziehen anpassbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-wysiwyg-controls-element-reihenfolge/03-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 — bereits implementiert -->

Aus frontend/js/layout/overrideState.js (nach Plan 01):
```javascript
export function setSpacingOverride(key, afterPt) { /* clamp 0-100 */ }
export function setImageSizeOverride(key, widthFraction) { /* clamp 0.1-1.0 */ }
export function getOverrides() { return { spacingOverrides, imageSizeOverrides, fontSizeOverrides }; }
```

Aus frontend/js/layout/engine.js (nach Plan 01 — Signatur):
```javascript
export async function calculateLayout(items, config, fonts, overrides = {}) {
    const { spacingOverrides = {}, imageSizeOverrides = {}, fontSizeOverrides = {} } = overrides;
    // image-block: liest item.getAttribute('data-override-key')
    // spacing: liest item.getAttribute('data-override-key')
}
```

Aus frontend/js/liedblattManagement.js (aktuell — Zeile 282):
```javascript
// PROBLEM: uniqueId enthält Timestamp — instabil
const uniqueId = `liedblatt-item-${index}-${Date.now()}`;
content.setAttribute('data-liedblatt-id', uniqueId);
```

Aus frontend/js/layout/domRenderer.js (nach Plan 01):
```javascript
export function renderToDOM(layoutResult, config, container) {
    container.innerHTML = ''; // leert bei jedem Re-Render — Handles müssen neu gerendert werden
    for (const pageData of layoutResult.pages) {
        for (const block of pageData.blocks) {
            const blockEl = _createBlockElement(block, config);
            if (blockEl) pageEl.appendChild(blockEl);
        }
    }
}
// blocks haben: { type, x, y, width, height, data }
// image-block data: { src, naturalWidth, naturalHeight }
// Handles brauchen: block.data.overrideKey (muss in engine.js in block.data mitgeschrieben werden)
```

Aus frontend/js/previewPageBreaks.js:
```javascript
export function updatePreviewWithPageBreaks(format = 'a5') {
    // nach renderToDOM: initDragHandles(container) aufrufen
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabile Override-Keys in updateLiedblatt() + engine.js block.data</name>
  <files>frontend/js/liedblattManagement.js, frontend/js/layout/engine.js</files>
  <action>
**liedblattManagement.js — Stabile Override-Keys**: Das Problem: `uniqueId = 'liedblatt-item-${index}-${Date.now()}'` ändert sich bei jedem `updateLiedblatt()`-Aufruf. Override-Keys werden ungültig.

Lösung: Override-Key basiert auf `objekt.id` + `index` (positional — stabil solange Reihenfolge gleich). Der Key wird EINMAL generiert und in `data-override-key` am selected-item UND am liedblatt-content-Kind gesetzt.

In `updateLiedblatt()` (Zeile 269), in der `forEach`-Schleife (nach `const objekt = JSON.parse(...)`), nach Zeile 278, Override-Key setzen:

```javascript
// Stabiler Override-Key: objekt.id + Positions-Index
// Format: '{objekt.id}:{index}' — stabil solange Reihenfolge gleich
const overrideKey = `${objekt.id}:${index}`;
content.setAttribute('data-override-key', overrideKey);
// Auch am selected-item für spätere Deserialisierung
selected.setAttribute('data-override-key', overrideKey);
```

Hinweis: `content` ist der DOM-Node der in `#liedblatt-content` eingefügt wird (prüfe die genaue Variable in Zeile 269ff — es könnte `div`, `wrapper` oder ähnlich heißen). Finde die korrekte Variable im updateLiedblatt-Block und setze dort das Attribut.

**engine.js — overrideKey in block.data mitschreiben**: Im Bild-Block (um Zeile 268), den `currentBlocks.push`-Aufruf erweitern, um `overrideKey` in `data` zu speichern:

```javascript
currentBlocks.push({
    type: 'image',
    x: MARGINS.left,
    y: currentY,
    width: effectiveWidth,
    height: imgHeight,
    data: {
        src: imgEl.src,
        naturalWidth: imgEl.naturalWidth,
        naturalHeight: imgEl.naturalHeight,
        overrideKey: item.getAttribute('data-override-key') || ''  // NEU
    }
});
```

Ebenso für Text-Blocks in `pushTextBlock`: flags-Objekt um `overrideKey` erweitern. `pushTextBlock` wird mit `flags` aufgerufen — dem flags-Objekt `overrideKey` hinzufügen. Dazu muss `pushTextBlock` den `overrideKey` vom aufrufenden Item kennen. Im Item-Traversal, vor dem `pushTextBlock`-Aufruf:

```javascript
// overrideKey des aktuellen Items für alle Blöcke dieses Items
const currentItemOverrideKey = item.getAttribute('data-override-key') || '';
// Dann pushTextBlock-Aufrufe mit flags = { ...existingFlags, overrideKey: currentItemOverrideKey }
```

In `pushTextBlock` (Zeile 190), `flags` wird bereits in `currentBlocks.push({ ..., data: { ..., ...flags } })` verbreitet — `overrideKey` wird automatisch in `block.data` landen, wenn es in flags steht.

Für den Spacing-Override in engine.js: Nach jedem Top-Level-Item-Block-Push, `extraSpacingAfter` mit Spacing-Override kombinieren. Der sauberste Ort: am Ende der Item-`continue`-Zweige, NICHT in `pushTextBlock` (da pushTextBlock mehrfach pro Item aufgerufen werden kann). Stattdessen: nach dem Item-Traversal-Block einen abschließenden Spacing-Block pushen:

```javascript
// Am Ende jedes Item-Zweigs (vor continue): Spacing-Override anwenden
const spacingOv = spacingOverrides[currentItemOverrideKey];
if (spacingOv && typeof spacingOv.after === 'number') {
    currentBlocks.push({
        type: 'spacing-override-marker',
        x: MARGINS.left,
        y: currentY,
        width: contentWidth,
        height: 0,
        data: { overrideKey: currentItemOverrideKey, afterPt: spacingOv.after }
    });
    currentY += spacingOv.after;
}
// (Standard-Spacing der Engine läuft parallel — muss NICHT doppelt addiert werden)
// Anmerkung: Standard-Spacing wird bereits durch extraSpacingAfter in pushTextBlock addiert.
// Spacing-Override ERSETZT NICHT den Standard, er addiert auf. Das ist per D-01 akzeptabel.
// Falls Override den Standard ersetzen soll: Standard-Spacing-Additions müssen für dieses Item
// auf 0 gesetzt werden — komplexer. Für v1: Override addiert auf Standard.
```

Für Spacing-Handle in domRenderer: Der Handle wird ZWISCHEN zwei Blöcken gerendert. Er braucht den `overrideKey` des vorherigen Items. Der `spacing-override-marker` Block liefert das via `data.overrideKey`.
  </action>
  <verify>
    <automated>grep -n "data-override-key" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | grep -q "setAttribute" && echo "PASS: data-override-key in liedblattManagement" || echo "FAIL: Attribut-Setzung fehlt"</automated>
    <automated>grep -n "overrideKey.*overrideKey\|overrideKey:" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/engine.js | grep -q "overrideKey" && echo "PASS: overrideKey in engine.js block.data" || echo "FAIL: overrideKey fehlt"</automated>
  </verify>
  <done>
    - Jedes liedblatt-content-Kind-Element hat `data-override-key="{objekt.id}:{index}"`
    - selected-items tragen ebenfalls `data-override-key` für Session-Serialisierung in Plan 04
    - engine.js schreibt `overrideKey` in block.data für Bild- und Text-Blöcke
    - Spacing-Override-Marker-Blöcke werden nach Items mit gesetztem Override eingefügt
  </done>
</task>

<task type="auto">
  <name>Task 2: Drag-Handles in domRenderer.js + Event-Delegation in previewPageBreaks.js</name>
  <files>frontend/js/layout/domRenderer.js, frontend/js/previewPageBreaks.js, frontend/css/dashboard.css</files>
  <action>
**domRenderer.js — Spacing-Handle nach Blöcken**: In `_createBlockElement`, wenn `type === 'spacing-override-marker'`, einen Spacing-Handle rendern:

```javascript
} else if (type === 'spacing-override-marker') {
    return _createSpacingHandle(x, y, width, data);
}
```

Neue private Funktion:

```javascript
function _createSpacingHandle(x, y, width, data) {
    const el = document.createElement('div');
    el.className = 'spacing-handle';
    el.dataset.overrideKey = data.overrideKey;
    el.dataset.currentSpacing = data.afterPt || 0;
    el.style.position = 'absolute';
    el.style.left = `${_ptToPx(x)}px`;
    el.style.top = `${_ptToPx(y)}px`;
    el.style.width = `${_ptToPx(width)}px`;
    el.style.height = '10px';
    el.style.cursor = 'ns-resize';
    el.style.background = 'transparent';
    el.style.zIndex = '10';
    // Visueller Balken in der Mitte
    el.style.display = 'flex';
    el.style.alignItems = 'center';
    el.style.justifyContent = 'center';
    el.setAttribute('title', 'Abstand ziehen');
    const bar = document.createElement('div');
    bar.style.width = '40px';
    bar.style.height = '3px';
    bar.style.background = '#3498db';
    bar.style.borderRadius = '2px';
    bar.style.opacity = '0.5';
    bar.style.pointerEvents = 'none';
    el.appendChild(bar);
    return el;
}
```

**domRenderer.js — Bild-Resize-Handle**: In `_createImageElement` (nach dem Erzeugen des img-Div), am Ende der Funktion den Handle anhängen. Da `_createImageElement` ein absolut positioniertes `div` zurückgibt, muss das äußere div `position: relative` und `overflow: visible` haben:

```javascript
// Am Ende von _createImageElement, vor return:
const resizeHandle = document.createElement('div');
resizeHandle.className = 'image-resize-handle';
resizeHandle.dataset.overrideKey = data.overrideKey || '';
resizeHandle.dataset.naturalWidth = data.naturalWidth || 0;
resizeHandle.dataset.naturalHeight = data.naturalHeight || 0;
resizeHandle.dataset.currentWidthFraction = data.widthFraction || 1.0; // falls von overrideState bekannt
// Höhe und Breite des Bild-Blocks als data-Attribute (für Drag-Berechnung)
resizeHandle.dataset.blockWidthPt = width; // pt — für Umrechnung in Drag-Handler
el.dataset.overrideKey = data.overrideKey || ''; // auch am Elternelement
el.style.overflow = 'visible'; // Handle ragt über Block-Kante heraus
resizeHandle.style.position = 'absolute';
resizeHandle.style.bottom = '-5px';
resizeHandle.style.left = '50%';
resizeHandle.style.width = '40px';
resizeHandle.style.height = '10px';
resizeHandle.style.transform = 'translateX(-50%)';
resizeHandle.style.cursor = 's-resize';
resizeHandle.style.background = '#3498db';
resizeHandle.style.borderRadius = '3px';
resizeHandle.style.opacity = '0.6';
resizeHandle.style.zIndex = '10';
resizeHandle.setAttribute('title', 'Bildgröße ziehen');
el.appendChild(resizeHandle);
```

Hinweis: `_createImageElement` erzeugt aktuell einen Container-div mit absolutem img. Das Attribut `data.overrideKey` kommt aus Plan 01 Task 1 — es ist in block.data vorhanden.

**previewPageBreaks.js — Event-Delegation + initDragHandles**: Neue Funktion `initDragHandles(container)` hinzufügen. Event-Delegation auf dem Container (wird NICHT bei Re-Render zerstört, da Container selbst überlebt):

```javascript
import { setSpacingOverride, setImageSizeOverride } from './layout/overrideState.js';

let _dragState = null;

export function initDragHandles(container) {
    // Bereits registriert? Flag vermeidet Doppel-Registration bei Re-Renders
    if (container._dragHandlesInitialized) return;
    container._dragHandlesInitialized = true;

    container.addEventListener('pointerdown', _onPointerDown);
    container.addEventListener('pointermove', _onPointerMove);
    container.addEventListener('pointerup', _onPointerUp);
    container.addEventListener('pointercancel', _onPointerUp);
}

function _onPointerDown(e) {
    const spacingHandle = e.target.closest('.spacing-handle');
    const imageHandle = e.target.closest('.image-resize-handle');

    if (spacingHandle) {
        _dragState = {
            type: 'spacing',
            overrideKey: spacingHandle.dataset.overrideKey,
            startY: e.clientY,
            startSpacing: parseFloat(spacingHandle.dataset.currentSpacing) || 0
        };
        e.target.setPointerCapture(e.pointerId);
        e.preventDefault();
    } else if (imageHandle) {
        _dragState = {
            type: 'image',
            overrideKey: imageHandle.dataset.overrideKey,
            startY: e.clientY,
            naturalWidth: parseFloat(imageHandle.dataset.naturalWidth) || 1,
            naturalHeight: parseFloat(imageHandle.dataset.naturalHeight) || 1,
            blockWidthPt: parseFloat(imageHandle.dataset.blockWidthPt) || 100,
            startWidthFraction: parseFloat(imageHandle.dataset.currentWidthFraction) || 1.0
        };
        e.target.setPointerCapture(e.pointerId);
        e.preventDefault();
    }
}

const PX_TO_PT = 0.75; // 1px = 0.75pt (bei 96dpi-Annahme der Engine)

function _onPointerMove(e) {
    if (!_dragState) return;

    if (_dragState.type === 'spacing') {
        const deltaYPx = e.clientY - _dragState.startY;
        const deltaYPt = deltaYPx * PX_TO_PT;
        const newSpacing = Math.max(0, _dragState.startSpacing + deltaYPt);
        setSpacingOverride(_dragState.overrideKey, newSpacing);
        _scheduleRelayout();
    } else if (_dragState.type === 'image') {
        // Bild-Resize: deltaY in px → neue Höhe → neue Breite (Seitenverhältnis)
        const deltaYPx = e.clientY - _dragState.startY;
        const deltaYPt = deltaYPx * PX_TO_PT;
        // naturalHeight / naturalWidth = aspectRatio (Höhe/Breite)
        const aspectRatio = _dragState.naturalHeight / _dragState.naturalWidth;
        const startWidthPt = _dragState.startWidthFraction * _dragState.blockWidthPt;
        // DeltaY erhöht Höhe → Breite = neue Höhe / aspectRatio
        const newHeightPt = startWidthPt * aspectRatio + deltaYPt;
        const newWidthPt = Math.max(10, newHeightPt / aspectRatio);
        const newWidthFraction = newWidthPt / _dragState.blockWidthPt;
        setImageSizeOverride(_dragState.overrideKey, Math.min(1.0, newWidthFraction));
        _scheduleRelayout();
    }
}

function _onPointerUp() {
    _dragState = null;
}

let _relayoutTimeout = null;
function _scheduleRelayout() {
    if (_relayoutTimeout) clearTimeout(_relayoutTimeout);
    _relayoutTimeout = setTimeout(() => {
        const formatSelect = document.getElementById('previewFormat');
        const format = formatSelect ? formatSelect.value : 'a5';
        updatePreviewWithPageBreaks(format);
    }, 150);
}
```

In `updatePreviewWithPageBreaks`, nach dem `renderToDOM`-Aufruf, `initDragHandles` aufrufen:

```javascript
renderToDOM(layoutResult, engineConfig, container);
initDragHandles(container); // Drag-Handles registrieren (einmalig via Flag)
```

**CSS** — in `dashboard.css` hinzufügen:

```css
.spacing-handle:hover .spacing-handle > div,
.spacing-handle:hover {
    opacity: 1 !important;
}

.image-resize-handle:hover {
    opacity: 1 !important;
    background: #2980b9 !important;
}

/* Verhindert Textauswahl während Drag */
.preview-page {
    user-select: none;
}
```
  </action>
  <verify>
    <automated>grep -n "spacing-handle\|_createSpacingHandle" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/domRenderer.js | grep -q "spacing-handle" && echo "PASS: Spacing-Handle in domRenderer" || echo "FAIL: fehlt"</automated>
    <automated>grep -n "image-resize-handle\|_createImageResize" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/domRenderer.js | grep -q "image-resize-handle" && echo "PASS: Bild-Handle in domRenderer" || echo "FAIL: fehlt"</automated>
    <automated>grep -n "initDragHandles\|pointerdown\|setPointerCapture" /Users/simonluthe/Documents/HymnoScribe/frontend/js/previewPageBreaks.js | grep -q "initDragHandles" && echo "PASS: initDragHandles in previewPageBreaks" || echo "FAIL: fehlt"</automated>
    <automated>grep -n "_dragHandlesInitialized" /Users/simonluthe/Documents/HymnoScribe/frontend/js/previewPageBreaks.js && echo "PASS: Doppel-Registrierungs-Guard vorhanden" || echo "FAIL: Guard fehlt"</automated>
  </verify>
  <done>
    - Spacing-Handle rendert als horizontaler Balken zwischen Blöcken (nur wenn spacing-override-marker Block vorhanden)
    - Bild-Resize-Handle rendert an Unterkante jedes Bild-Blocks in Vorschau
    - Pointer-Event-Delegation auf Container — überlebt Re-Render
    - setPointerCapture verhindert Pointer-Verlust bei schnellem Drag (per D-01, D-02)
    - Debounce 150ms für Re-Layout nach jedem Drag-Move
    - Seitenverhältnis bleibt erhalten beim Bild-Drag (per D-02)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer-Events → overrideState | Drag-Bewegung erzeugt Float-Werte die in Engine fließen |
| block.data.overrideKey | Aus DOM-Attributen gelesen — kein Server-Input, kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | Rapid Drag triggert zu viele Re-Layouts | mitigate | 150ms Debounce in _scheduleRelayout() |
| T-03-02-02 | Tampering | aspectRatio = NaN wenn naturalWidth=0 | mitigate | Fallback: `_dragState.naturalWidth || 1` in _onPointerDown — Division durch 0 verhindert |
| T-03-02-03 | Information Disclosure | data-override-key im DOM sichtbar | accept | Nur layout-interne Keys, kein PII |
</threat_model>

<verification>
1. `grep -n "spacing-handle\|image-resize-handle" frontend/js/layout/domRenderer.js` — zeigt beide Handle-Typen
2. `grep -n "setPointerCapture" frontend/js/previewPageBreaks.js` — zeigt Capture-Calls
3. `grep -n "_dragHandlesInitialized" frontend/js/previewPageBreaks.js` — zeigt Guard
4. `grep -n "data-override-key" frontend/js/liedblattManagement.js` — zeigt Attribut-Setzung
5. `grep -n "overrideKey" frontend/js/layout/engine.js` — zeigt block.data Eintrag
</verification>

<success_criteria>
- Spacing-Handle erscheint als visueller Balken zwischen Elementen wenn overrides gesetzt
- Drag am Handle ändert spacingOverrides[key] und triggert Re-Layout nach 150ms
- Bild-Handle erscheint an Unterkante jedes Bild-Blocks
- Drag am Bild-Handle ändert widthFraction, Seitenverhältnis bleibt erhalten (per D-02)
- Event-Delegation verhindert Listener-Verlust bei Re-Renders (per Pitfall #2 aus Research)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-wysiwyg-controls-element-reihenfolge/03-02-SUMMARY.md` erstellen.
</output>
