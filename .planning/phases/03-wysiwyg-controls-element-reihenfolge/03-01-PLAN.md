---
phase: 03-wysiwyg-controls-element-reihenfolge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/layout/overrideState.js
  - frontend/js/layout/engine.js
  - frontend/js/layout/domRenderer.js
  - frontend/dashboard.html
  - frontend/js/previewPageBreaks.js
  - frontend/css/dashboard.css
autonomous: true
requirements: [WYSI-03, WYSI-04, WYSI-05]

must_haves:
  truths:
    - "Schriftgröße lässt sich global über ein Dropdown mit Presets (8, 10, 12, 14, 16, 18, 20, 24) und freier Eingabe ändern"
    - "Per-Element-Schriftgröße-Override wird in overrideState.js verwaltet und vom engine.js gelesen"
    - "Per-Element-Font-Size-Override ist über ein Input-Feld neben jedem Element in der Liedblatt-Liste aufrufbar"
    - "Format-Wechsel (A5/DL/A4-schmal/A3-schmal) triggert sofortiges Re-Layout, overrides bleiben erhalten"
    - "Manueller Seitenumbruch ist als Marker in der domRenderer-Vorschau sichtbar"
    - "overrideState.js exportiert getOverrides(), setFontSizeOverride(), serializeOverrides(), deserializeOverrides()"
  artifacts:
    - path: "frontend/js/layout/overrideState.js"
      provides: "Zentrales Override-State-Modul für alle Override-Typen"
      exports: [getOverrides, setSpacingOverride, setImageSizeOverride, setFontSizeOverride, serializeOverrides, deserializeOverrides, clearOverrides]
    - path: "frontend/js/layout/engine.js"
      provides: "calculateLayout mit optionalem overrides-Parameter"
      contains: "overrides = {}"
    - path: "frontend/js/layout/domRenderer.js"
      provides: "Seitenumbruch-Marker-Rendering"
      contains: "page-break-marker"
    - path: "frontend/dashboard.html"
      provides: "Erweiterte Font-Size-Steuerung mit Dropdown + freier Eingabe"
      contains: "id=\"fontSizePresets\""
  key_links:
    - from: "frontend/js/previewPageBreaks.js"
      to: "frontend/js/layout/overrideState.js"
      via: "import { getOverrides } from './layout/overrideState.js'"
      pattern: "getOverrides"
    - from: "frontend/js/layout/engine.js"
      to: "overrides parameter"
      via: "calculateLayout(items, config, fonts, overrides = {})"
      pattern: "fontSizeOverrides\\[.*\\]"
    - from: "#selected-items .liedblatt-item"
      to: "overrideState.js setFontSizeOverride"
      via: "per-Element Font-Size Input im Seitenpanel"
      pattern: "setFontSizeOverride"
---

<objective>
Override-State-Modul anlegen, Layout-Engine mit overrides-Parameter erweitern, Schriftgröße-Presets in die Toolbar integrieren, per-Element-Font-Size-UI im Seitenpanel verdrahten, Format-Switch mit sofortigem Re-Layout implementieren. Manuelle Seitenumbrüche werden im domRenderer als sichtbarer Marker gerendert.

Purpose: Baut die technische Grundlage für alle Override-Typen (spacing, image, fontSize) die in Plan 02 und 03 genutzt werden.
Output: overrideState.js, erweiterte engine.js-Signatur, funktionierende Font-Size-Presets (global + per-Element), Format-Switch, Seitenumbruch-Marker in Vorschau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-unified-layout-engine/02-04-SUMMARY.md

<interfaces>
<!-- Bestehende Interfaces — Executor benötigt diese ohne Codebase-Erkundung -->

Aus frontend/js/layout/engine.js:
```javascript
// Aktuelle Signatur (Zeile 160)
export async function calculateLayout(items, config, fonts) {
    const format = config.format || 'a5';
    // config = { format, fontSize, lineHeight, textAlign, fontFamily }
    // fonts = { regular, bold, italic, bolditalic } PDFFont-Objekte
    // items = Array von HTMLElement (DOM-Kinder von #liedblatt-content)
    //
    // Bildblock (Zeilen 255-281): imgEl.naturalHeight / naturalWidth * contentWidth
    // — gibt type:'image' Block mit { src, naturalWidth, naturalHeight }
    //
    // Item-Key für Overrides: item.getAttribute('data-liedblatt-id') existiert NOCH NICHT
    // — wird in Plan 02 über updateLiedblatt() gesetzt
}
```

Aus frontend/js/previewPageBreaks.js:
```javascript
export function updatePreviewWithPageBreaks(format = 'a5') {
    // Zeile 84: const layoutResult = await calculateLayout(items, engineConfig, fonts);
    // Zeile 87: renderToDOM(layoutResult, engineConfig, container);
    // Debounce: 150ms setTimeout (Zeile 48)
}
export function initPreviewFormatSelector() { /* existiert — Zeile 979+ */ }
```

Aus frontend/js/layout/domRenderer.js:
```javascript
export function renderToDOM(layoutResult, config, container) {
    container.innerHTML = ''; // leert bei jedem Re-Render
    // Rendert type:'text', type:'image', type:'icon' Blöcke
    // type:'spacing' wird ignoriert (kein DOM-Element)
}
```

Aus frontend/dashboard.html (Zeile 196-236):
```html
<!-- Bestehendes Format-Dropdown -->
<select id="previewFormat">
    <option value="a5">Din A5</option>
    <option value="dl">DIN Lang</option>
    <option value="a4-schmal">DIN A4 schmal</option>
    <option value="a3-schmal">DIN A3 schmal</option>
</select>

<!-- Bestehende Schriftgröße-Eingabe (config-modal) -->
<input type="number" id="fontSize" min="8" max="24" value="12">
```

Aus frontend/js/layout/engine.js — Seitenumbruch-Erkennung (Zeile 228):
```javascript
if (item.classList.contains('page-break')) {
    newPage();
    continue;
}
// engine.js gibt für page-break KEINEN Block zurück — domRenderer zeigt nichts
// Muss als type:'page-break-marker' Block explizit eingebaut werden
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: overrideState.js erstellen + engine.js Signatur erweitern + domRenderer.js Marker</name>
  <files>frontend/js/layout/overrideState.js, frontend/js/layout/engine.js, frontend/js/layout/domRenderer.js</files>
  <action>
**overrideState.js neu erstellen** (`frontend/js/layout/overrideState.js`):

```javascript
/**
 * Override-State — verwaltet per-Instanz-Overrides für Spacing, Bildgröße und Schriftgröße.
 * Key: stabiler liedblatt-item-Key (wird in Plan 02 durch updateLiedblatt() gesetzt).
 * Alle Werte in pt (kanonische Einheit der Layout-Engine).
 */

const _state = {
    spacingOverrides: {},    // { 'key': { after: 20 } } — pt nach Block
    imageSizeOverrides: {},  // { 'key': { widthFraction: 0.7 } } — 0.1–1.0
    fontSizeOverrides: {}    // { 'key': { fontSize: 10 } } — pt
};

export function getOverrides() {
    return {
        spacingOverrides: { ..._state.spacingOverrides },
        imageSizeOverrides: { ..._state.imageSizeOverrides },
        fontSizeOverrides: { ..._state.fontSizeOverrides }
    };
}

export function setSpacingOverride(key, afterPt) {
    const clamped = Math.max(0, Math.min(100, parseFloat(afterPt) || 0));
    _state.spacingOverrides[key] = { after: clamped };
}

export function setImageSizeOverride(key, widthFraction) {
    const clamped = Math.max(0.1, Math.min(1.0, parseFloat(widthFraction) || 1.0));
    _state.imageSizeOverrides[key] = { widthFraction: clamped };
}

export function setFontSizeOverride(key, fontSizePt) {
    const clamped = Math.max(6, Math.min(36, parseFloat(fontSizePt) || 12));
    _state.fontSizeOverrides[key] = { fontSize: clamped };
}

export function clearOverride(key) {
    delete _state.spacingOverrides[key];
    delete _state.imageSizeOverrides[key];
    delete _state.fontSizeOverrides[key];
}

export function clearOverrides() {
    _state.spacingOverrides = {};
    _state.imageSizeOverrides = {};
    _state.fontSizeOverrides = {};
}

export function serializeOverrides() {
    return JSON.stringify(_state);
}

export function deserializeOverrides(json) {
    try {
        const parsed = JSON.parse(json);
        if (parsed && typeof parsed === 'object') {
            _state.spacingOverrides  = parsed.spacingOverrides  || {};
            _state.imageSizeOverrides = parsed.imageSizeOverrides || {};
            _state.fontSizeOverrides = parsed.fontSizeOverrides  || {};
        }
    } catch (e) {
        console.error('overrideState: Fehler beim Deserialisieren:', e);
    }
}
```

**engine.js Signatur erweitern**: Die Funktion `calculateLayout` (Zeile 160) erhält einen vierten Parameter `overrides = {}`. Innerhalb der Funktion, direkt nach den bestehenden `const`-Deklarationen (nach Zeile 166), folgendes hinzufügen:

```javascript
const { spacingOverrides = {}, imageSizeOverrides = {}, fontSizeOverrides = {} } = overrides;
```

In `pushTextBlock` (Zeile 190): `fontSize`-Parameter wird bereits übergeben — keine Änderung nötig. Der Override wird beim Item-Traversal in Task 2 von Plan 02 angewendet.

Im Bild-Block (ab Zeile 253): Nach der `imgEl`-Prüfung, Override-Breite berechnen:

```javascript
// Bild-Block Override (nach imgEl-Prüfung, vor imgHeight-Berechnung)
const itemKey = item.getAttribute('data-override-key') || '';
const imgSizeOverride = imageSizeOverrides[itemKey];
const effectiveWidth = imgSizeOverride
    ? Math.max(10, imgSizeOverride.widthFraction * contentWidth)
    : contentWidth;
const imgHeight = (imgEl.naturalHeight && imgEl.naturalWidth)
    ? (imgEl.naturalHeight / imgEl.naturalWidth) * effectiveWidth
    : 150;
// Ersetze contentWidth durch effectiveWidth in: width: contentWidth → width: effectiveWidth
// und in: x: MARGINS.left (bleibt, Bild bleibt linksbündig)
```

Spacing-Override nach jedem Block-Push: Nach jeder `currentY += blockHeight + (extraSpacingAfter || 0)` (Zeile 219) Zeile:

```javascript
// Spacing-Override anwenden falls gesetzt
const spacingOverride = spacingOverrides[item.getAttribute('data-override-key') || ''];
if (spacingOverride && typeof spacingOverride.after === 'number') {
    // Override addieren statt Standard-Spacing
    currentY += spacingOverride.after;
} else {
    // Standard-Spacing bleibt (bereits addiert in extraSpacingAfter)
}
```

Hinweis: `data-override-key` wird in Plan 02 von `updateLiedblatt()` auf jedem Item gesetzt. In Plan 01 ist der Override-Key noch nicht im DOM — alle Override-Lookups geben undefined zurück und verhalten sich wie bisher.

**WYSI-05 — Seitenumbruch-Marker in engine.js**: Im page-break-Block (Zeile 228), STATT nur `newPage()` aufzurufen, einen Marker-Block auf der vorherigen Seite hinzufügen:

```javascript
if (item.classList.contains('page-break')) {
    // Marker am Ende der aktuellen Seite hinzufügen (für domRenderer)
    currentBlocks.push({
        type: 'page-break-marker',
        x: MARGINS.left,
        y: currentY,
        width: contentWidth,
        height: 16,
        data: { label: 'Manueller Seitenumbruch' }
    });
    newPage();
    continue;
}
```

**domRenderer.js — Marker-Rendering**: In `_createBlockElement` (Zeile 89), neuen Case hinzufügen:

```javascript
} else if (type === 'page-break-marker') {
    return _createPageBreakMarkerElement(x, y, width, data);
}
```

Neue private Funktion `_createPageBreakMarkerElement` in domRenderer.js hinzufügen:

```javascript
function _createPageBreakMarkerElement(x, y, width, data) {
    const el = document.createElement('div');
    el.className = 'page-break-marker';
    el.style.position = 'absolute';
    el.style.left = `${_ptToPx(x)}px`;
    el.style.top = `${_ptToPx(y)}px`;
    el.style.width = `${_ptToPx(width)}px`;
    el.style.height = '16px';
    el.style.borderTop = '2px dashed #e74c3c';
    el.style.display = 'flex';
    el.style.alignItems = 'center';
    el.style.justifyContent = 'center';
    el.style.fontSize = '10px';
    el.style.color = '#e74c3c';
    el.style.pointerEvents = 'none';
    el.style.userSelect = 'none';
    const label = document.createElement('span');
    label.textContent = data.label || 'Seitenumbruch';
    el.appendChild(label);
    return el;
}
```
  </action>
  <verify>
    <automated>grep -n "export function getOverrides\|export function serializeOverrides\|export function deserializeOverrides\|export function setSpacingOverride\|export function setImageSizeOverride\|export function setFontSizeOverride\|export function clearOverrides" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/overrideState.js | wc -l | grep -q "^7$" && echo "PASS: overrideState.js hat alle 7 Exporte" || echo "FAIL: Exporte fehlen"</automated>
    <automated>grep -n "overrides = {}" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/engine.js && echo "PASS: engine.js hat overrides-Parameter" || echo "FAIL: engine.js Parameter fehlt"</automated>
    <automated>grep -n "page-break-marker" /Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/domRenderer.js && echo "PASS: Seitenumbruch-Marker in domRenderer" || echo "FAIL: Marker fehlt"</automated>
  </verify>
  <done>
    - overrideState.js existiert mit 7 benannten Exporten
    - calculateLayout-Signatur akzeptiert vierten Parameter `overrides = {}`
    - imageSizeOverrides wird in engine.js im Bild-Block konsumiert (effectiveWidth)
    - page-break-marker Block-Typ in engine.js UND domRenderer.js implementiert
  </done>
</task>

<task type="auto">
  <name>Task 2: Globale Font-Size-Presets in Toolbar + Format-Switch verdrahten</name>
  <files>frontend/dashboard.html, frontend/js/previewPageBreaks.js, frontend/css/dashboard.css</files>
  <action>
**WYSI-03 — Font-Size-Presets**: Das bestehende `fontSize`-Input (dashboard.html Zeile 236) liegt im config-modal. Für den WYSIWYG-Workflow muss die Schriftgröße direkt in der Toolbar (right-panel oder über dem Liedblatt) zugänglich sein. In `dashboard.html` direkt nach dem `preview-format-container` div (nach Zeile 204) einfügen:

```html
<div class="font-size-control-container">
    <label for="fontSizePresets">Schriftgröße:</label>
    <select id="fontSizePresets">
        <option value="8">8 pt</option>
        <option value="10">10 pt</option>
        <option value="12" selected>12 pt</option>
        <option value="14">14 pt</option>
        <option value="16">16 pt</option>
        <option value="18">18 pt</option>
        <option value="20">20 pt</option>
        <option value="24">24 pt</option>
        <option value="custom">Benutzerdefiniert</option>
    </select>
    <input type="number" id="fontSizeCustom" min="6" max="36" step="1" value="12"
           style="display:none; width:60px;" placeholder="pt">
</div>
```

In `previewPageBreaks.js`, in `initPreviewFormatSelector()` (Zeile 979+), Event-Handler für fontSizePresets und fontSizeCustom ergänzen. Der Handler liest den Wert, schreibt ihn in `globalConfig.fontSize` (in pt, nach Umrechnung falls nötig — globalConfig.fontSize ist in px, Engine rechnet mit `* 0.75` auf pt) und triggert `updatePreviewWithPageBreaks(currentFormat)`:

```javascript
// Font-Size-Presets Handler (in initPreviewFormatSelector ergänzen)
const fontSizePresets = document.getElementById('fontSizePresets');
const fontSizeCustom = document.getElementById('fontSizeCustom');

if (fontSizePresets) {
    fontSizePresets.addEventListener('change', () => {
        if (fontSizePresets.value === 'custom') {
            fontSizeCustom.style.display = 'inline-block';
            fontSizeCustom.focus();
        } else {
            fontSizeCustom.style.display = 'none';
            // pt-Wert → px: globalConfig nutzt px (engine multipliziert mit 0.75)
            // Da Engine bereits pt erwartet und previewPageBreaks.js mit pxToPt konvertiert:
            // globalConfig.fontSize = pt-Wert / 0.75 (px-Äquivalent)
            const ptValue = parseFloat(fontSizePresets.value);
            globalConfig.fontSize = ptValue / 0.75;
            const currentFormat = document.getElementById('previewFormat')?.value || 'a5';
            updatePreviewWithPageBreaks(currentFormat);
        }
    });

    fontSizeCustom.addEventListener('change', () => {
        const ptValue = Math.max(6, Math.min(36, parseFloat(fontSizeCustom.value) || 12));
        globalConfig.fontSize = ptValue / 0.75;
        const currentFormat = document.getElementById('previewFormat')?.value || 'a5';
        updatePreviewWithPageBreaks(currentFormat);
    });
}
```

**WYSI-04 — Format-Switch**: `initPreviewFormatSelector()` hat bereits einen Event-Handler für `#previewFormat` (prüfen ob vorhanden). Falls der Handler nur das DOM-Select initialisiert aber NICHT `updatePreviewWithPageBreaks` direkt aufruft, sicherstellen dass der Handler so aussieht:

```javascript
const formatSelect = document.getElementById('previewFormat');
if (formatSelect) {
    formatSelect.addEventListener('change', () => {
        updatePreviewWithPageBreaks(formatSelect.value);
    });
    // Beim Start mit aktuellem Format rendern
    updatePreviewWithPageBreaks(formatSelect.value || 'a5');
}
```

Wichtig: `getOverrides()` aus overrideState.js importieren und in `updatePreviewWithPageBreaks` an `calculateLayout` übergeben:

```javascript
// In previewPageBreaks.js, Import ergänzen:
import { getOverrides } from './layout/overrideState.js';

// In updatePreviewWithPageBreaks, Zeile 84 ändern von:
const layoutResult = await calculateLayout(items, engineConfig, fonts);
// zu:
const layoutResult = await calculateLayout(items, engineConfig, fonts, getOverrides());
```

**CSS** — In `frontend/css/dashboard.css` hinzufügen:

```css
.font-size-control-container {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 0;
    font-size: 13px;
}

.font-size-control-container label {
    white-space: nowrap;
}

.font-size-control-container select,
.font-size-control-container input[type="number"] {
    padding: 3px 6px;
    border: 1px solid #ccc;
    border-radius: 3px;
    font-size: 13px;
}
```
  </action>
  <verify>
    <automated>grep -n "fontSizePresets\|fontSizeCustom" /Users/simonluthe/Documents/HymnoScribe/frontend/dashboard.html | grep -q "fontSizePresets" && echo "PASS: fontSizePresets in HTML" || echo "FAIL: Element fehlt"</automated>
    <automated>grep -n "getOverrides\|from.*overrideState" /Users/simonluthe/Documents/HymnoScribe/frontend/js/previewPageBreaks.js && echo "PASS: overrideState Import in previewPageBreaks" || echo "FAIL: Import fehlt"</automated>
    <automated>grep -n "getOverrides" /Users/simonluthe/Documents/HymnoScribe/frontend/js/previewPageBreaks.js | grep -q "calculateLayout" && echo "PASS: overrides an calculateLayout übergeben" || echo "FAIL: Übergabe fehlt"</automated>
  </verify>
  <done>
    - Toolbar zeigt fontSizePresets-Dropdown (8, 10, 12, 14, 16, 18, 20, 24pt + Benutzerdefiniert) direkt über dem Liedblatt
    - Auswahl eines Presets triggert sofortiges Re-Layout via updatePreviewWithPageBreaks (per D-03)
    - "Benutzerdefiniert" blendet ein Zahlenfeld ein, Enter/Blur triggert Re-Layout
    - Format-Switch (previewFormat) triggert calculateLayout mit übergebenen overrides (per D-04)
    - overrides bleiben bei Format-Wechsel erhalten (getOverrides() liefert aktuellen State)
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-Element Font-Size-Override UI im Seitenpanel</name>
  <files>frontend/js/liedblattManagement.js, frontend/css/dashboard.css</files>
  <action>
**Per-Element Font-Size-Override UI** (per D-03: "Schriftgröße global als Default + Override pro Element möglich"):

In `liedblattManagement.js`, in der Funktion die Liedblatt-Items rendert (vermutlich `addToSelected()` oder das umgebende Item-Div in `updateLiedblatt()`), ein kleines Font-Size-Override-Input neben jedem Element in der Liedblatt-Liste hinzufügen.

Das Input erscheint als Teil der Element-Zeile im `#selected-items`-Container (rechts neben Titel/Handle), NICHT im expandierten Optionen-Panel.

```javascript
// In addToSelected() oder beim Erstellen des selected-item-Divs,
// nach dem Erstellen von title/label des Elements:

function createFontSizeOverrideControl(itemKey) {
    const wrapper = document.createElement('div');
    wrapper.classList.add('font-size-override-control');
    wrapper.title = 'Schriftgröße für dieses Element überschreiben (leer = global)';

    const label = document.createElement('span');
    label.textContent = 'pt';
    label.classList.add('font-size-override-label');

    const input = document.createElement('input');
    input.type = 'number';
    input.min = '6';
    input.max = '36';
    input.step = '1';
    input.placeholder = '—';
    input.classList.add('font-size-override-input');
    input.dataset.overrideKey = itemKey;

    // Import setFontSizeOverride am Dateianfang von liedblattManagement.js hinzufügen:
    // import { setFontSizeOverride, clearOverride } from './layout/overrideState.js';

    input.addEventListener('change', () => {
        const val = input.value.trim();
        if (val === '' || isNaN(parseFloat(val))) {
            // Leeres Feld: Override löschen → globaler Default gilt
            clearOverride(itemKey);
            input.value = '';
        } else {
            setFontSizeOverride(itemKey, parseFloat(val));
        }
        // Re-Layout triggern
        const currentFormat = document.getElementById('previewFormat')?.value || 'a5';
        if (typeof updatePreviewWithPageBreaks === 'function') {
            updatePreviewWithPageBreaks(currentFormat);
        }
    });

    wrapper.appendChild(input);
    wrapper.appendChild(label);
    return wrapper;
}
```

**Import-Ergänzung am Dateianfang von liedblattManagement.js**:

```javascript
import { setFontSizeOverride, clearOverride } from './layout/overrideState.js';
```

**Integration**: `createFontSizeOverrideControl(itemKey)` wird in `addToSelected()` aufgerufen, direkt nachdem das Element-Div erstellt und der Titel gesetzt wurde. `itemKey` ist dabei `data-override-key`-Attribut des Items (wird in Plan 02 gesetzt — in Plan 01 kann ein temporärer Key `item-${Date.now()}` genutzt werden, Plan 02 stabilisiert ihn).

Das resultierende Element-Div in der Liste sieht so aus:
```
[⠿ Handle] [Titel des Elements] [___pt] [✕ Entfernen]
                                  ^input
```

**CSS** in `dashboard.css` hinzufügen:

```css
.font-size-override-control {
    display: inline-flex;
    align-items: center;
    gap: 2px;
    margin-left: auto;
    flex-shrink: 0;
}

.font-size-override-input {
    width: 40px;
    padding: 2px 4px;
    font-size: 11px;
    border: 1px solid #ccc;
    border-radius: 3px;
    text-align: right;
}

.font-size-override-input:focus {
    border-color: #4a90e2;
    outline: none;
}

.font-size-override-label {
    font-size: 10px;
    color: #888;
}
```
  </action>
  <verify>
    <automated>grep -n "setFontSizeOverride\|font-size-override\|createFontSizeOverrideControl" /Users/simonluthe/Documents/HymnoScribe/frontend/js/liedblattManagement.js | grep -q "setFontSizeOverride" && echo "PASS: setFontSizeOverride in liedblattManagement.js aufgerufen" || echo "FAIL: setFontSizeOverride fehlt"</automated>
    <automated>grep -n "font-size-override-input\|font-size-override-control" /Users/simonluthe/Documents/HymnoScribe/frontend/css/dashboard.css | grep -q "font-size-override" && echo "PASS: CSS-Klassen vorhanden" || echo "FAIL: CSS fehlt"</automated>
  </verify>
  <done>
    - Jedes Element in der Liedblatt-Liste hat ein kleines pt-Input-Feld (Placeholder: "—")
    - Eingabe eines Werts ruft setFontSizeOverride(itemKey, pt) auf und triggert Re-Layout (per D-03)
    - Leeres Feld löscht den Override — globale Schriftgröße gilt wieder
    - Import von setFontSizeOverride und clearOverride in liedblattManagement.js ergänzt
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Override-Values → engine.js | Werte kommen aus DOM-Interaktion (Drag, Input) — müssen geclampt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | overrideState.js clamp-Logik | mitigate | setSpacingOverride: Math.max(0, Math.min(100, ...)); setImageSizeOverride: Math.max(0.1, Math.min(1.0, ...)); setFontSizeOverride: Math.max(6, Math.min(36, ...)) — bereits in Task 1 implementiert |
| T-03-01-02 | Denial of Service | fontSizeCustom input — NaN/Infinity | mitigate | parseFloat(value) || 12 Guard + clamp in Handler |
| T-03-01-03 | Information Disclosure | overrideState in localStorage | accept | Nur Layout-Präferenzen, keine PII — localStorage für Session-Persistenz ist etabliertes Pattern |
</threat_model>

<verification>
1. `grep -rn "export function" frontend/js/layout/overrideState.js` — zeigt 7 Exporte
2. `grep -n "calculateLayout.*getOverrides\|getOverrides.*calculateLayout" frontend/js/previewPageBreaks.js` — zeigt Übergabe
3. `grep -n "page-break-marker" frontend/js/layout/engine.js frontend/js/layout/domRenderer.js` — zeigt je mind. 1 Treffer
4. `grep -n "fontSizePresets" frontend/dashboard.html` — zeigt Element im HTML
5. `grep -n "overrides = {}" frontend/js/layout/engine.js` — zeigt erweiterte Signatur
6. `grep -n "setFontSizeOverride" frontend/js/liedblattManagement.js` — zeigt per-Element-Override-Verdrahtung
</verification>

<success_criteria>
- overrideState.js existiert und ist fehlerfrei importierbar
- engine.js akzeptiert calculateLayout(items, config, fonts, overrides = {}) und wendet imageSizeOverrides an
- previewPageBreaks.js übergibt getOverrides() an calculateLayout
- Schriftgröße-Dropdown sichtbar in Toolbar, Änderung triggert Re-Layout (global)
- Jedes Liedblatt-Item hat ein pt-Input für per-Element-Font-Size-Override (per D-03)
- Format-Wechsel löscht keine Overrides (getOverrides() gibt State zurück, nicht DOM-State)
- Manueller Seitenumbruch rendert roten gestrichelten Marker in Vorschau
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-wysiwyg-controls-element-reihenfolge/03-01-SUMMARY.md` erstellen.
</output>
