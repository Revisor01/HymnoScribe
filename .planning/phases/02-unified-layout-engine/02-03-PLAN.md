---
phase: 02-unified-layout-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/js/layout/pdfRenderer.js
  - frontend/js/layout/domRenderer.js
autonomous: true
requirements:
  - LYOT-02
  - LYOT-03
must_haves:
  truths:
    - "pdfRenderer.js nimmt LayoutResult entgegen und zeichnet es via pdf-lib auf PDFDocument-Seiten"
    - "domRenderer.js nimmt LayoutResult entgegen und rendert seitenweise weisse Blätter in den Container"
    - "Die Vorschau zeigt separate Seiten-Divs (weisse Rechtecke im Seitenverhältnis des Formats), nicht einen langen Scroll-Container mit Umbruchmarkern"
    - "Beide Renderer sind eigenständige Module — kein gegenseitiger Import"
    - "Y-Koordinaten-Inversion ist nur im pdfRenderer (engineY→pdfY = pageHeight - engineY - blockHeight)"
  artifacts:
    - path: "frontend/js/layout/pdfRenderer.js"
      provides: "renderToPDF(layoutResult, config, fonts, logoArrayBuffer) → PDFDocument"
      exports: ["renderToPDF"]
    - path: "frontend/js/layout/domRenderer.js"
      provides: "renderToDOM(layoutResult, config, container) — synchron"
      exports: ["renderToDOM"]
  key_links:
    - from: "frontend/js/layout/pdfRenderer.js"
      to: "frontend/js/layout/engine.js LayoutResult"
      via: "layoutResult.pages[].blocks[] iterieren"
      pattern: "for.*pages.*for.*blocks"
    - from: "frontend/js/layout/domRenderer.js"
      to: "#liedblatt-content DOM-Container"
      via: "container.innerHTML = '' dann Seiten-Divs appendChild"
      pattern: "preview-page.*position.*relative"
---

<objective>
Erstellt pdfRenderer.js und domRenderer.js — die zwei Renderer der Layout-Engine. Beide konsumieren den LayoutResult aus engine.js und zeichnen ihn in ihr jeweiliges Medium. Der domRenderer ersetzt die bisherige Umbruchmarker-Logik durch seitenweise Blatt-Darstellung.

Purpose: Nach diesem Plan gibt es eine vollständige, aber noch nicht integrierte Engine. generatePDF.js und previewPageBreaks.js nutzen die neuen Renderer noch nicht — die Verdrahtung erfolgt in Plan 02-04.

Output: pdfRenderer.js und domRenderer.js in frontend/js/layout/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-unified-layout-engine/02-CONTEXT.md
@.planning/phases/02-unified-layout-engine/02-RESEARCH.md
@.planning/phases/02-unified-layout-engine/02-02-SUMMARY.md

<interfaces>
<!-- engine.js Exports (aus Plan 02-02) -->
```javascript
// LayoutResult-Struktur (alle Werte pt, y=0 = Seitenoberseite)
{
    pages: [
        {
            pageNumber: 1,
            blocks: [
                {
                    type: 'text',    // 'text' | 'image' | 'icon' | 'spacing'
                    x: 20,           // pt
                    y: 30,           // pt von Seitenoberseite
                    width: 379.53,   // pt
                    height: 16.8,    // pt
                    data: {
                        lines: ['Kyrie eleison'],  // vorberechnete Zeilen (für type='text')
                        fontSize: 14,              // pt
                        fontStyle: 'regular',      // 'regular'|'bold'|'italic'|'boldItalic'
                        alignment: 'center',
                        lineHeight: 1.5,
                        isCopyright: false,
                        isRefrain: false,
                        isStrophe: false,
                        // type='image': src, naturalWidth, naturalHeight
                        // type='icon': iconType
                    }
                }
            ]
        }
    ],
    totalPages: 2
}
```

<!-- fontManager.js Exports (aus Plan 02-01) -->
```javascript
import { embedFontsInDoc, getFontForStyle } from './fontManager.js';
// embedFontsInDoc(doc, arrayBuffers) → { regular, bold, italic, bolditalic } PDFFont-Objekte
// getFontForStyle(fonts, bold, italic) → PDFFont
```

<!-- constants.js Exports -->
```javascript
import { PAGE_SIZES, PT_TO_PX, MARGINS } from './constants.js';
// PT_TO_PX = 96/72 = 1.3333
// PAGE_SIZES['a5'] = { width: 419.53, height: 595.28 }
```

<!-- pdf-lib API (via CDN global window.PDFLib) -->
```javascript
// pdfRenderer nutzt: PDFLib (global), doc.addPage([width, height])
// page.drawText(text, { x, y, size, font, color })
// page.drawImage(image, { x, y, width, height })
// Y-Koordinate in pdf-lib: y=0 = Seitenunterkante → Inversion nötig
// pdfY = pageHeight - engineY - blockHeight  (für Textbasislinie: pdfY = pageHeight - engineY - fontSize)
```

<!-- generatePDF.js drawText Signatur (aktuell, Zeilen 82-160) -->
```javascript
// Aktuell: page.drawText(line, { x, y: currentY, size: fontSize, font, lineHeight, maxWidth })
// pdfRenderer übernimmt diese Zeichenlogik, liest aber aus block.data statt DOM
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pdfRenderer.js — LayoutResult zu pdf-lib</name>
  <files>frontend/js/layout/pdfRenderer.js</files>
  <read_first>
    - frontend/js/layout/engine.js (LayoutResult-Struktur und exports verstehen)
    - frontend/js/layout/constants.js (PAGE_SIZES, MARGINS)
    - frontend/js/layout/fontManager.js (embedFontsInDoc, getFontForStyle)
    - frontend/js/generatePDF.js (Zeilen 82-160: drawText-Logik; Zeilen 41-69: addNewPage mit Logo; Zeilen 200-340: drawImage; Zeilen 140-165: Textausrichtung center/right/justify)
    - .planning/phases/02-unified-layout-engine/02-RESEARCH.md (Pitfall 1: Y-Koordinaten-Inversion)
  </read_first>
  <action>
Erstelle frontend/js/layout/pdfRenderer.js. Diese Datei zeichnet einen LayoutResult auf ein pdf-lib PDFDocument.

**Kernanforderung:** Y-Koordinaten invertieren. engine.js rechnet y=0 oben (DOM-Konvention). pdf-lib rechnet y=0 unten. Inversion: `pdfY = pageHeight - engineY - blockHeight`. Für Textbasislinie (drawText): `pdfY = pageHeight - engineY - fontSize` (da drawText y = Textbasislinie, nicht Elementoberseite).

```javascript
// frontend/js/layout/pdfRenderer.js
import { embedFontsInDoc, getFontForStyle } from './fontManager.js';
import { PAGE_SIZES, MARGINS } from './constants.js';

const { rgb } = window.PDFLib;  // CDN global

/**
 * Rendert einen LayoutResult als pdf-lib PDFDocument.
 * @param {LayoutResult} layoutResult — Ausgabe von engine.calculateLayout()
 * @param {Object} config — { format, fontFamily, lineHeight, textAlign }
 * @param {Object} arrayBuffers — { regular, bold, italic, bolditalic } Font-ArrayBuffers
 * @param {ArrayBuffer|null} logoArrayBuffer — Logo-Bild als ArrayBuffer (optional)
 * @returns {Promise<PDFDocument>}
 */
export async function renderToPDF(layoutResult, config, arrayBuffers, logoArrayBuffer) {
    const { PDFDocument } = window.PDFLib;
    const fontkit = window.fontkit;

    const doc = await PDFDocument.create();
    doc.registerFontkit(fontkit);

    // Fonts einbetten (arrayBuffers kommen vom fontManager, wurden gecached)
    const fonts = await embedFontsInDoc(doc, arrayBuffers);

    const format = config.format || 'a5';
    const pageSize = PAGE_SIZES[format] || PAGE_SIZES['a5'];
    const lineHeightFactor = config.lineHeight || 1.5;

    // Logo einbetten falls vorhanden
    let logoImage = null;
    if (logoArrayBuffer) {
        try {
            logoImage = await _embedLogo(doc, logoArrayBuffer);
        } catch (e) {
            console.warn('pdfRenderer: Logo konnte nicht eingebettet werden:', e.message);
        }
    }

    // Seiten rendern
    for (const pageData of layoutResult.pages) {
        const page = doc.addPage([pageSize.width, pageSize.height]);

        // Logo auf jede Seite (oben rechts, 30pt Höhe, 30% Deckkraft)
        if (logoImage) {
            _drawLogo(page, logoImage, pageSize);
        }

        // Blöcke zeichnen
        for (const block of pageData.blocks) {
            try {
                await _drawBlock(page, block, pageSize, fonts, lineHeightFactor, config);
            } catch (e) {
                console.error('pdfRenderer: Block-Render-Fehler:', e.message, block);
            }
        }
    }

    return doc;
}

// --- Private Hilfsfunktionen ---

async function _drawBlock(page, block, pageSize, fonts, lineHeightFactor, config) {
    const { type, x, y, height, data } = block;

    if (type === 'text') {
        await _drawTextBlock(page, x, y, height, data, pageSize, fonts, lineHeightFactor);
    } else if (type === 'image') {
        await _drawImageBlock(page, x, y, block.width, height, data, pageSize);
    } else if (type === 'icon') {
        // Icons: Trennlinie als einfache Linie (Platzhalter — erweiterbar)
        const pdfY = pageSize.height - y - height / 2;
        page.drawLine({
            start: { x, y: pdfY },
            end:   { x: x + block.width, y: pdfY },
            thickness: 0.5,
            color: rgb(0.7, 0.7, 0.7),
        });
    }
}

async function _drawTextBlock(page, engineX, engineY, blockHeight, data, pageSize, fonts, lineHeightFactor) {
    const { lines, fontSize, fontStyle, alignment, lineHeight: lh, isCopyright, underline } = data;
    const font = getFontForStyle(fonts,
        fontStyle === 'bold' || fontStyle === 'boldItalic',
        fontStyle === 'italic' || fontStyle === 'boldItalic');
    const actualLineHeight = lh || lineHeightFactor;
    const lineHeightPt = fontSize * actualLineHeight;

    // Y-Inversion: engineY ist Oberkante des Blocks (y=0 oben)
    // Erste Textzeile Basislinie: pageHeight - engineY - fontSize
    let currentPdfY = pageSize.height - engineY - fontSize;

    for (const line of (lines || [])) {
        if (!line.trim()) { currentPdfY -= lineHeightPt; continue; }

        const contentWidth = pageSize.width - MARGINS.left - MARGINS.right;
        let xPos = engineX;

        if (alignment === 'center') {
            const lineWidth = await font.widthOfTextAtSize(line, fontSize);
            xPos = engineX + (contentWidth - lineWidth) / 2;
        } else if (alignment === 'right') {
            const lineWidth = await font.widthOfTextAtSize(line, fontSize);
            xPos = engineX + contentWidth - lineWidth;
        }

        page.drawText(line, {
            x: xPos,
            y: currentPdfY,
            size: fontSize,
            font,
            color: rgb(0, 0, 0),
        });

        if (underline) {
            const lw = await font.widthOfTextAtSize(line, fontSize);
            page.drawLine({
                start: { x: xPos, y: currentPdfY - 2 },
                end:   { x: xPos + lw, y: currentPdfY - 2 },
                thickness: 0.5,
                color: rgb(0, 0, 0),
            });
        }

        currentPdfY -= lineHeightPt;
    }
}

async function _drawImageBlock(page, engineX, engineY, width, height, data, pageSize) {
    if (!data.src) return;
    try {
        const token = localStorage.getItem('token');
        const response = await fetch(data.src, {
            headers: { 'Authorization': `Bearer ${token}` }
        });
        if (!response.ok) throw new Error(`HTTP ${response.status}`);
        const buffer = await response.arrayBuffer();

        const { PDFDocument } = window.PDFLib;
        // Bildtyp erkennen
        const uint8 = new Uint8Array(buffer);
        const isPng = uint8[0] === 137 && uint8[1] === 80;
        const embeddedImage = isPng
            ? await page.doc.embedPng(buffer)
            : await page.doc.embedJpg(buffer);

        const pdfY = pageSize.height - engineY - height;
        page.drawImage(embeddedImage, { x: engineX, y: pdfY, width, height, opacity: 1 });
    } catch (e) {
        console.error('pdfRenderer: Bild konnte nicht eingebettet werden:', data.src, e.message);
    }
}

async function _embedLogo(doc, logoArrayBuffer) {
    const uint8 = new Uint8Array(logoArrayBuffer);
    const isPng = uint8[0] === 137 && uint8[1] === 80;
    return isPng
        ? await doc.embedPng(logoArrayBuffer)
        : await doc.embedJpg(logoArrayBuffer);
}

function _drawLogo(page, logoImage, pageSize) {
    const logoHeight = 30;
    const aspectRatio = logoImage.width / logoImage.height;
    const logoWidth = logoHeight * aspectRatio;
    page.drawImage(logoImage, {
        x: pageSize.width - logoWidth - MARGINS.right,
        y: pageSize.height - logoHeight - MARGINS.top,
        width: logoWidth,
        height: logoHeight,
        opacity: 0.3,
    });
}
```

WICHTIG:
- KEIN `window.generatePDF` Aufruf oder ähnliches — pdfRenderer ist reine Bibliothek
- KEIN direkter DOM-Zugriff
- `page.doc` wird intern von pdf-lib bereitgestellt; alternativ `doc` via Closure — prüfe welche API pdf-lib 1.17.1 hat. Bei _drawImageBlock: `doc` als Parameter übergeben oder Closure nutzen
- Fehlerbehandlung pro Block (try/catch) damit ein fehlerhafter Block nicht den ganzen Export stoppt
  </action>
  <verify>
    <automated>node --input-type=module -e "
import { readFile } from 'fs/promises';
const content = await readFile('/Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/pdfRenderer.js', 'utf8');
const checks = {
  'export renderToPDF': content.includes('export async function renderToPDF'),
  'Y-Inversion vorhanden': content.includes('pageSize.height - engineY') || content.includes('pageSize.height - y'),
  'embedFontsInDoc import': content.includes('embedFontsInDoc'),
  'Kein DOM-Zugriff': !content.includes('document.getElementById') && !content.includes('element.offsetHeight'),
  'Fehlerbehandlung': content.includes('try') && content.includes('catch'),
};
let ok = true;
for (const [k,v] of Object.entries(checks)) { console.log(v ? 'OK' : 'FAIL', k); if(!v) ok=false; }
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/js/layout/pdfRenderer.js
    - Export: renderToPDF(layoutResult, config, arrayBuffers, logoArrayBuffer) → Promise&lt;PDFDocument&gt;
    - Y-Koordinaten-Inversion implementiert: pdfY = pageSize.height - engineY - blockHeight (oder äquivalent)
    - Importiert embedFontsInDoc und getFontForStyle aus fontManager.js
    - Importiert PAGE_SIZES und MARGINS aus constants.js
    - Kein document.getElementById, kein DOM-Zugriff
    - Pro Block try/catch — Fehler in einem Block stoppen nicht den gesamten Export
    - Logo wird auf jede Seite gezeichnet (30pt Höhe, oben rechts, opacity 0.3)
    - Textausrichtung center und right implementiert (via widthOfTextAtSize)
  </acceptance_criteria>
  <done>pdfRenderer.js exportiert renderToPDF, invertiert Y-Koordinaten korrekt, Fehlerbehandlung pro Block vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: domRenderer.js — LayoutResult zu seitenweiser DOM-Vorschau</name>
  <files>frontend/js/layout/domRenderer.js</files>
  <read_first>
    - frontend/js/layout/engine.js (LayoutResult-Struktur — insbesondere block.data.lines Feld)
    - frontend/js/layout/constants.js (PT_TO_PX, PAGE_SIZES, MARGINS)
    - frontend/js/previewPageBreaks.js (Zeilen 1-85: aktuelle Vorschau-Logik die ersetzt wird; Zeilen 200-280: insertPageBreakMarker um zu verstehen was bisher eingesetzt wurde)
    - frontend/dashboard.html (Zeilen rund um #liedblatt-content — Container-Struktur)
    - frontend/css/*.css (aktuelles #liedblatt-content Styling — muss kompatibel bleiben oder gezielt überschrieben werden)
    - .planning/phases/02-unified-layout-engine/02-RESEARCH.md (Pattern 2: pt→px Konversion; Pattern 2: createPageElement; Pitfall 6: seitenweise DOM vs. Scroll-Preview)
  </read_first>
  <action>
Erstelle frontend/js/layout/domRenderer.js. Diese Datei rendert LayoutResult als seitenweise Blatt-Darstellung.

**Kernprinzip:** Pro page im LayoutResult wird ein `.preview-page` div erstellt (weisses Rechteck, exakte pt→px Abmessungen). Blocks werden als absolute-positioned Divs innerhalb der Seite positioniert. container.innerHTML wird beim Render vollständig geleert und neu befüllt.

```javascript
// frontend/js/layout/domRenderer.js
import { PT_TO_PX, PAGE_SIZES } from './constants.js';

/**
 * Rendert LayoutResult als seitenweise DOM-Vorschau.
 * Leert den Container komplett und baut neue Seiten-Divs.
 * Synchron — kein async (alle Maße kommen aus LayoutResult, kein Font-Laden).
 * @param {LayoutResult} layoutResult — Ausgabe von engine.calculateLayout()
 * @param {Object} config — { format, fontFamily, fontSize, lineHeight, textAlign }
 * @param {HTMLElement} container — z.B. document.getElementById('liedblatt-content')
 */
export function renderToDOM(layoutResult, config, container) {
    if (!container) {
        console.error('domRenderer: Container nicht gefunden');
        return;
    }

    // Container leeren
    container.innerHTML = '';

    // Container-Styling für seitenweise Darstellung
    container.style.padding = '20px 0';
    container.style.background = '#e8e8e8';
    container.style.overflowY = 'auto';
    container.style.display = 'flex';
    container.style.flexDirection = 'column';
    container.style.alignItems = 'center';

    const format = config.format || 'a5';
    const pageSize = PAGE_SIZES[format] || PAGE_SIZES['a5'];
    const fontFamily = config.fontFamily || 'Jost';

    for (const pageData of layoutResult.pages) {
        const pageEl = _createPageElement(pageSize, fontFamily);
        pageEl.setAttribute('data-page-number', pageData.pageNumber);

        for (const block of pageData.blocks) {
            try {
                const blockEl = _createBlockElement(block, config);
                if (blockEl) pageEl.appendChild(blockEl);
            } catch (e) {
                console.error('domRenderer: Block-Render-Fehler:', e.message, block);
            }
        }

        container.appendChild(pageEl);
    }
}

// --- Private Hilfsfunktionen ---

function _createPageElement(pageSize, fontFamily) {
    const el = document.createElement('div');
    el.className = 'preview-page';
    el.style.position = 'relative';
    el.style.width    = `${ptToPx(pageSize.width)}px`;
    el.style.height   = `${ptToPx(pageSize.height)}px`;
    el.style.background = '#ffffff';
    el.style.boxShadow  = '0 2px 8px rgba(0,0,0,0.15)';
    el.style.margin     = '16px auto';
    el.style.flexShrink = '0';
    el.style.fontFamily = fontFamily;
    el.style.overflow   = 'hidden';
    return el;
}

function _createBlockElement(block, config) {
    const { type, x, y, width, height, data } = block;

    if (type === 'text') {
        return _createTextElement(x, y, width, height, data, config);
    } else if (type === 'image') {
        return _createImageElement(x, y, width, height, data);
    } else if (type === 'icon') {
        return _createIconElement(x, y, width, height, data);
    }
    return null;
}

function _createTextElement(x, y, width, height, data, config) {
    const el = document.createElement('div');
    el.style.position  = 'absolute';
    el.style.left      = `${ptToPx(x)}px`;
    el.style.top       = `${ptToPx(y)}px`;
    el.style.width     = `${ptToPx(width)}px`;
    el.style.fontSize  = `${ptToPx(data.fontSize)}px`;
    el.style.lineHeight = String(data.lineHeight || 1.5);
    el.style.textAlign = data.alignment || config.textAlign || 'left';
    el.style.fontFamily = 'inherit';
    el.style.overflow  = 'hidden';

    // Font-Stil
    const isBold   = data.fontStyle === 'bold' || data.fontStyle === 'boldItalic';
    const isItalic = data.fontStyle === 'italic' || data.fontStyle === 'boldItalic';
    el.style.fontWeight = isBold ? 'bold' : 'normal';
    el.style.fontStyle  = isItalic ? 'italic' : 'normal';

    // Zeilen rendern — jede Zeile als eigener Span damit Abstände stimmen
    const lineHeightPx = ptToPx(data.fontSize) * (data.lineHeight || 1.5);
    (data.lines || []).forEach(line => {
        const lineEl = document.createElement('div');
        lineEl.style.height     = `${lineHeightPx}px`;
        lineEl.style.lineHeight = `${lineHeightPx}px`;
        lineEl.style.whiteSpace = 'nowrap';
        lineEl.style.overflow   = 'hidden';
        lineEl.textContent      = line;
        el.appendChild(lineEl);
    });

    if (data.isCopyright) {
        el.style.color = '#666';
    }

    return el;
}

function _createImageElement(x, y, width, height, data) {
    const el = document.createElement('div');
    el.style.position = 'absolute';
    el.style.left     = `${ptToPx(x)}px`;
    el.style.top      = `${ptToPx(y)}px`;
    el.style.width    = `${ptToPx(width)}px`;
    el.style.height   = `${ptToPx(height)}px`;
    el.style.overflow = 'hidden';

    const img = document.createElement('img');
    img.src = data.src;
    img.style.width  = '100%';
    img.style.height = '100%';
    img.style.objectFit = 'contain';
    el.appendChild(img);

    return el;
}

function _createIconElement(x, y, width, height, data) {
    const el = document.createElement('div');
    el.style.position = 'absolute';
    el.style.left     = `${ptToPx(x)}px`;
    el.style.top      = `${ptToPx(y)}px`;
    el.style.width    = `${ptToPx(width)}px`;
    el.style.height   = `${ptToPx(height)}px`;
    el.style.display  = 'flex';
    el.style.alignItems = 'center';
    el.style.justifyContent = 'center';
    el.className = 'preview-icon-block';

    // Trennlinie als HR
    const hr = document.createElement('hr');
    hr.style.width  = '100%';
    hr.style.border = 'none';
    hr.style.borderTop = '1px solid #bbb';
    el.appendChild(hr);

    return el;
}

/** pt → px Konversion (96 DPI Standard) */
function ptToPx(pt) {
    return pt * PT_TO_PX;
}
```

WICHTIG:
- line-height wird als explizites Style auf jede Zeile gesetzt (nicht via CSS `line-height` des Containers), damit die Zeilenhöhe exakt mit der Engine-Berechnung übereinstimmt (Pitfall 3 aus RESEARCH.md)
- container.innerHTML = '' am Anfang — kein Akkumulieren von alten Seiten
- Kein window.lastCalculatedBreakPositions Schreiben — domRenderer kommuniziert nicht nach außen
- data.lines enthält bereits vorberechnete Zeilen aus engine.js (kein erneutes Umbrechen nötig)
  </action>
  <verify>
    <automated>node --input-type=module -e "
import { readFile } from 'fs/promises';
const content = await readFile('/Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/domRenderer.js', 'utf8');
const checks = {
  'export renderToDOM': content.includes('export function renderToDOM'),
  'PT_TO_PX import': content.includes('PT_TO_PX'),
  'PAGE_SIZES import': content.includes('PAGE_SIZES'),
  'preview-page Klasse': content.includes('preview-page'),
  'container leeren': content.includes('innerHTML') && content.includes(\"''\"),
  'absolute positioning': content.includes('absolute'),
  'kein window.lastCalculated': !content.includes('window.lastCalculated'),
  'kein offsetHeight': !content.includes('offsetHeight'),
  'ptToPx vorhanden': content.includes('ptToPx'),
};
let ok = true;
for (const [k,v] of Object.entries(checks)) { console.log(v ? 'OK' : 'FAIL', k); if(!v) ok=false; }
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/js/layout/domRenderer.js
    - Export: renderToDOM(layoutResult, config, container) — synchron, kein async
    - Importiert PT_TO_PX und PAGE_SIZES aus constants.js
    - container.innerHTML = '' am Render-Beginn
    - Pro page ein .preview-page div mit position:relative und exakten px-Abmessungen
    - Blöcke als position:absolute Kinder der Seiten-Divs
    - Kein window.lastCalculatedBreakPositions Schreiben
    - Kein element.offsetHeight, kein window.getComputedStyle
    - line-height wird als explizites Style pro Zeilen-Element gesetzt (nicht nur Container)
    - Kein Import aus generatePDF.js oder previewPageBreaks.js
  </acceptance_criteria>
  <done>domRenderer.js exportiert renderToDOM, erzeugt seitenweise Blatt-Darstellung, kein globaler State-Schreibzugriff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LayoutResult → Renderer | data.src für Images kommt aus DOM (intern) — kein externer User-Input direkt |
| domRenderer → DOM | textContent statt innerHTML für Texte — kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | XSS | domRenderer Textrendering | mitigate | `el.textContent = line` statt `innerHTML` — verhindert Script-Injection aus Liedtext |
| T-02-03-02 | Information Disclosure | pdfRenderer Image-Fetch | accept | Bilder werden mit Bearer-Token aus localStorage geladen — gleicher Auth-Mechanismus wie restliche API-Calls |
| T-02-03-03 | Tampering | pdfRenderer Y-Inversion | accept | Reine Berechnung, kein Angriffspfad |
</threat_model>

<verification>
Nach Abschluss:
1. `ls frontend/js/layout/` zeigt 4 Dateien: constants.js, fontManager.js, engine.js, pdfRenderer.js, domRenderer.js
2. Verifikationsbefehl Task 1 und Task 2 laufen durch
3. Manueller Check: pdfRenderer.js enthält `pageSize.height - engineY` (Y-Inversion)
4. Manueller Check: domRenderer.js enthält `preview-page` und `innerHTML = ''`
5. generatePDF.js und previewPageBreaks.js sind noch unverändert (Verdrahtung in 02-04)
</verification>

<success_criteria>
- pdfRenderer.js: renderToPDF exportiert, Y-Inversion korrekt, Fehlerbehandlung pro Block
- domRenderer.js: renderToDOM exportiert, seitenweise Divs, kein globaler State
- Beide Renderer importieren NICHT aus generatePDF.js oder previewPageBreaks.js
- Beide Renderer sind eigenständig und können unabhängig verwendet werden
</success_criteria>

<output>
Nach Completion: `.planning/phases/02-unified-layout-engine/02-03-SUMMARY.md` erstellen mit:
- renderToPDF Signatur und Parameter-Beschreibung
- renderToDOM Signatur und Parameter-Beschreibung
- CSS-Klassen die domRenderer erzeugt (.preview-page, .preview-icon-block)
- Y-Koordinaten-Konvention (für Plan 02-04 Referenz)
- Hinweis: _drawImageBlock nutzt page.doc — falls pdf-lib 1.17.1 das nicht unterstützt, muss doc als Parameter übergeben werden
</output>
