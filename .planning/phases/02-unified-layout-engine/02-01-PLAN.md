---
phase: 02-unified-layout-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/layout/constants.js
  - frontend/js/layout/fontManager.js
autonomous: true
requirements:
  - LYOT-05
must_haves:
  truths:
    - "constants.js existiert als einzige Quelle für alle Layout-Konstanten in pt"
    - "fontManager.js lädt Fonts einmal und cached ArrayBuffers"
    - "Keine Konstante existiert mehr doppelt (generatePDF.js und previewPageBreaks.js nutzen noch lokale Kopien — das ist Aufgabe späterer Pläne)"
  artifacts:
    - path: "frontend/js/layout/constants.js"
      provides: "FONT, SPACING, MARGINS, PAGE_SIZES, LAYOUT, PT_TO_PX Exports"
      exports: ["FONT", "SPACING", "MARGINS", "PAGE_SIZES", "LAYOUT", "PT_TO_PX"]
    - path: "frontend/js/layout/fontManager.js"
      provides: "Font-Lade-Cache mit ArrayBuffer-Storage"
      exports: ["loadFontArrayBuffers", "embedFontsInDoc", "getFontForStyle"]
  key_links:
    - from: "frontend/js/layout/fontManager.js"
      to: "/api/ttf/{fontFamily}-{Style}.ttf"
      via: "fetch mit Bearer-Token aus localStorage"
      pattern: "fetch.*api/ttf"
---

<objective>
Erstellt die Foundation der Layout-Engine: constants.js als Single Source of Truth für alle Layout-Konstanten, und fontManager.js als Font-Lade-Abstraktion.

Purpose: Alle nachfolgenden Dateien (engine.js, pdfRenderer.js, domRenderer.js) importieren aus diesen zwei Dateien. constants.js eliminiert die Konstanten-Divergenz (LYOT-05). fontManager.js extrahiert die bewährte fetchAndEmbedFont-Logik aus generatePDF.js zur Wiederverwendung ohne Duplikation.

Output: Zwei neue Dateien in frontend/js/layout/. generatePDF.js und previewPageBreaks.js werden in diesem Plan NICHT berührt — deren Umbau erfolgt in Plänen 02-03 und 02-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-unified-layout-engine/02-CONTEXT.md
@.planning/phases/02-unified-layout-engine/02-RESEARCH.md

<interfaces>
<!-- Aus generatePDF.js Zeilen 1-34 — Konstanten die ersetzt werden -->
```javascript
// Aktuell in generatePDF.js (NICHT in constants.js)
const BASE_FONT_SIZE = 14;
const HEADING_1_SCALE = 1.6;
const HEADING_2_SCALE = 1.4;
const HEADING_3_SCALE = 1.2;
const COPYRIGHT_FONT_SIZE = 12;     // PDF-Wert — gewinnt
const ICON_SIZE = 20;
const ICON_MARGIN = 25;
const DEFAULT_OBJECT_SPACING = 15;  // PDF-Wert — gewinnt
const IMAGE_MARGIN_TOP = -10;
const IMAGE_MARGIN_BOTTOM = 15;
const STROPHE_SPACING = 8;          // PDF-Wert — gewinnt
const MAX_STROPHES_BEFORE_BREAK = 3;
const MAX_PSALM_LINES_BEFORE_BREAK = 4;
const MIN_SPACE_FOR_NEXT_GROUP = 50;
const QUILL_H1_MARGIN_TOP = 0;
const QUILL_H1_MARGIN_BOTTOM = 12;
const QUILL_H2_MARGIN_TOP = 5;
const QUILL_H2_MARGIN_BOTTOM = 10;
const QUILL_H3_MARGIN_TOP = 5;
const QUILL_H3_MARGIN_BOTTOM = 5;
const COPYRIGHT_MARGIN_TOP = -5;
const COPYRIGHT_MARGIN_BOTTOM = -5;

// Aktuell in previewPageBreaks.js (abweichende Werte)
const COPYRIGHT_FONT_SIZE = 10;     // VERLIERT — PDF-Wert 12 gewinnt
const STROPHE_SPACING = 6;          // VERLIERT — PDF-Wert 8 gewinnt
const DEFAULT_OBJECT_SPACING = 12;  // VERLIERT — PDF-Wert 15 gewinnt
const TITLE_MARGIN_BOTTOM = 6;      // Nur in Preview — wird übernommen
const STROPHE_MARGIN_BOTTOM = 8;    // Nur in Preview — wird übernommen
```

<!-- Aus generatePDF.js Zeilen 1344-1415 — fetchAndEmbedFont die extrahiert wird -->
```javascript
// Aktuell in generatePDF.js — diese Logik zieht nach fontManager.js
async function fetchAndEmbedFont(doc, fontFamily) {
    fontFamily = getCleanFontFamily(fontFamily);
    const fontFamilyMapping = {
        'Playfair Display': 'PlayfairDisplay',
        'Crimson Text': 'CrimsonText',
        'Open Sans': 'OpenSans',
        'Alegreya Sans': 'AlegreyaSans',
        'Andada Pro': 'AndadaPro',
        'Bodoni Moda': 'BodoniModa'
    };
    const formattedFontFamily = fontFamilyMapping[fontFamily] || fontFamily.replace(/\s+/g, '');
    // Lädt Regular, Bold, Italic, BoldItalic via /api/ttf/{name}.ttf
    // Fallback auf Regular wenn Bold/Italic fehlt
    // Wirft Error wenn Regular fehlt
    return loadedFonts; // { regular, bold, italic, bolditalic }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: constants.js — Single Source of Truth</name>
  <files>frontend/js/layout/constants.js</files>
  <read_first>
    - frontend/js/generatePDF.js (Zeilen 1-34 für Konstanten, Zeilen 460-470 für pageSizes/mmToPt)
    - frontend/js/previewPageBreaks.js (Zeilen 1-35 für abweichende Konstanten)
    - .planning/phases/02-unified-layout-engine/02-RESEARCH.md (Konstanten-Divergenz-Tabelle, constants.js Zielstruktur)
  </read_first>
  <action>
Erstelle das Verzeichnis frontend/js/layout/ und darin constants.js mit folgendem exakten Inhalt:

```javascript
// frontend/js/layout/constants.js
// Single source of truth für alle Layout-Konstanten.
// Alle Werte in pt (PDF-Punkte). Bei Divergenz zwischen generatePDF.js und
// previewPageBreaks.js gewinnt immer der PDF-Wert — das ist was gedruckt wird.

const mmToPt = (mm) => mm * 2.83465;

export const FONT = {
    BASE_SIZE: 14,        // pt — Grundschriftgröße
    H1_SCALE: 1.6,
    H2_SCALE: 1.4,
    H3_SCALE: 1.2,
    COPYRIGHT_SIZE: 12,   // pt — PDF-Wert gewinnt (previewPageBreaks.js hatte 10)
    LINE_HEIGHT: 1.5,     // Standard-Zeilenhöhe (Faktor, nicht pt)
};

export const SPACING = {
    STROPHE: 8,           // pt — PDF-Wert gewinnt (previewPageBreaks.js hatte 6)
    OBJECT_DEFAULT: 15,   // pt — PDF-Wert gewinnt (previewPageBreaks.js hatte 12)
    TITLE_BOTTOM: 6,      // pt — nur in previewPageBreaks.js vorhanden, sinnvoll übernehmen
    STROPHE_BOTTOM: 8,    // pt — nur in previewPageBreaks.js vorhanden, sinnvoll übernehmen
    IMAGE_TOP: -10,       // pt — nur in generatePDF.js
    IMAGE_BOTTOM: 15,     // pt — nur in generatePDF.js
    ICON_SIZE: 20,        // pt
    ICON_MARGIN: 25,      // pt
    COPYRIGHT_TOP: -5,    // pt
    COPYRIGHT_BOTTOM: -5, // pt
    QUILL_H1_TOP: 0,
    QUILL_H1_BOTTOM: 12,
    QUILL_H2_TOP: 5,
    QUILL_H2_BOTTOM: 10,
    QUILL_H3_TOP: 5,
    QUILL_H3_BOTTOM: 5,
};

export const MARGINS = {
    top: 30,    // pt
    right: 20,  // pt
    bottom: 20, // pt
    left: 20,   // pt
};

export const PAGE_SIZES = {
    'a5':        { width: mmToPt(148), height: mmToPt(210) },   // 419.53 x 595.28 pt
    'dl':        { width: mmToPt(99),  height: mmToPt(210) },   // 280.63 x 595.28 pt
    'a4-schmal': { width: mmToPt(105), height: mmToPt(297) },   // 297.64 x 841.89 pt
    'a3-schmal': { width: mmToPt(148), height: mmToPt(420) },   // 419.53 x 1190.55 pt
};

export const LAYOUT = {
    MAX_STROPHES_BEFORE_BREAK: 3,
    MAX_PSALM_LINES_BEFORE_BREAK: 4,
    MIN_SPACE_FOR_NEXT_GROUP: 50,  // pt — Mindestplatz für nächste Gruppe
};

// DOM-Renderer: pt → px Umrechnungsfaktor (96 DPI / 72 pt per inch)
export const PT_TO_PX = 96 / 72; // 1.3333...
```

Kein weiterer Code, keine default exports, keine Hilfsfunktionen außer mmToPt (wird intern benötigt).
  </action>
  <verify>
    <automated>node -e "import('/Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/constants.js').then(m => { console.log('FONT.COPYRIGHT_SIZE:', m.FONT.COPYRIGHT_SIZE); console.log('SPACING.STROPHE:', m.SPACING.STROPHE); console.log('SPACING.OBJECT_DEFAULT:', m.SPACING.OBJECT_DEFAULT); console.log('PT_TO_PX:', m.PT_TO_PX); if (m.FONT.COPYRIGHT_SIZE !== 12) throw new Error('COPYRIGHT_SIZE falsch'); if (m.SPACING.STROPHE !== 8) throw new Error('STROPHE falsch'); if (m.SPACING.OBJECT_DEFAULT !== 15) throw new Error('OBJECT_DEFAULT falsch'); console.log('OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/js/layout/constants.js
    - FONT.COPYRIGHT_SIZE === 12 (PDF-Wert, nicht 10 aus previewPageBreaks.js)
    - SPACING.STROPHE === 8 (PDF-Wert, nicht 6)
    - SPACING.OBJECT_DEFAULT === 15 (PDF-Wert, nicht 12)
    - PT_TO_PX === 96/72 ≈ 1.3333
    - PAGE_SIZES['a5'].width ≈ 419.53 (mmToPt(148))
    - Alle benannten Objekte (FONT, SPACING, MARGINS, PAGE_SIZES, LAYOUT) werden exportiert
    - Keine lokale PX_TO_PT_RATIO (eliminiert per Entscheidung)
  </acceptance_criteria>
  <done>constants.js existiert mit allen korrekten Werten, PDF-Werte haben bei Divergenz gewonnen, alle 6 Exporte vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: fontManager.js — Font-Lade-Abstraktion</name>
  <files>frontend/js/layout/fontManager.js</files>
  <read_first>
    - frontend/js/generatePDF.js (Zeilen 1344-1415 — fetchAndEmbedFont und getCleanFontFamily)
    - frontend/js/generatePDF.js (Zeilen 1-10 für globale Variablen und Importe)
  </read_first>
  <action>
Erstelle frontend/js/layout/fontManager.js. Diese Datei extrahiert die Font-Lade-Logik aus generatePDF.js, trennt aber ArrayBuffer-Laden (format-agnostisch) von PDF-Embedding (pdf-lib-spezifisch).

```javascript
// frontend/js/layout/fontManager.js
// Font-Manager: lädt und cached Font-ArrayBuffers, trennt Laden von Embedding.
// ArrayBuffers sind format-agnostisch (nutzbar für pdf-lib und DOM).
// pdf-lib-spezifisches embedFont() ruft erst pdfRenderer.js auf.

const arrayBufferCache = new Map(); // key: "FontFamily-Style", value: ArrayBuffer

const FONT_FAMILY_MAPPING = {
    'Playfair Display': 'PlayfairDisplay',
    'Crimson Text': 'CrimsonText',
    'Open Sans': 'OpenSans',
    'Alegreya Sans': 'AlegreyaSans',
    'Andada Pro': 'AndadaPro',
    'Bodoni Moda': 'BodoniModa',
};

const FONT_STYLES = ['Regular', 'Bold', 'Italic', 'BoldItalic'];

function normalizeFamily(fontFamily) {
    const trimmed = fontFamily.trim();
    return FONT_FAMILY_MAPPING[trimmed] || trimmed.replace(/\s+/g, '');
}

/**
 * Lädt ArrayBuffers für alle Stile einer Schriftfamilie.
 * Cached Ergebnisse — mehrfache Aufrufe laden nicht erneut.
 * @param {string} fontFamily - z.B. 'Jost', 'Playfair Display'
 * @returns {Promise<Object>} { regular, bold, italic, bolditalic } — je ArrayBuffer
 */
export async function loadFontArrayBuffers(fontFamily) {
    const formattedFamily = normalizeFamily(fontFamily);
    const result = {};

    for (const style of FONT_STYLES) {
        const cacheKey = `${formattedFamily}-${style}`;
        if (arrayBufferCache.has(cacheKey)) {
            result[style.toLowerCase()] = arrayBufferCache.get(cacheKey);
            continue;
        }

        const url = `/api/ttf/${formattedFamily}-${style}.ttf`;
        try {
            const token = localStorage.getItem('token');
            const response = await fetch(url, {
                headers: { 'Authorization': `Bearer ${token}` }
            });
            if (!response.ok) throw new Error(`HTTP ${response.status} für ${url}`);

            const buffer = await response.arrayBuffer();
            arrayBufferCache.set(cacheKey, buffer);
            result[style.toLowerCase()] = buffer;
        } catch (error) {
            console.error(`fontManager: Fehler beim Laden von ${cacheKey}:`, error);
            if (style !== 'Regular' && result.regular) {
                console.warn(`fontManager: Nutze Regular als Fallback für ${style}`);
                result[style.toLowerCase()] = result.regular;
            } else if (style === 'Regular') {
                throw new Error(`fontManager: Regular-Font nicht ladbar für ${fontFamily}: ${error.message}`);
            }
        }
    }

    if (!result.regular) {
        throw new Error(`fontManager: Kein Regular-Font für ${fontFamily}`);
    }

    return result;
}

/**
 * Bettet ArrayBuffers in ein PDFDocument ein.
 * Gibt PDFFont-Objekte zurück — nur für pdfRenderer.js gedacht.
 * @param {PDFDocument} doc - pdf-lib PDFDocument
 * @param {Object} arrayBuffers - { regular, bold, italic, bolditalic } ArrayBuffers
 * @returns {Promise<Object>} { regular, bold, italic, bolditalic } — PDFFont-Objekte
 */
export async function embedFontsInDoc(doc, arrayBuffers) {
    const embedded = {};
    const embedOptions = { subset: true, features: { liga: true, kern: true } };

    for (const [style, buffer] of Object.entries(arrayBuffers)) {
        try {
            const font = await doc.embedFont(buffer, embedOptions);
            if (!font || typeof font.widthOfTextAtSize !== 'function') {
                throw new Error(`Eingebetteter Font für ${style} hat kein widthOfTextAtSize`);
            }
            // Schnelltest
            const testWidth = font.widthOfTextAtSize('Test', 12);
            if (typeof testWidth !== 'number' || isNaN(testWidth)) {
                throw new Error(`widthOfTextAtSize defekt für ${style}`);
            }
            embedded[style] = font;
        } catch (error) {
            console.error(`fontManager: Embed-Fehler für ${style}:`, error);
            if (style !== 'regular' && embedded.regular) {
                embedded[style] = embedded.regular;
            } else if (style === 'regular') {
                throw error;
            }
        }
    }

    return embedded;
}

/**
 * Wählt den richtigen PDFFont für eine Formatierungskombination.
 * @param {Object} fonts - { regular, bold, italic, bolditalic } PDFFont-Objekte
 * @param {boolean} bold
 * @param {boolean} italic
 * @returns {PDFFont}
 */
export function getFontForStyle(fonts, bold, italic) {
    if (bold && italic) return fonts.bolditalic || fonts.bold || fonts.regular;
    if (bold)           return fonts.bold       || fonts.regular;
    if (italic)         return fonts.italic     || fonts.regular;
    return fonts.regular;
}

/** Cache leeren (z.B. für Tests oder Font-Wechsel) */
export function clearFontCache() {
    arrayBufferCache.clear();
}
```
  </action>
  <verify>
    <automated>node -e "import('/Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/fontManager.js').then(m => { const exp = ['loadFontArrayBuffers','embedFontsInDoc','getFontForStyle','clearFontCache']; exp.forEach(e => { if (typeof m[e] !== 'function') throw new Error('Missing export: ' + e); }); console.log('Alle Exports vorhanden. OK'); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/js/layout/fontManager.js
    - Exportiert: loadFontArrayBuffers, embedFontsInDoc, getFontForStyle, clearFontCache
    - loadFontArrayBuffers(fontFamily) gibt Promise zurück
    - arrayBufferCache ist modul-privat (nicht exportiert)
    - embedFontsInDoc ist separat von loadFontArrayBuffers — keine zirkuläre Abhängigkeit zu pdfRenderer
    - Fallback auf Regular wenn Bold/Italic fehlt (Zeile mit console.warn vorhanden)
    - Wirft Error wenn Regular nicht ladbar
    - Kein Import aus generatePDF.js (fontManager ist unabhängig)
  </acceptance_criteria>
  <done>fontManager.js existiert mit vier Exporten, ArrayBuffer-Cache ist privat, PDF-Embedding ist separat von Laden getrennt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/ttf/* | Font-Dateien vom Server geladen — Bearer Token im Header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | fontManager fetch | accept | Token aus localStorage wird bei jedem Font-Fetch mitgeschickt — bereits etabliertes Muster im Projekt; Font-Endpunkt ist authentifiziert |
| T-02-02 | Tampering | constants.js | accept | Reine Konstanten-Datei, kein user input, kein Angriffspfad |
</threat_model>

<verification>
Nach Abschluss dieses Plans:
1. `ls frontend/js/layout/` zeigt constants.js und fontManager.js
2. Node-Import-Test für constants.js läuft durch (PT_TO_PX, PAGE_SIZES, FONT, SPACING, MARGINS, LAYOUT vorhanden)
3. Node-Import-Test für fontManager.js zeigt alle 4 Exports
4. generatePDF.js und previewPageBreaks.js sind unverändert (werden in Plan 02-03/02-04 umgebaut)
</verification>

<success_criteria>
- frontend/js/layout/constants.js existiert, exportiert 6 benannte Objekte
- FONT.COPYRIGHT_SIZE === 12, SPACING.STROPHE === 8, SPACING.OBJECT_DEFAULT === 15 (PDF-Werte)
- frontend/js/layout/fontManager.js existiert, exportiert 4 Funktionen
- Keine Duplikation: fontManager.js importiert NICHT aus generatePDF.js
- Beide Dateien sind syntaktisch korrekte ES6-Module (import/export)
</success_criteria>

<output>
Nach Completion: `.planning/phases/02-unified-layout-engine/02-01-SUMMARY.md` erstellen mit:
- Was erstellt wurde (constants.js, fontManager.js)
- Konkrete Exportnamen
- Bestätigungen welche PDF-Werte gewonnen haben
- Hinweise für Plan 02-02 (engine.js importiert aus beiden)
</output>
