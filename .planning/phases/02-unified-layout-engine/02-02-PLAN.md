---
phase: 02-unified-layout-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/js/layout/engine.js
autonomous: true
requirements:
  - LYOT-01
must_haves:
  truths:
    - "calculateLayout(items, config, fonts) existiert als pure async Funktion ohne DOM- oder pdf-lib-Zugriff"
    - "Alle Werte im LayoutResult sind in pt"
    - "Texthöhen werden via font.widthOfTextAtSize berechnet — gleiche Metrik wie der spätere pdfRenderer"
    - "Quill-HTML wird via DOMParser geparst — kein window.getComputedStyle, kein live-DOM-Zugriff"
    - "Seitenumbrüche und Y-Positionen sind im LayoutResult enthalten (kein window.lastCalculatedBreakPositions)"
  artifacts:
    - path: "frontend/js/layout/engine.js"
      provides: "calculateLayout, parseQuillHTML, splitTextToLines"
      exports: ["calculateLayout", "parseQuillHTML", "splitTextToLines"]
  key_links:
    - from: "frontend/js/layout/engine.js"
      to: "frontend/js/layout/constants.js"
      via: "ES6 import"
      pattern: "import.*constants"
    - from: "calculateLayout return value"
      to: "pdfRenderer/domRenderer input"
      via: "LayoutResult { pages[], totalPages }"
      pattern: "pages.*blocks.*type.*x.*y.*width.*height"
---

<objective>
Erstellt engine.js — die pure Layout-Engine. calculateLayout(items, config, fonts) nimmt Liedblatt-Elemente entgegen und gibt ein format-agnostisches LayoutResult zurück. Kein DOM-Zugriff (außer DOMParser für Quill-Parsing), kein pdf-lib-Aufruf.

Purpose: Alle Positionen, Höhen und Seitenumbrüche werden hier berechnet. pdfRenderer und domRenderer konsumieren das Ergebnis — sie "zeichnen" nur noch. Diese Trennung eliminiert die strukturelle Ursache der Preview/PDF-Divergenz.

Output: frontend/js/layout/engine.js mit drei Exports. generatePDF.js und previewPageBreaks.js werden in diesem Plan NICHT berührt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-unified-layout-engine/02-CONTEXT.md
@.planning/phases/02-unified-layout-engine/02-RESEARCH.md
@.planning/phases/02-unified-layout-engine/02-01-SUMMARY.md

<interfaces>
<!-- Aus 02-01: constants.js Exports -->
```javascript
import { FONT, SPACING, MARGINS, PAGE_SIZES, LAYOUT, PT_TO_PX } from './constants.js';
// FONT.BASE_SIZE = 14, FONT.H1_SCALE = 1.6, FONT.COPYRIGHT_SIZE = 12
// SPACING.STROPHE = 8, SPACING.OBJECT_DEFAULT = 15, SPACING.MIN_SPACE_FOR_NEXT_GROUP = 50
// MARGINS = { top: 30, right: 20, bottom: 20, left: 20 }
// PAGE_SIZES['a5'] = { width: 419.53, height: 595.28 }
// LAYOUT.MAX_STROPHES_BEFORE_BREAK = 3
```

<!-- LayoutResult-Datenstruktur (aus RESEARCH.md Pattern 1) -->
```javascript
// Rückgabetyp von calculateLayout — alle Werte in pt
{
    pages: [
        {
            pageNumber: 1,          // 1-basiert
            blocks: [
                {
                    type: 'text',   // 'text' | 'image' | 'icon' | 'spacing'
                    x: 20,          // pt vom linken Rand (= MARGINS.left)
                    y: 30,          // pt von Seitenoberseite (y=0 = Seitenoberkante, DOM-Konvention)
                    width: 379.53,  // pt (pageWidth - MARGINS.left - MARGINS.right)
                    height: 16.8,   // pt — berechnet via splitTextToLines + lineHeightFactor
                    data: {
                        text: 'Kyrie eleison',   // für type='text'
                        fontSize: 14,            // pt
                        fontStyle: 'regular',    // 'regular'|'bold'|'italic'|'boldItalic'
                        alignment: 'center',
                        lineHeight: 1.5,
                        isCopyright: false,
                        isRefrain: false,
                        isStrophe: false,
                        // für type='image':
                        src: '/api/uploads/...',
                        naturalWidth: 800,
                        naturalHeight: 600,
                        // für type='icon':
                        iconType: 'cross',
                    }
                }
            ]
        }
    ],
    totalPages: 2
}
```

<!-- DOM-Eingabestruktur: items = Array.from(liedblattContent.children) -->
<!-- Aus generatePDF.js Zeilen 1002-1003 und 1144-1200 -->
```javascript
// Jedes item ist ein HTMLElement mit:
// - .classList: 'lied-item', 'page-break', 'trenner', 'freier-text', 'custom-image', etc.
// - item.querySelector('.item-title') — Titel des Liedes
// - item.querySelector('.strophe'), '.refrain' — Text-Blöcke
// - item.querySelectorAll('h1, h2, h3, p, img, em, u, strong, .copyright-info')
// - item.classList.contains('page-break') — manueller Seitenumbruch
// - Quill-Editor-HTML in .ql-editor Elementen
```

<!-- splitTextToLines aus generatePDF.js Zeilen 1425-1451 — wird nach engine.js verschoben -->
```javascript
// Diese Funktion existiert BEREITS in generatePDF.js (Zeile 1425).
// Sie wird nach engine.js extrahiert (nicht kopiert — generatePDF.js importiert sie später).
async function splitTextToLines(text, font, fontSize, maxWidth) {
    const words = text.split(' ');
    const lines = [];
    let currentLine = '';
    for (const word of words) {
        const testLine = currentLine ? `${currentLine} ${word}` : word;
        const width = await font.widthOfTextAtSize(testLine, fontSize);
        if (width > maxWidth) {
            if (currentLine) { lines.push(currentLine); currentLine = word; }
            else { lines.push(word); }
        } else { currentLine = testLine; }
    }
    if (currentLine) lines.push(currentLine);
    return lines;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: engine.js — Kern-Infrastruktur (splitTextToLines, parseQuillHTML, scaleValue)</name>
  <files>frontend/js/layout/engine.js</files>
  <read_first>
    - frontend/js/layout/constants.js (alle Exports — sind die Basis für alle Berechnungen)
    - frontend/js/generatePDF.js (Zeilen 1-35: Konstanten; Zeilen 459-462: scaleValue; Zeilen 1425-1451: splitTextToLines; Zeilen 1144-1260: Element-Traversal-Logik)
    - frontend/js/generatePDF.js (Zeilen 400-460: isHeading, isStropheOrRefrain, isPsalmOrPrayer Klassifizierungsfunktionen)
    - .planning/phases/02-unified-layout-engine/02-RESEARCH.md (Pattern 3: Texthöhen-Berechnung; Pattern 4: Quill-HTML; Anti-Patterns; Pitfall 4 DOMParser+getComputedStyle)
  </read_first>
  <action>
Erstelle frontend/js/layout/engine.js mit drei öffentlichen Exports und den nötigen privaten Hilfsfunktionen.

Die Datei gliedert sich in vier Abschnitte:

**Abschnitt 1 — Imports:**
```javascript
import { FONT, SPACING, MARGINS, PAGE_SIZES, LAYOUT } from './constants.js';
```

**Abschnitt 2 — Hilfsfunktionen (privat, nicht exportiert):**

`scaleValue(value, globalFontSize)` — proportionale Skalierung wie in generatePDF.js:
```javascript
function scaleValue(value, globalFontSize) {
    return (value / FONT.BASE_SIZE) * globalFontSize;
}
```

`classifyItem(element)` — bestimmt den Typ eines DOM-Elements:
- Gibt zurück: 'page-break' | 'trenner' | 'icon' | 'image' | 'quill-text' | 'lied' | 'unknown'
- Prüft element.classList für: 'page-break', 'trenner', 'custom-image', 'freier-text', 'lied-item'
- Prüft item.querySelector('img') für Bilder-Elemente

`getFontForStyle(fonts, bold, italic)` — wählt richtigen Font (falls fontManager nicht importiert werden soll, inline):
```javascript
function getFontForStyle(fonts, bold, italic) {
    if (bold && italic) return fonts.bolditalic || fonts.bold || fonts.regular;
    if (bold)           return fonts.bold       || fonts.regular;
    if (italic)         return fonts.italic     || fonts.regular;
    return fonts.regular;
}
```

**Abschnitt 3 — Drei exportierte Funktionen:**

```javascript
/**
 * Teilt Text in Zeilen auf, die in maxWidth pt passen.
 * Autoritäre Höhenquelle für beide Renderer.
 * @param {string} text
 * @param {PDFFont} font — pdf-lib PDFFont mit widthOfTextAtSize
 * @param {number} fontSize — pt
 * @param {number} maxWidth — pt
 * @returns {Promise<string[]>}
 */
export async function splitTextToLines(text, font, fontSize, maxWidth) {
    // Logik aus generatePDF.js Zeile 1425-1451 — exakte Kopie
    // (generatePDF.js wird in Plan 02-03 auf diesen Import umgestellt)
}

/**
 * Parst Quill-HTML via DOMParser.
 * Extrahiert Formatierung aus HTML-Markup (NICHT aus CSS/getComputedStyle).
 * @param {string} htmlString — innerHTML eines .ql-editor Elements
 * @returns {Array<{text, tag, bold, italic, underline, alignment, isQuillHeading}>}
 */
export function parseQuillHTML(htmlString) {
    const parser = new DOMParser();
    const doc = parser.parseFromString(htmlString, 'text/html');
    const runs = [];

    // Traversiere alle Block-Elemente
    doc.body.querySelectorAll('p, h1, h2, h3, li').forEach(blockEl => {
        const tag = blockEl.tagName.toLowerCase();
        const alignment = blockEl.style.textAlign || blockEl.getAttribute('data-align') || 'left';
        const isQuillHeading = blockEl.classList.contains('isQuillHeading') ||
                               blockEl.closest('[class*="isQuillHeading"]') !== null;

        // Inline-Formatierung via Kinder-Elemente
        // Wenn keine Inline-Kinder: Text direkt
        const inlineEls = blockEl.querySelectorAll('strong, em, u, s, span');

        if (inlineEls.length === 0) {
            const text = (blockEl.textContent || '').trim();
            if (text) {
                runs.push({ text, tag, bold: false, italic: false, underline: false,
                            alignment, isQuillHeading });
            }
        } else {
            // Walk children for inline runs
            blockEl.childNodes.forEach(node => {
                if (node.nodeType === Node.TEXT_NODE) {
                    const text = node.textContent.trim();
                    if (text) runs.push({ text, tag, bold: false, italic: false,
                                          underline: false, alignment, isQuillHeading });
                } else if (node.nodeType === Node.ELEMENT_NODE) {
                    const text = (node.textContent || '').trim();
                    if (!text) return;
                    runs.push({
                        text,
                        tag,
                        bold: node.tagName === 'STRONG' || !!node.closest('strong'),
                        italic: node.tagName === 'EM' || !!node.closest('em'),
                        underline: node.tagName === 'U' || !!node.closest('u'),
                        alignment,
                        isQuillHeading
                    });
                }
            });
        }
    });

    return runs;
}

/**
 * Hauptfunktion der Layout-Engine.
 * Pure async Funktion — kein DOM-Zugriff (außer DOMParser via parseQuillHTML),
 * kein globaler State, kein window.xxx.
 * @param {HTMLElement[]} items — Array.from(liedblattContent.children)
 * @param {Object} config — { format, fontSize, lineHeight, textAlign, fontFamily }
 * @param {Object} fonts — { regular, bold, italic, bolditalic } PDFFont-Objekte
 * @returns {Promise<LayoutResult>}
 */
export async function calculateLayout(items, config, fonts) {
    const format = config.format || 'a5';
    const pageSize = PAGE_SIZES[format] || PAGE_SIZES['a5'];
    const scaledFontSize = scaleValue(FONT.BASE_SIZE, config.fontSize || FONT.BASE_SIZE);
    const lineHeightFactor = config.lineHeight || FONT.LINE_HEIGHT;
    const contentWidth = pageSize.width - MARGINS.left - MARGINS.right;
    const availableHeight = pageSize.height - MARGINS.top - MARGINS.bottom;

    const pages = [];
    let currentBlocks = [];
    let currentY = MARGINS.top; // y=0 ist Seitenoberseite, wir beginnen beim Rand

    function newPage() {
        pages.push({ pageNumber: pages.length + 1, blocks: currentBlocks });
        currentBlocks = [];
        currentY = MARGINS.top;
    }

    function remainingHeight() {
        return pageSize.height - MARGINS.bottom - currentY;
    }

    async function pushTextBlock(text, fontSize, fontStyle, alignment, extraSpacingAfter, flags) {
        const font = getFontForStyle(fonts,
            fontStyle === 'bold' || fontStyle === 'boldItalic',
            fontStyle === 'italic' || fontStyle === 'boldItalic');
        const lines = await splitTextToLines(text, font, fontSize, contentWidth);
        const lineHeightPt = fontSize * lineHeightFactor;
        const blockHeight = lines.length * lineHeightPt;

        // Seitenumbruch falls kein Platz
        if (currentY + blockHeight > pageSize.height - MARGINS.bottom) {
            newPage();
        }

        currentBlocks.push({
            type: 'text',
            x: MARGINS.left,
            y: currentY,
            width: contentWidth,
            height: blockHeight,
            data: {
                lines,         // vorberechnete Zeilen
                fontSize,
                fontStyle,
                alignment: alignment || config.textAlign || 'left',
                lineHeight: lineHeightFactor,
                ...flags
            }
        });
        currentY += blockHeight + (extraSpacingAfter || 0);
    }

    // Item-Traversal — analoge Logik zu generatePDF.js processElementGroups
    for (const item of items) {
        // Manueller Seitenumbruch
        if (item.classList.contains('page-break')) {
            newPage();
            continue;
        }

        // Trenner / Icon
        if (item.classList.contains('trenner')) {
            const iconType = item.getAttribute('data-icon-type') || 'line';
            const scaledIconSize = scaleValue(SPACING.ICON_SIZE, scaledFontSize);
            const scaledIconMargin = scaleValue(SPACING.ICON_MARGIN, scaledFontSize);
            if (currentY + scaledIconSize > pageSize.height - MARGINS.bottom) newPage();
            currentBlocks.push({
                type: 'icon',
                x: MARGINS.left,
                y: currentY,
                width: contentWidth,
                height: scaledIconSize,
                data: { iconType }
            });
            currentY += scaledIconSize + scaledIconMargin;
            continue;
        }

        // Bild-Element (custom-image)
        const imgEl = item.querySelector('img');
        if (imgEl) {
            const imgHeight = imgEl.naturalHeight && imgEl.naturalWidth
                ? (imgEl.naturalHeight / imgEl.naturalWidth) * contentWidth
                : 150; // Fallback
            const scaledImgTop = scaleValue(Math.abs(SPACING.IMAGE_TOP), scaledFontSize);
            const scaledImgBottom = scaleValue(SPACING.IMAGE_BOTTOM, scaledFontSize);
            currentY += SPACING.IMAGE_TOP < 0 ? -scaledImgTop : scaledImgTop;
            if (currentY + imgHeight > pageSize.height - MARGINS.bottom) newPage();
            currentBlocks.push({
                type: 'image',
                x: MARGINS.left,
                y: currentY,
                width: contentWidth,
                height: imgHeight,
                data: { src: imgEl.src, naturalWidth: imgEl.naturalWidth, naturalHeight: imgEl.naturalHeight }
            });
            currentY += imgHeight + scaledImgBottom;
            continue;
        }

        // Freier Text (Quill) — item.classList enthält 'freier-text' oder 'quill-text'
        const quillEditor = item.querySelector('.ql-editor');
        if (quillEditor) {
            const runs = parseQuillHTML(quillEditor.innerHTML);
            for (const run of runs) {
                let fontSize = scaledFontSize;
                let marginTop = 0;
                let marginBottom = 0;
                let fontStyle = 'regular';

                if (run.tag === 'h1' || run.isQuillHeading) {
                    fontSize = scaledFontSize * FONT.H1_SCALE;
                    marginTop = scaleValue(SPACING.QUILL_H1_TOP, scaledFontSize);
                    marginBottom = scaleValue(SPACING.QUILL_H1_BOTTOM, scaledFontSize);
                } else if (run.tag === 'h2') {
                    fontSize = scaledFontSize * FONT.H2_SCALE;
                    marginTop = scaleValue(SPACING.QUILL_H2_TOP, scaledFontSize);
                    marginBottom = scaleValue(SPACING.QUILL_H2_BOTTOM, scaledFontSize);
                } else if (run.tag === 'h3') {
                    fontSize = scaledFontSize * FONT.H3_SCALE;
                    marginTop = scaleValue(SPACING.QUILL_H3_TOP, scaledFontSize);
                    marginBottom = scaleValue(SPACING.QUILL_H3_BOTTOM, scaledFontSize);
                }

                if (run.bold && run.italic) fontStyle = 'boldItalic';
                else if (run.bold)   fontStyle = 'bold';
                else if (run.italic) fontStyle = 'italic';

                currentY += marginTop;
                await pushTextBlock(run.text, fontSize, fontStyle, run.alignment,
                    marginBottom, { isQuillHeading: run.isQuillHeading });
            }
            currentY += scaleValue(SPACING.OBJECT_DEFAULT, scaledFontSize);
            continue;
        }

        // Standard Lied-Item — Titel + Strophen/Refrains
        const subElements = item.querySelectorAll('h1, h2, h3, p, .copyright-info, .strophe, .refrain');
        for (const subEl of subElements) {
            const text = (subEl.innerText || subEl.textContent || '').trim();
            if (!text) continue;

            const isCopyright = subEl.classList.contains('copyright-info');
            const isRefrain = subEl.classList.contains('refrain');
            const isStrophe = subEl.classList.contains('strophe');

            let fontSize = scaledFontSize;
            let marginTop = 0;
            let marginBottom = 0;
            let fontStyle = 'regular';

            if (subEl.tagName === 'H1') {
                fontSize = scaledFontSize * FONT.H1_SCALE;
                fontStyle = 'bold';
                marginBottom = scaleValue(SPACING.TITLE_BOTTOM, scaledFontSize);
            } else if (subEl.tagName === 'H2') {
                fontSize = scaledFontSize * FONT.H2_SCALE;
            } else if (subEl.tagName === 'H3') {
                fontSize = scaledFontSize * FONT.H3_SCALE;
            } else if (isCopyright) {
                fontSize = scaleValue(FONT.COPYRIGHT_SIZE, scaledFontSize);
                marginTop = scaleValue(SPACING.COPYRIGHT_TOP, scaledFontSize);
                marginBottom = scaleValue(SPACING.COPYRIGHT_BOTTOM, scaledFontSize);
            }

            if (isStrophe || isRefrain) {
                marginBottom = scaleValue(SPACING.STROPHE, scaledFontSize);
            }

            currentY += marginTop;
            await pushTextBlock(text, fontSize, fontStyle, config.textAlign,
                marginBottom, { isCopyright, isRefrain, isStrophe });
        }
        currentY += scaleValue(SPACING.OBJECT_DEFAULT, scaledFontSize);
    }

    // Letzte Seite abschließen
    if (currentBlocks.length > 0) {
        pages.push({ pageNumber: pages.length + 1, blocks: currentBlocks });
    }

    return { pages, totalPages: pages.length };
}
```

WICHTIG beim Implementieren:
- Kein `window.xxx` Zugriff (außer innerhalb von parseQuillHTML — DOMParser ist Browser-API, kein side-effect)
- Kein `document.getElementById`, kein `element.offsetHeight`, kein `window.getComputedStyle`
- splitTextToLines ist der einzige Ort wo font.widthOfTextAtSize aufgerufen wird
- Die obige Action enthält die vollständige Zielstruktur — implementiere exakt diese API-Form
  </action>
  <verify>
    <automated>node --input-type=module -e "
import { readFile } from 'fs/promises';
const content = await readFile('/Users/simonluthe/Documents/HymnoScribe/frontend/js/layout/engine.js', 'utf8');
const hasCalcLayout = content.includes('export async function calculateLayout');
const hasParseQuill = content.includes('export function parseQuillHTML');
const hasSplitLines = content.includes('export async function splitTextToLines');
const hasNoWindow = !content.includes('window.getComputedStyle') && !content.includes('element.offsetHeight');
const hasNoGlobalState = !content.includes('window.lastCalculated');
const hasDOMParser = content.includes('DOMParser');
const hasImportConstants = content.includes(\"from './constants.js'\");
console.log('calculateLayout export:', hasCalcLayout);
console.log('parseQuillHTML export:', hasParseQuill);
console.log('splitTextToLines export:', hasSplitLines);
console.log('Kein window.getComputedStyle/offsetHeight:', hasNoWindow);
console.log('Kein window.lastCalculated:', hasNoGlobalState);
console.log('DOMParser vorhanden:', hasDOMParser);
console.log('Importiert constants.js:', hasImportConstants);
if (!hasCalcLayout || !hasParseQuill || !hasSplitLines || !hasNoWindow || !hasNoGlobalState || !hasDOMParser || !hasImportConstants) {
  process.exit(1);
}
console.log('OK');
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert unter frontend/js/layout/engine.js
    - Drei Exports: calculateLayout (async), parseQuillHTML, splitTextToLines (async)
    - Import aus './constants.js' — keine lokalen Konstanten-Definitionen
    - KEIN window.getComputedStyle, KEIN element.offsetHeight im gesamten File
    - KEIN window.lastCalculatedBreakPositions oder ähnliche globale State-Schreibzugriffe
    - DOMParser wird verwendet (für parseQuillHTML)
    - calculateLayout gibt { pages, totalPages } zurück (LayoutResult-Struktur)
    - Jede page hat { pageNumber, blocks } 
    - Jeder block hat { type, x, y, width, height, data }
    - splitTextToLines verwendet font.widthOfTextAtSize (nicht DOM-Maßnahmen)
  </acceptance_criteria>
  <done>engine.js exportiert calculateLayout, parseQuillHTML, splitTextToLines. Kein DOM-Seiteneffekt außer DOMParser. Alle Konstanten aus constants.js importiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM-Input → Engine | items-Array enthält DOM-Elemente — innerHTML wird via DOMParser gelesen (kein eval, kein Script-Execution-Risiko) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | parseQuillHTML DOMParser | accept | DOMParser erzeugt isoliertes Document — Script-Tags werden nicht ausgeführt. Quill-Output ist bereits sanitized. |
| T-02-02-02 | Information Disclosure | calculateLayout | accept | Reine Berechnungsfunktion, keine Netzwerk-Calls, kein Storage-Zugriff |
</threat_model>

<verification>
Nach Abschluss:
1. `ls frontend/js/layout/` zeigt constants.js, fontManager.js, engine.js
2. Der Verifikationsbefehl aus Task 1 läuft durch (alle 7 Checks grün)
3. Manuell prüfen: engine.js enthält KEIN `element.offsetHeight` und KEIN `window.getComputedStyle`
4. generatePDF.js ist noch unverändert (splitTextToLines existiert noch doppelt — wird in 02-03 aufgelöst)
</verification>

<success_criteria>
- engine.js exportiert calculateLayout, parseQuillHTML, splitTextToLines
- Kein DOM-Seiteneffekt außer DOMParser (isoliertes Parsing)
- LayoutResult-Struktur: { pages: [{ pageNumber, blocks: [{ type, x, y, width, height, data }] }], totalPages }
- Alle Y-Koordinaten in pt, y=0 = Seitenoberkante
- Importiert ausschließlich aus constants.js (keine anderen lokalen Abhängigkeiten)
</success_criteria>

<output>
Nach Completion: `.planning/phases/02-unified-layout-engine/02-02-SUMMARY.md` erstellen mit:
- engine.js API-Signatur (für Plan 02-03 Referenz)
- LayoutResult-Struktur (exaktes Schema mit Typen)
- Welche DOM-Elemente werden klassifiziert (page-break, trenner, img, quill, lied-item)
- Hinweis auf Y-Koordinaten-Konvention (y=0 oben, PDF-Renderer muss invertieren)
</output>
