---
phase: 01-security-backend
plan: 05
type: execute
wave: 3
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - backend/routes/auth.js
  - backend/routes/users.js
  - backend/routes/institutions.js
  - backend/routes/objekte.js
  - backend/routes/sessions.js
  - backend/routes/vorlagen.js
  - backend/routes/uploads.js
  - backend/routes/contact.js
  - backend/server.js
  - backend/app.js
autonomous: false
requirements:
  - SEC-01
  - SEC-06
  - SEC-08
  - BACK-01
  - BACK-02
must_haves:
  truths:
    - "server.js enthält nur noch: dotenv-Load, app.listen()-Aufruf nach initializeDatabase(), cron-Job-Start"
    - "Alle API-Routen sind in backend/routes/ Dateien organisiert"
    - "Rate Limiter (loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter) sind auf den sechs Auth-Endpunkten aktiv"
    - "Input-Validierung ist auf allen HIGH-Risk-Endpunkten aktiv"
    - "Der Server startet ohne Fehler und bestehende Funktionen sind unverändert"
    - "app.js importiert den zentralen Routes-Router"
  artifacts:
    - path: "backend/routes/auth.js"
      provides: "Auth-Routen mit Rate Limitern und Validierung"
      contains: "loginLimiter"
    - path: "backend/server.js"
      provides: "Reiner Bootstrapper — dotenv + initializeDatabase + app.listen"
      contains: "app.listen"
  key_links:
    - from: "backend/app.js"
      to: "backend/routes/index.js"
      via: "app.use('/api', require('./routes')) — zentraler Router-Einstieg"
      pattern: "require\\('./routes'\\)"
    - from: "backend/routes/auth.js"
      to: "backend/middleware/rateLimits.js"
      via: "loginLimiter als Middleware auf POST /login"
      pattern: "loginLimiter"
---

<objective>
Routes/Controllers aus server.js extrahieren und den Monolith-Split abschliessen. server.js wird zum reinen Bootstrapper.

Purpose: Letzter Modularisierungs-Schritt (BACK-01 vollständig). Alle Routen werden in backend/routes/ Dateien organisiert. Dabei werden Rate Limiter und Input-Validierung (aus Plan 03) auf den richtigen Endpunkten eingehängt — das ist der Moment wo SEC-01, SEC-06 und SEC-08 tatsächlich aktiv werden. Danach: Server startet aus app.js, server.js ist nur noch Bootstrapper.

Output: 8 Route-Dateien in backend/routes/, server.js reduziert auf ~30 Zeilen, app.js mit aktivem API-Router.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-backend/01-RESEARCH.md
@backend/server.js
@.planning/phases/01-security-backend/01-03-SUMMARY.md
@.planning/phases/01-security-backend/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Route-Zuordnung aus RESEARCH.md Architecture Patterns -->

routes/auth.js: /login, /super-login, /verify-token, /request-password-reset, /reset-password, /set-password, /request-email-verification, /verify-email
routes/users.js: /admin/user, /admin/users, /user/* (change-password, change-email, delete-account, resend-verification)
routes/institutions.js: /admin/institution, /admin/institutions
routes/objekte.js: /objekte (GET, POST, PUT, DELETE)
routes/sessions.js: /sessions (GET, POST, PUT, DELETE)
routes/vorlagen.js: /vorlagen (GET, POST, PUT, DELETE)
routes/uploads.js: /upload-logo, /upload-custom-image, /upload-noten, /upload-liturgie
routes/contact.js: /contact

<!-- Middleware-Importe die Routes brauchen -->
```javascript
const { loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter } = require('../middleware/rateLimits');
const { loginValidation, passwordValidation, emailValidation, userCreateValidation, userUpdateValidation, handleValidationErrors } = require('../middleware/validation');
const { authenticateToken, authenticateAdmin, authenticateSuperAdmin } = require('../middleware/auth');
```

<!-- Rate Limiter Zuweisung (aus RESEARCH.md Pattern 2 + Endpoint-Inventur) -->
POST /login → loginLimiter
POST /super-login → superLoginLimiter
POST /request-password-reset → resetLimiter
POST /reset-password → resetLimiter
POST /set-password → resetLimiter
POST /request-email-verification → verificationLimiter
POST /verify-email → verificationLimiter

<!-- Validation Zuweisung (HIGH-Risk, aus RESEARCH.md Endpoint-Inventur Priorität 1) -->
POST /login → loginValidation, handleValidationErrors
POST /request-password-reset → emailValidation, handleValidationErrors
POST /reset-password → passwordValidation, handleValidationErrors
POST /set-password → passwordValidation, handleValidationErrors
POST /admin/user → userCreateValidation, handleValidationErrors
PUT /admin/users/:id → userUpdateValidation, handleValidationErrors
PUT /users/change-password → passwordValidation, handleValidationErrors
PUT /user/change-email → changeEmailValidation, handleValidationErrors
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Routes extrahieren — auth.js, users.js, institutions.js, restliche Routes</name>
  <files>
    backend/routes/auth.js,
    backend/routes/users.js,
    backend/routes/institutions.js,
    backend/routes/objekte.js,
    backend/routes/sessions.js,
    backend/routes/vorlagen.js,
    backend/routes/uploads.js,
    backend/routes/contact.js,
    backend/routes/index.js
  </files>
  <read_first>
    - backend/server.js Zeilen 150-500 (Auth-Routen und User-Routen vollständig)
    - backend/server.js Zeilen 500-900 (Objekte, Sessions, Vorlagen)
    - backend/server.js Zeilen 900-1230 (Uploads, restliche Routen)
    - .planning/phases/01-security-backend/01-03-SUMMARY.md (welche Validierungsketten verfügbar sind)
    - .planning/phases/01-security-backend/01-04-SUMMARY.md (welche Services verfügbar sind)
  </read_first>
  <action>
    Erstelle backend/routes/ Verzeichnis und 9 Dateien. Strategie: ERST alle Route-Dateien schreiben, DANN server.js anfassen.

    **Für jede Route-Datei:**
    ```javascript
    const express = require('express');
    const router = express.Router();
    // [benötigte Middleware-Imports]
    // [benötigte Service-Imports]
    // [benötigte DB-Imports: const { pool } = require('../db/pool')]

    // Routen-Handler EXAKT aus server.js kopieren
    // Middleware-Stack anpassen: Rate Limiter + Validation VOR dem Handler

    module.exports = router;
    ```

    **routes/auth.js — Rate Limiter und Validation anwenden:**
    Jede Route muss in genau dieser Middleware-Reihenfolge stehen:
    ```javascript
    // POST /login:
    router.post('/login', loginLimiter, loginValidation, handleValidationErrors, async (req, res) => { ... });

    // POST /super-login:
    router.post('/super-login', superLoginLimiter, async (req, res) => { ... });

    // POST /request-password-reset:
    router.post('/request-password-reset', resetLimiter, emailValidation, handleValidationErrors, async (req, res) => { ... });

    // POST /reset-password:
    router.post('/reset-password', resetLimiter, passwordValidation, handleValidationErrors, async (req, res) => { ... });

    // POST /set-password:
    router.post('/set-password', resetLimiter, passwordValidation, handleValidationErrors, async (req, res) => { ... });

    // POST /request-email-verification:
    router.post('/request-email-verification', verificationLimiter, emailValidation, handleValidationErrors, async (req, res) => { ... });

    // POST /verify-email:
    router.post('/verify-email', verificationLimiter, async (req, res) => { ... });
    ```

    **routes/users.js — mit Validation:**
    ```javascript
    // POST /admin/user:
    router.post('/admin/user', authenticateAdmin, userCreateValidation, handleValidationErrors, async (req, res) => { ... });

    // PUT /admin/users/:id:
    router.put('/admin/users/:id', authenticateAdmin, userUpdateValidation, handleValidationErrors, async (req, res) => { ... });

    // PUT /users/change-password:
    router.put('/users/change-password', authenticateToken, passwordValidation, handleValidationErrors, async (req, res) => { ... });

    // PUT /user/change-email:
    router.put('/user/change-email', authenticateToken, changeEmailValidation, handleValidationErrors, async (req, res) => { ... });
    ```

    **routes/index.js — zentraler Router:**
    ```javascript
    const express = require('express');
    const router = express.Router();

    router.use('/', require('./auth'));
    router.use('/', require('./users'));
    router.use('/', require('./institutions'));
    router.use('/', require('./objekte'));
    router.use('/', require('./sessions'));
    router.use('/', require('./vorlagen'));
    router.use('/', require('./uploads'));
    router.use('/', require('./contact'));

    module.exports = router;
    ```

    **Handler-Inhalt:** Jeder Route-Handler wird EXAKT aus server.js kopiert. Keine inhaltliche Änderung der Geschäftslogik — nur Extraktion.

    **Services ersetzen direkte Aufrufe:** Wo server.js z.B. `sendPasswordResetEmail()` direkt aufrief (weil die Funktion in derselben Datei stand), muss jetzt importiert werden:
    ```javascript
    const { sendPasswordResetEmail } = require('../services/emailService');
    ```

    Multer-Konfigurationen (customImageStorage etc.) die in server.js oben definiert sind und von Uploads genutzt werden: In routes/uploads.js mitextrahieren.
  </action>
  <verify>
    <automated>node -c /Users/simonluthe/Documents/HymnoScribe/backend/routes/index.js && node -c /Users/simonluthe/Documents/HymnoScribe/backend/routes/auth.js && echo "Routes Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - Alle 9 Dateien existieren: routes/index.js, routes/auth.js, routes/users.js, routes/institutions.js, routes/objekte.js, routes/sessions.js, routes/vorlagen.js, routes/uploads.js, routes/contact.js
    - `grep -c "loginLimiter" backend/routes/auth.js` gibt mindestens 1 (Rate Limiter auf /login aktiv)
    - `grep -c "superLoginLimiter" backend/routes/auth.js` gibt mindestens 1
    - `grep -c "resetLimiter" backend/routes/auth.js` gibt mindestens 3 (reset-password, set-password, request-reset)
    - `grep -c "verificationLimiter" backend/routes/auth.js` gibt mindestens 2
    - `grep -c "passwordValidation" backend/routes/auth.js` gibt mindestens 2 (/reset-password, /set-password)
    - `grep -c "loginValidation" backend/routes/auth.js` gibt mindestens 1
    - `grep -c "userCreateValidation" backend/routes/users.js` gibt mindestens 1
    - `grep -c "handleValidationErrors" backend/routes/auth.js` gibt mindestens 4
    - node -c auf alle 9 Dateien gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>9 Route-Dateien erstellt, Rate Limiter auf 7 Auth-Endpunkten aktiv, Input-Validierung auf allen HIGH-Risk-Endpunkten aktiv</done>
</task>

<task type="auto">
  <name>Task 2: server.js auf Bootstrapper reduzieren und app.js aktivieren</name>
  <files>backend/server.js, backend/app.js</files>
  <read_first>
    - backend/server.js Zeilen 1450-1463 (app.listen Block am Ende)
    - backend/server.js Zeilen 1-25 (Imports und dotenv-Load)
    - backend/app.js (aktueller Stand aus Plan 01 — API-Router-Kommentar aktivieren)
    - .planning/phases/01-security-backend/01-01-SUMMARY.md (Middleware-Reihenfolge wie dokumentiert)
  </read_first>
  <action>
    **Schritt A: app.js — API-Router aktivieren**

    In app.js den auskommentierten Router-Block aktivieren:
    ```javascript
    // VORHER (aus Plan 01):
    // app.use('/api', require('./routes'));

    // NACHHER:
    app.use('/api', require('./routes'));
    ```

    **Schritt B: server.js auf Bootstrapper reduzieren**

    server.js wird auf folgende Struktur reduziert (alle Routen, Handler, Middleware, Service-Funktionen sind jetzt in den Modulen):

    ```javascript
    const path = require('path');
    require('dotenv').config({ path: path.resolve(__dirname, '..', '.env') });

    const app = require('./app');
    const { initializeDatabase } = require('./db/pool');
    const { cleanupUnusedImages } = require('./services/imageCleanupService');
    const cron = require('node-cron');

    const PORT = process.env.PORT || 3000;

    async function startServer() {
        await initializeDatabase();
        app.listen(PORT, () => {
            console.log(`HymnoScribe läuft auf Port ${PORT}`);
        });

        // Stündlicher Cleanup-Job
        cron.schedule('0 * * * *', async () => {
            console.log('Führe geplante stündliche Bildbereinigung durch...');
            try {
                const result = await cleanupUnusedImages();
                console.log(`Cron-Job Ergebnis: ${result.deletedCount} von ${result.scannedCount} Dateien gelöscht.`);
            } catch (error) {
                console.error('Fehler beim Cleanup-Job:', error);
            }
        });
    }

    startServer().catch(console.error);
    ```

    **Wichtig:** Alle Route-Handler, alle Middleware-Definitionen, alle Service-Funktionen aus server.js entfernen — sie sind jetzt in den Modulen. Nur der Bootstrapper-Code bleibt. Kein Code verdoppeln.

    **Nach dem Umbau:** Server mit `URL=https://hymnoscribe.de node server.js` (aus backend/) testen. Erwarteter Output: "HymnoScribe läuft auf Port 3000" (oder konfigurierten Port).
  </action>
  <verify>
    <automated>wc -l /Users/simonluthe/Documents/HymnoScribe/backend/server.js</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l backend/server.js` gibt einen Wert <= 50 aus (Bootstrapper hat ~30-40 Zeilen)
    - `grep -c "app.listen" backend/server.js` gibt 1 zurück
    - `grep -c "initializeDatabase" backend/server.js` gibt 1 zurück
    - `grep -c "require('./app')" backend/server.js` gibt 1 zurück
    - `grep -c "cron.schedule" backend/server.js` gibt 1 zurück
    - `grep -c "apiRouter.post\|apiRouter.get\|apiRouter.put\|apiRouter.delete" backend/server.js` gibt 0 (keine Route-Definitionen mehr in server.js)
    - `grep "// app.use('/api'" backend/app.js | wc -l` gibt 0 (Router-Import nicht mehr auskommentiert)
    - `grep -c "require('./routes')" backend/app.js` gibt 1 (Router aktiv)
    - node -c backend/server.js gibt keinen Syntax-Fehler
    - node -c backend/app.js gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>server.js ist reiner Bootstrapper (~30-40 Zeilen), app.js hat aktiven API-Router — Modularisierung vollständig abgeschlossen</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständige Modularisierung und alle Security-Fixes:
    - Rate Limiting auf 7 Auth-Endpunkten (loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter)
    - Input-Validierung auf allen HIGH-Risk-Endpunkten
    - Passwort-Regeln (min 8 Zeichen, Großbuchstabe, Zahl, Sonderzeichen)
    - Helmet Security Headers (CSP, HSTS, etc.)
    - CORS ohne Wildcard (Pflicht-Env-Var URL)
    - SQL-Injection-Fix (Whitelist)
    - Kein Credential-Logging
    - TLS aktiv für E-Mail
    - server.js: ~30-40 Zeilen Bootstrapper
    - backend/middleware/, backend/routes/, backend/services/, backend/db/ Verzeichnisse
  </what-built>
  <how-to-verify>
    1. **Server starten:**
       ```bash
       cd /Users/simonluthe/Documents/HymnoScribe/backend
       URL=https://hymnoscribe.de node server.js
       ```
       Erwartet: "HymnoScribe läuft auf Port 3000" — KEIN Fehler, KEIN Credential-Log

    2. **Login testen (Funktion unverändert):**
       ```bash
       curl -X POST http://localhost:3000/api/login \
         -H "Content-Type: application/json" \
         -d '{"usernameOrEmail":"testuser","password":"wrongpassword"}'
       ```
       Erwartet: HTTP 401 (nicht 500, nicht CORS-Fehler)

    3. **Rate Limiting testen:**
       ```bash
       for i in {1..6}; do
         curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3000/api/login \
           -H "Content-Type: application/json" \
           -d '{"usernameOrEmail":"test@test.de","password":"wrong"}';
       done
       ```
       Erwartet: Erste 5 Antworten: 401, 6. Antwort: 429 (Too Many Requests)

    4. **Passwort-Validierung testen:**
       ```bash
       curl -X POST http://localhost:3000/api/reset-password \
         -H "Content-Type: application/json" \
         -d '{"token":"dummy","newPassword":"abc"}'
       ```
       Erwartet: HTTP 400 mit errors-Array (nicht 500)

    5. **Helmet Headers testen:**
       ```bash
       curl -I http://localhost:3000/api/verify-token
       ```
       Erwartet: Response enthält "X-Content-Type-Options: nosniff" und "X-Frame-Options"

    6. **Verzeichnisstruktur prüfen:**
       ```bash
       ls backend/middleware/ backend/routes/ backend/services/ backend/db/
       ```
       Erwartet: Alle Dateien vorhanden

    7. **Credential-Logging prüfen:**
       Server-Output beim Start: DARF NICHT "password" oder Passwort-Wert enthalten
  </how-to-verify>
  <resume-signal>Alle 7 Tests bestanden? Gib "approved" ein. Bei Fehlern: beschreibe welcher Test fehlschlug und was du gesehen hast.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Rate Limiter → Auth-Handler | Brute-Force-Schutz muss in der Route-Middleware-Kette aktiv sein |
| Routes → Services | Service-Imports müssen korrekte Pfade haben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Denial of Service | POST /login nach Extraktion | mitigate | loginLimiter als erste Middleware in routes/auth.js vor Handler eingehängt — `router.post('/login', loginLimiter, loginValidation, handleValidationErrors, handler)` |
| T-01-16 | Elevation of Privilege | Middleware-Reihenfolge nach Refactoring | mitigate | Nummerierte Kommentare in app.js (BACK-02); Checkpoint prüft tatsächliches Server-Verhalten |
| T-01-17 | Tampering | Route-Handler-Logik bei Extraktion verändert | mitigate | Handler EXAKT aus server.js kopieren ohne inhaltliche Änderung; Checkpoint 7-Test-Suite verifiziert Verhalten |
</threat_model>

<verification>
Phase 1 vollständig abgeschlossen wenn:
1. Server startet ohne Fehler mit gesetzter URL-Env-Var
2. Rate Limiting blockiert nach 5 Versuchen (HTTP 429)
3. Passwort-Validierung lehnt schwache Passwörter ab (HTTP 400)
4. Helmet-Headers im Response (X-Content-Type-Options vorhanden)
5. server.js hat <= 50 Zeilen
6. Kein Credential-Logging beim Start
7. `grep -c "rejectUnauthorized: false" backend/services/emailService.js` — gibt 0
</verification>

<success_criteria>
- Alle 10 Requirements (SEC-01 bis SEC-08, BACK-01, BACK-02) erfüllt
- Server startet und alle bestehenden Funktionen laufen unverändert
- Rate Limiting aktiv auf 7 Endpoints (429 nach 5 Versuchen)
- Passwort-Validierung aktiv (400 bei schwachen Passwörtern)
- server.js ist reiner Bootstrapper (~30-40 Zeilen)
- Modularisierte Verzeichnisstruktur: middleware/, routes/, services/, db/
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-security-backend/01-05-SUMMARY.md` erstellen mit:
- Verzeichnisstruktur (vollständige Dateiliste)
- Checkpoint-Ergebnis (approved / Fehler beschreiben)
- Zeilen in server.js nach Reduktion
- Welche Requirements mit welchem Deliverable erfüllt wurden
- Hinweis für REQUIREMENTS.md-Update (alle SEC-01 bis BACK-02 auf "Done" setzen)
</output>
