---
phase: 01-security-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/app.js
autonomous: true
requirements:
  - SEC-04
  - BACK-02
must_haves:
  truths:
    - "Die drei neuen Libraries (helmet, express-rate-limit, express-validator) sind in package.json installiert"
    - "backend/app.js existiert mit dokumentierter, nummerierter Middleware-Reihenfolge"
    - "Env-Var-Validierung beim App-Start bricht den Prozess mit process.exit(1) ab wenn URL nicht gesetzt ist"
    - "app.js exportiert die Express-App (module.exports = app)"
  artifacts:
    - path: "backend/app.js"
      provides: "Express-App Grundgerüst mit dokumentierter Middleware-Reihenfolge"
      exports: ["app"]
    - path: "backend/package.json"
      provides: "Aktualisierte Dependencies mit helmet, express-rate-limit, express-validator"
      contains: "helmet"
  key_links:
    - from: "backend/app.js"
      to: "backend/server.js"
      via: "module.exports = app — server.js wird später app.js importieren"
      pattern: "module\\.exports\\s*=\\s*app"
---

<objective>
Libraries installieren und app.js als neue Express-App-Datei mit dokumentierter Middleware-Reihenfolge anlegen.

Purpose: Schafft die Grundlage für alle weiteren Security-Fixes und die Modularisierung. Die Middleware-Reihenfolge muss BEVOR irgendeine Extraktion stattfindet dokumentiert sein (BACK-02). Env-Var-Validierung schliesst den CORS-Wildcard-Pitfall ab.

Output: backend/app.js (Grundgerüst, noch ohne vollständige Routes), package.json mit neuen Dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-security-backend/01-RESEARCH.md
@backend/server.js
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Libraries installieren</name>
  <files>backend/package.json, backend/package-lock.json</files>
  <read_first>
    - backend/package.json — aktuelle Dependencies prüfen bevor npm install
  </read_first>
  <action>
    Wechsle in das backend/-Verzeichnis und installiere die drei neuen Libraries:

    ```bash
    cd /Users/simonluthe/Documents/HymnoScribe/backend
    npm install helmet@8.1.0 express-rate-limit@8.3.2 express-validator@7.3.2
    ```

    Verifiziere, dass die Versionen exakt stimmen (nicht höher/niedriger):
    - helmet: 8.1.0
    - express-rate-limit: 8.3.2
    - express-validator: 7.3.2

    Keine weiteren Änderungen an package.json — nur was npm install hinzufügt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/HymnoScribe/backend && node -e "require('helmet'); require('express-rate-limit'); require('express-validator'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"helmet"' backend/package.json` gibt 1 zurück
    - `grep -c '"express-rate-limit"' backend/package.json` gibt 1 zurück
    - `grep -c '"express-validator"' backend/package.json` gibt 1 zurück
    - Node require() für alle drei Libraries wirft keinen Fehler
  </acceptance_criteria>
  <done>Alle drei Libraries sind installiert und per require() ladbar</done>
</task>

<task type="auto">
  <name>Task 2: app.js mit Middleware-Reihenfolge und Env-Validierung anlegen</name>
  <files>backend/app.js</files>
  <read_first>
    - backend/server.js — aktuelle Middleware-Reihenfolge (Zeilen 1-50 und 37-47, 150-160) lesen, bevor app.js erstellt wird
    - .planning/phases/01-security-backend/01-RESEARCH.md — Pattern 3 (CORS) und Middleware-Reihenfolge-Sektion
  </read_first>
  <action>
    Erstelle backend/app.js als neue Datei (noch existiert sie nicht). Diese Datei enthält:

    1. **Env-Var-Validierung** (per SEC-04) — MUSS vor pool-Erstellung und app.use() stehen:
    ```javascript
    const requiredEnvVars = ['DB_HOST', 'DB_USER', 'DB_PASSWORD', 'DB_NAME', 'JWT_SECRET', 'URL'];
    for (const envVar of requiredEnvVars) {
        if (!process.env[envVar]) {
            console.error(`FEHLER: Pflicht-Env-Var ${envVar} nicht gesetzt. Server wird beendet.`);
            process.exit(1);
        }
    }
    ```

    2. **Express-App mit nummerierter Middleware-Reihenfolge** (BACK-02):
    ```javascript
    const express = require('express');
    const helmet = require('helmet');
    const compression = require('compression');
    const cors = require('cors');
    const path = require('path');

    const app = express();

    // Middleware-Reihenfolge (BACK-02): Reihenfolge MUSS bei Modularisierung erhalten bleiben
    // 1. Security Headers — helmet() als ERSTE Middleware (vor compression, vor cors)
    app.use(helmet({
        contentSecurityPolicy: {
            directives: {
                defaultSrc: ["'self'"],
                scriptSrc: ["'self'", "cdn.jsdelivr.net", "cdnjs.cloudflare.com", "unpkg.com"],
                styleSrc: ["'self'", "'unsafe-inline'", "cdn.jsdelivr.net", "cdnjs.cloudflare.com"],
                imgSrc: ["'self'", "data:", "blob:"],
                fontSrc: ["'self'", "unpkg.com", "cdnjs.cloudflare.com"],
                connectSrc: ["'self'"]
            }
        }
    }));

    // 2. HTTP-Kompression
    app.use(compression());

    // 3. Body-Parser (50MB für Bild-Uploads — zusammengeführt aus zwei express.json()-Aufrufen)
    app.use(express.json({ limit: '50mb' }));
    app.use(express.urlencoded({ limit: '50mb', extended: true }));

    // 4. CORS — nur explizite Origins, kein Wildcard-Fallback (SEC-04)
    app.use(cors({
        origin: process.env.URL.split(','),
        methods: ['GET', 'POST', 'PUT', 'DELETE'],
        allowedHeaders: ['Content-Type', 'Authorization']
    }));

    // 5. Static Files (Reihenfolge aus server.js erhalten)
    app.use('/api/icons', express.static(path.join(__dirname, 'icons')));
    app.use('/api/uploads', express.static(path.join(__dirname, 'uploads')));
    app.use('/api/ttf', express.static(path.join(__dirname, 'ttf')));

    // 6. API-Router — wird in Plan 05 (BACK-01) eingehängt
    // app.use('/api', require('./routes'));

    // 7. Frontend Static Serving
    app.use(express.static(path.join(__dirname, '../frontend')));

    module.exports = app;
    ```

    **Wichtig:** Der API-Router-Kommentar (Schritt 6) bleibt auskommentiert — wird in Plan 05 aktiviert wenn die Routes-Extraktion abgeschlossen ist. Die Datei muss loadable sein ohne Fehler.

    Die Env-Var-Validierung muss OBEN in app.js stehen, BEVOR require('dotenv') Zeilen aus server.js übernommen werden. server.js bleibt unverändert in dieser Wave.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/HymnoScribe/backend && URL=https://test.example.com node -e "require('./app'); console.log('app.js loaded OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/app.js existiert
    - `grep -c "module.exports = app" backend/app.js` gibt 1 zurück
    - `grep -c "helmet(" backend/app.js` gibt 1 zurück
    - `grep -c "process.exit(1)" backend/app.js` gibt mindestens 1 zurück
    - `grep -c "Middleware-Reihenfolge" backend/app.js` gibt mindestens 1 zurück (Dokumentationskommentar)
    - `grep -c "contentSecurityPolicy" backend/app.js` gibt 1 zurück (CDN-Freigaben für pdf-lib, Quill, fontkit)
    - `grep "origin: process.env.URL.split" backend/app.js` findet einen Treffer (kein Wildcard-Fallback)
    - Laden von app.js via `node -e "require('./app')"` mit gesetzter URL-Env-Var wirft keinen Fehler
    - Laden ohne URL-Env-Var: `URL="" node -e "require('./app')"` beendet Prozess mit Exit-Code 1
  </acceptance_criteria>
  <done>app.js existiert, ist loadable, enthält CSP für CDN-Ressourcen, Env-Validierung und dokumentierte Middleware-Reihenfolge</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Express | HTTP-Requests von beliebigen Clients treffen alle Middleware-Layers |
| Env-Vars → App-Start | Fehlende/falsche Env-Vars können Security-Features deaktivieren |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | CORS origin check | mitigate | URL als Pflicht-Env-Var; process.exit(1) wenn leer — kein Wildcard-Fallback möglich |
| T-01-02 | Information Disclosure | HTTP Response Headers | mitigate | helmet() als erste Middleware setzt X-Content-Type-Options, removes X-Powered-By, setzt HSTS |
| T-01-03 | Tampering | CSP-Bypass über CDN | mitigate | Explizite CDN-Origins in CSP: cdn.jsdelivr.net, cdnjs.cloudflare.com, unpkg.com — kein 'unsafe-eval' |
</threat_model>

<verification>
Nach Abschluss beider Tasks:
1. `cd backend && node -e "require('helmet'); require('express-rate-limit'); require('express-validator'); console.log('Libraries OK')"` — muss "Libraries OK" ausgeben
2. `URL=https://hymnoscribe.de node -e "require('./app'); console.log('app.js OK')"` — muss ohne Fehler laufen
3. `grep "process.exit" app.js` — muss Treffer liefern
4. `grep "contentSecurityPolicy" app.js` — muss Treffer liefern (CDN-Freigaben)
5. `grep -v "wildcard\|'\*'" app.js` — kein Wildcard in CORS
</verification>

<success_criteria>
- helmet@8.1.0, express-rate-limit@8.3.2, express-validator@7.3.2 in node_modules installiert
- backend/app.js existiert mit: Env-Validierung (URL Pflichtfeld), helmet() als erste Middleware mit CDN-CSP, CORS ohne Wildcard-Fallback, dokumentierter Middleware-Reihenfolge, module.exports = app
- server.js ist unverändert (Änderungen kommen in Plan 02)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-security-backend/01-01-SUMMARY.md` erstellen mit:
- Installierten Library-Versionen
- CSP-Konfiguration (welche CDN-Origins freigegeben)
- Env-Vars die als Pflichtfelder validiert werden
- Middleware-Reihenfolge (nummeriert, für BACK-02 Nachweis)
</output>
