---
phase: 01-security-backend
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/db/pool.js
  - backend/services/emailService.js
  - backend/services/imageCleanupService.js
autonomous: true
requirements:
  - BACK-01
must_haves:
  truths:
    - "MySQL-Pool und initializeDatabase() sind in db/pool.js extrahiert und exportiert"
    - "E-Mail-Funktionen (createTransporter, sendPasswordResetEmail, sendEmailVerification, sendWelcomeEmail, sendContactEmail) sind in services/emailService.js extrahiert"
    - "Cleanup-Logik (cleanupUnusedImages) ist in services/imageCleanupService.js extrahiert"
    - "Alle Service-Module sind per require() ohne Fehler ladbar"
  artifacts:
    - path: "backend/db/pool.js"
      provides: "MySQL-Pool-Konfiguration und initializeDatabase-Funktion"
      exports: ["pool", "initializeDatabase"]
    - path: "backend/services/emailService.js"
      provides: "E-Mail-Sendefunktionen mit korrekt konfiguriertem Transporter (TLS aktiv)"
      exports: ["sendPasswordResetEmail", "sendEmailVerification", "sendWelcomeEmail", "sendContactEmail"]
    - path: "backend/services/imageCleanupService.js"
      provides: "Cleanup-Logik für ungenutzte Bilder"
      exports: ["cleanupUnusedImages"]
  key_links:
    - from: "backend/db/pool.js"
      to: "MySQL"
      via: "mysql2.createPool mit Env-Vars aus app.js Env-Validierung"
      pattern: "mysql.createPool"
    - from: "backend/middleware/auth.js"
      to: "backend/db/pool.js"
      via: "require('../db/pool') — löst den temporären Import aus Plan 03 auf"
      pattern: "require.*db/pool"
---

<objective>
Services und DB-Layer extrahieren: db/pool.js, services/emailService.js, services/imageCleanupService.js anlegen.

Purpose: Zweiter Modularisierungs-Schritt (BACK-01). Diese Module haben keine Abhängigkeiten zu den Middleware-Modulen aus Plan 03 — deshalb kann Plan 04 parallel zu Plan 03 laufen. db/pool.js löst außerdem den pending Import in middleware/auth.js auf (der dort mit require('../db/pool') schon vorbereitet ist). server.js bleibt noch der Haupt-Einstiegspunkt bis Plan 05.

Output: backend/db/ und backend/services/ Verzeichnisse mit je einer bis zwei Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-backend/01-RESEARCH.md
@backend/server.js
@.planning/phases/01-security-backend/01-01-SUMMARY.md
@.planning/phases/01-security-backend/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Zu extrahierende Code-Blöcke aus server.js -->

MySQL Pool (server.js Zeilen 24-32):
```javascript
const pool = mysql.createPool({
    host: process.env.DB_HOST,
    user: process.env.DB_USER,
    password: process.env.DB_PASSWORD,
    database: process.env.DB_NAME,
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
});
```

initializeDatabase() — ca. Zeilen 1230-1280: Erstellt DB-Tabellen wenn nicht vorhanden. Enthält die drei DDL-Stellen, die in Plan 02 mit ALLOWED_TABLES-Whitelist gesichert wurden.

E-Mail-Funktionen (ca. Zeilen 1283-1460):
- createTransporter() — erstellt Nodemailer-Transporter (nach Plan 02 ohne tls.rejectUnauthorized: false)
- sendPasswordResetEmail(email, resetLink)
- sendEmailVerification(email, verificationLink, username)
- sendWelcomeEmail(email, username)
- sendContactEmail(name, email, message, inquiryType)

imageCleanupService (ca. Zeilen 90-105 oder ähnlich):
- cleanupUnusedImages() — scannt uploads/, vergleicht mit DB-Referenzen, löscht nicht-referenzierte Bilder
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: db/pool.js erstellen — MySQL-Pool und initializeDatabase extrahieren</name>
  <files>backend/db/pool.js</files>
  <read_first>
    - backend/server.js Zeilen 1-35 (Pool-Konfiguration oben)
    - backend/server.js Zeilen 1225-1285 (initializeDatabase-Funktion vollständig — mit den ALLOWED_TABLES-Guards aus Plan 02)
  </read_first>
  <action>
    Erstelle das Verzeichnis backend/db/ und die Datei backend/db/pool.js.

    Die Datei enthält:
    1. MySQL-Pool-Konfiguration (EXAKT aus server.js kopiert — keine Env-Validierung hier, die ist in app.js)
    2. initializeDatabase()-Funktion (EXAKT aus server.js kopiert, mit den ALLOWED_TABLES-Guards aus Plan 02)

    ```javascript
    const mysql = require('mysql2/promise');

    const pool = mysql.createPool({
        host: process.env.DB_HOST,
        user: process.env.DB_USER,
        password: process.env.DB_PASSWORD,
        database: process.env.DB_NAME,
        waitForConnections: true,
        connectionLimit: 10,
        queueLimit: 0
    });

    // [ALLOWED_TABLES und initializeDatabase exakt aus server.js übernehmen]

    module.exports = { pool, initializeDatabase };
    ```

    **Wichtig:**
    - require('dotenv') NICHT in pool.js einfügen — dotenv wird in server.js beim Start geladen (bleibt so bis Plan 05)
    - ALLOWED_TABLES muss mit in die Datei (wurde in Plan 02 definiert, gehört logisch zur DB-Schicht)
    - server.js NICHT ändern
    - node -c backend/db/pool.js muss ohne Fehler durchlaufen
  </action>
  <verify>
    <automated>node -c /Users/simonluthe/Documents/HymnoScribe/backend/db/pool.js && echo "Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/db/pool.js existiert
    - `grep -c "module.exports" backend/db/pool.js` gibt 1 zurück
    - `grep -c "mysql.createPool\|mysql2" backend/db/pool.js` gibt mindestens 1 zurück
    - `grep -c "initializeDatabase" backend/db/pool.js` gibt mindestens 2 (Definition + Export)
    - `grep -c "ALLOWED_TABLES" backend/db/pool.js` gibt mindestens 4 (Konstante + 3 Guard-Nutzungen)
    - `grep -c "module.exports.*pool" backend/db/pool.js` gibt 1 zurück
    - `node -c backend/db/pool.js` gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>db/pool.js mit Pool-Konfiguration, ALLOWED_TABLES-Whitelist und initializeDatabase() — exportiert pool und initializeDatabase</done>
</task>

<task type="auto">
  <name>Task 2: services/emailService.js und services/imageCleanupService.js erstellen</name>
  <files>backend/services/emailService.js, backend/services/imageCleanupService.js</files>
  <read_first>
    - backend/server.js Zeilen 1280-1463 (E-Mail-Funktionen vollständig — createTransporter bis Ende)
    - backend/server.js Zeilen 85-110 (cleanupUnusedImages Funktion — Zeilen ggf. abweichend, nach Funktionsname suchen)
    - backend/email-template.html — wird von emailService referenziert, Pfad muss stimmen
    - backend/contact-email-template.html — wird von emailService referenziert, Pfad muss stimmen
  </read_first>
  <action>
    Erstelle das Verzeichnis backend/services/ und zwei Dateien:

    **Datei A: backend/services/emailService.js**

    Enthält alle E-Mail-Funktionen EXAKT aus server.js kopiert. Benötigt:
    - nodemailer
    - fs, path (für Template-Dateien)
    - Env-Vars (EMAIL_HOST, EMAIL_PORT, EMAIL_USER, EMAIL_PASS, EMAIL_FROM, LOGO_URL, FRONTEND_URL)

    ```javascript
    const nodemailer = require('nodemailer');
    const fs = require('fs');
    const path = require('path');

    // createTransporter() — exakt aus server.js, OHNE tls.rejectUnauthorized: false (Plan 02 hat das entfernt)
    // sendPasswordResetEmail(email, resetLink) — exakt aus server.js
    // sendEmailVerification(email, verificationLink, username) — exakt aus server.js
    // sendWelcomeEmail(email, username) — exakt aus server.js
    // sendContactEmail(name, email, message, inquiryType) — exakt aus server.js

    module.exports = {
        sendPasswordResetEmail,
        sendEmailVerification,
        sendWelcomeEmail,
        sendContactEmail
    };
    ```

    **Achtung zu Template-Pfaden:** Die Template-Dateien liegen in backend/ (email-template.html, contact-email-template.html). In server.js werden sie mit `path.join(__dirname, 'email-template.html')` referenziert. In emailService.js lautet der korrekte Pfad `path.join(__dirname, '..', 'email-template.html')` (da die Datei in backend/services/ liegt, ein Verzeichnis tiefer). Pfade entsprechend anpassen.

    **Datei B: backend/services/imageCleanupService.js**

    Enthält cleanupUnusedImages() exakt aus server.js. Benötigt:
    - fs, path
    - pool aus db/pool.js

    ```javascript
    const fs = require('fs').promises;
    const path = require('path');
    const { pool } = require('../db/pool');

    // cleanupUnusedImages() — exakt aus server.js
    // (Scannt uploads/, fragt DB nach Referenzen, löscht nicht-referenzierte Dateien)

    module.exports = { cleanupUnusedImages };
    ```

    server.js NICHT ändern.
  </action>
  <verify>
    <automated>node -c /Users/simonluthe/Documents/HymnoScribe/backend/services/emailService.js && node -c /Users/simonluthe/Documents/HymnoScribe/backend/services/imageCleanupService.js && echo "Beide OK"</automated>
  </verify>
  <acceptance_criteria>
    - backend/services/emailService.js existiert
    - backend/services/imageCleanupService.js existiert
    - `grep -c "sendPasswordResetEmail\|sendEmailVerification\|sendWelcomeEmail\|sendContactEmail" backend/services/emailService.js` gibt mindestens 8 Treffer (4 Definitionen + 4 Exports)
    - `grep -c "module.exports" backend/services/emailService.js` gibt 1 zurück
    - `grep -c "cleanupUnusedImages" backend/services/imageCleanupService.js` gibt mindestens 2 (Definition + Export)
    - `grep -c "require('../db/pool')" backend/services/imageCleanupService.js` gibt 1 zurück
    - `grep "rejectUnauthorized: false" backend/services/emailService.js | wc -l` gibt 0 (TLS-Fix aus Plan 02 ist erhalten)
    - `grep -c "path.join(__dirname, '..'" backend/services/emailService.js` gibt mindestens 1 (korrigierte Template-Pfade)
    - node -c auf beide Dateien gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>emailService.js mit 4 E-Mail-Funktionen (korrekte Template-Pfade, ohne tls.rejectUnauthorized: false), imageCleanupService.js mit cleanupUnusedImages — beide exportiert und syntaktisch korrekt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/emailService → SMTP-Server | E-Mail-Transport muss TLS-validiert sein |
| services/imageCleanupService → Filesystem | Cleanup darf nur uploads/-Dateien löschen, nicht andere Verzeichnisse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Information Disclosure | db/pool.js — Pool-Credentials aus Env-Vars | accept | Env-Vars werden in app.js validiert; kein Logging der Credentials in pool.js |
| T-01-13 | Tampering | imageCleanupService — File-Deletion außerhalb uploads/ | mitigate | Cleanup-Funktion exakt aus server.js kopieren (Pfad-Validierung dort bereits vorhanden); keine Erweiterung |
| T-01-14 | Information Disclosure | emailService — SMTP-Credentials im Log | accept | Nodemailer loggt keine Credentials; createTransporter() loggt nur Verbindungsfehler |
</threat_model>

<verification>
Nach Abschluss beider Tasks:
1. `node -c backend/db/pool.js` — OK
2. `node -c backend/services/emailService.js` — OK
3. `node -c backend/services/imageCleanupService.js` — OK
4. `grep "rejectUnauthorized: false" backend/services/emailService.js | wc -l` — muss 0 sein
5. `grep -c "ALLOWED_TABLES" backend/db/pool.js` — muss >= 4 sein
6. `ls backend/db/ backend/services/` — zeigt pool.js, emailService.js, imageCleanupService.js
</verification>

<success_criteria>
- backend/db/pool.js: exportiert pool und initializeDatabase, enthält ALLOWED_TABLES-Whitelist
- backend/services/emailService.js: exportiert alle 4 Sendefunktionen, korrigierte Template-Pfade, kein rejectUnauthorized: false
- backend/services/imageCleanupService.js: exportiert cleanupUnusedImages, importiert pool aus db/pool.js
- Alle drei Dateien syntaktisch korrekt
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-security-backend/01-04-SUMMARY.md` erstellen mit:
- Erstellte Verzeichnisse und Dateien
- Exportierte Funktionen je Modul
- Template-Pfad-Anpassungen in emailService.js (Vorher → Nachher)
- Ob pool.js über middleware/auth.js (Plan 03) verknüpft werden kann (Import-Kette prüfen)
</output>
