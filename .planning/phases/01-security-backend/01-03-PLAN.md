---
phase: 01-security-backend
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/middleware/auth.js
  - backend/middleware/rateLimits.js
  - backend/middleware/validation.js
autonomous: true
requirements:
  - SEC-01
  - SEC-06
  - SEC-07
  - SEC-08
  - BACK-01
must_haves:
  truths:
    - "Rate Limiting blockiert Login-Versuche nach 5 Fehlversuchen in 15 Minuten"
    - "Passwort-Validierung lehnt Passwörter unter 8 Zeichen ab"
    - "Passwort-Validierung verlangt Großbuchstaben, Zahlen und Sonderzeichen"
    - "Auth-Middleware (authenticateToken, authenticateAdmin, authenticateSuperAdmin, checkRole) ist aus server.js in middleware/auth.js extrahiert"
    - "Input-Validierungsketten für alle HIGH-Risk-Endpoints sind in middleware/validation.js definiert"
  artifacts:
    - path: "backend/middleware/auth.js"
      provides: "Auth-Middleware-Funktionen"
      exports: ["authenticateToken", "authenticateAdmin", "authenticateSuperAdmin", "checkRole"]
    - path: "backend/middleware/rateLimits.js"
      provides: "Rate Limiter Instanzen"
      exports: ["loginLimiter", "resetLimiter", "verificationLimiter", "superLoginLimiter"]
    - path: "backend/middleware/validation.js"
      provides: "Input-Validierungsketten"
      exports: ["loginValidation", "passwordValidation", "userCreateValidation", "userUpdateValidation", "emailValidation", "handleValidationErrors"]
  key_links:
    - from: "backend/middleware/auth.js"
      to: "backend/db/pool.js"
      via: "pool für Token-Blacklist oder User-Queries importieren"
      pattern: "require.*pool"
    - from: "backend/middleware/rateLimits.js"
      to: "backend/routes/auth.js (Plan 05)"
      via: "loginLimiter wird in Auth-Routes als Middleware eingehängt"
      pattern: "loginLimiter"
---

<objective>
Middleware-Module erstellen: Auth-Funktionen aus server.js extrahieren, Rate Limiter definieren, Input-Validierungsketten schreiben.

Purpose: Dies ist der erste Modularisierungs-Schritt (BACK-01). Die Middleware-Module werden von den Routes (Plan 05) importiert. Gleichzeitig werden SEC-01 (Rate Limiting), SEC-06 (Passwort-Regeln), SEC-07 (bereits in app.js — hier wird Helmet korrekt konfiguriert bestätigt) und SEC-08 (Input-Validierung) implementiert. server.js wird noch NICHT geändert — die Module werden vorbereitet.

Output: backend/middleware/ Verzeichnis mit drei Dateien. server.js bleibt vorerst unverändert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-security-backend/01-RESEARCH.md
@backend/server.js
@.planning/phases/01-security-backend/01-01-SUMMARY.md
@.planning/phases/01-security-backend/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Auth-Middleware in server.js — Zeilen 105-149 laut RESEARCH.md -->
<!-- Zu extrahierende Funktionen: -->

Aus server.js (Zeilen 105-149):
```javascript
// authenticateToken(req, res, next) — verifiziert JWT aus Authorization-Header
// authenticateAdmin(req, res, next) — ruft authenticateToken + checkRole('admin', 'super-admin')
// authenticateSuperAdmin(req, res, next) — ruft authenticateToken + checkRole('super-admin')
// checkRole(...roles) — gibt Middleware zurück die req.user.role gegen roles prüft
```

Rate Limiter (aus RESEARCH.md Pattern 2):
```javascript
const { rateLimit } = require('express-rate-limit');
const loginLimiter = rateLimit({
    windowMs: 15 * 60 * 1000,
    max: 5,
    standardHeaders: 'draft-7',
    legacyHeaders: false,
    message: { error: 'Zu viele Anmeldeversuche, bitte versuchen Sie es in 15 Minuten erneut.' }
});
```

Validation (aus RESEARCH.md Pattern 5):
```javascript
const { body, validationResult } = require('express-validator');
// passwordValidation: min 8 Zeichen, 1 Großbuchstabe, 1 Zahl, 1 Sonderzeichen
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: middleware/auth.js erstellen — Auth-Funktionen aus server.js extrahieren</name>
  <files>backend/middleware/auth.js</files>
  <read_first>
    - backend/server.js Zeilen 1-15 (require-Statements — welche Libraries auth.js braucht)
    - backend/server.js Zeilen 100-155 (Auth-Middleware-Funktionen vollständig lesen)
  </read_first>
  <action>
    Erstelle das Verzeichnis backend/middleware/ und die Datei backend/middleware/auth.js.

    Die Datei enthält die vier Auth-Funktionen, EXAKT kopiert aus server.js (Zeilen 105-149). Keine inhaltlichen Änderungen — nur Extraktion. Die Funktionen benötigen:
    - `jwt` (jsonwebtoken)
    - `pool` (MySQL-Pool — wird aus backend/db/pool.js importiert, die in Plan 04 erstellt wird)

    Da pool.js noch nicht existiert, temporär direkt aus server.js-Kontext importieren: `const pool = require('../db/pool');` — dieser Import wird in Plan 04 aufgelöst wenn db/pool.js erstellt wird.

    Struktur der Datei:
    ```javascript
    const jwt = require('jsonwebtoken');
    const pool = require('../db/pool'); // Wird in Plan 04 erstellt

    function authenticateToken(req, res, next) {
        // [exakt kopiert aus server.js]
    }

    function authenticateAdmin(req, res, next) {
        // [exakt kopiert aus server.js]
    }

    function authenticateSuperAdmin(req, res, next) {
        // [exakt kopiert aus server.js]
    }

    function checkRole(...roles) {
        // [exakt kopiert aus server.js]
    }

    module.exports = { authenticateToken, authenticateAdmin, authenticateSuperAdmin, checkRole };
    ```

    **Wichtig:** server.js NICHT ändern. Die Funktionen dort bleiben erhalten bis Plan 05 (vollständige Extraktion).
  </action>
  <verify>
    <automated>node -e "const m = require('/Users/simonluthe/Documents/HymnoScribe/backend/middleware/auth.js'); console.log(Object.keys(m).join(','))" 2>/dev/null || echo "pool.js fehlt noch — Import-Fehler erwartet, Datei aber erstellt"</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/middleware/auth.js existiert
    - `grep -c "module.exports" backend/middleware/auth.js` gibt 1 zurück
    - `grep "authenticateToken\|authenticateAdmin\|authenticateSuperAdmin\|checkRole" backend/middleware/auth.js | wc -l` gibt mindestens 8 Treffer (4 Definitionen + 4 Exports)
    - `grep -c "require('../db/pool')" backend/middleware/auth.js` gibt 1 zurück
    - `grep -c "jwt.verify\|jwt.sign" backend/middleware/auth.js` gibt mindestens 1 zurück (JWT-Nutzung vorhanden)
    - node -c backend/middleware/auth.js gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>middleware/auth.js enthält alle vier Auth-Funktionen, exportiert sie korrekt, importiert pool aus db/pool.js (noch nicht auflösbar bis Plan 04)</done>
</task>

<task type="auto">
  <name>Task 2: middleware/rateLimits.js und middleware/validation.js erstellen (SEC-01, SEC-06, SEC-08)</name>
  <files>backend/middleware/rateLimits.js, backend/middleware/validation.js</files>
  <read_first>
    - .planning/phases/01-security-backend/01-RESEARCH.md Zeilen 169-199 (Pattern 2: Rate Limiter)
    - .planning/phases/01-security-backend/01-RESEARCH.md Zeilen 253-391 (Pattern 5 + express-validator Beispiele)
    - backend/server.js Zeilen 226-246 (Endpoint-Definitionen für /request-password-reset, /set-password)
    - backend/server.js Zeilen 763-806 (change-password Endpoint)
  </read_first>
  <action>
    **Datei A: backend/middleware/rateLimits.js**

    ```javascript
    const { rateLimit } = require('express-rate-limit');

    // Für POST /login — 5 Versuche pro 15min pro IP (SEC-01)
    const loginLimiter = rateLimit({
        windowMs: 15 * 60 * 1000,
        max: 5,
        standardHeaders: 'draft-7',
        legacyHeaders: false,
        skip: (req) => req.ip === '127.0.0.1' || req.ip === '::1', // Healthchecks nicht limitieren
        message: { error: 'Zu viele Anmeldeversuche, bitte versuchen Sie es in 15 Minuten erneut.' }
    });

    // Für /request-password-reset, /reset-password, /set-password (SEC-01)
    const resetLimiter = rateLimit({
        windowMs: 15 * 60 * 1000,
        max: 5,
        standardHeaders: 'draft-7',
        legacyHeaders: false,
        message: { error: 'Zu viele Anfragen, bitte versuchen Sie es in 15 Minuten erneut.' }
    });

    // Für /request-email-verification, /verify-email (SEC-01)
    const verificationLimiter = rateLimit({
        windowMs: 15 * 60 * 1000,
        max: 5,
        standardHeaders: 'draft-7',
        legacyHeaders: false,
        message: { error: 'Zu viele Anfragen, bitte versuchen Sie es in 15 Minuten erneut.' }
    });

    // Für POST /super-login — strenger: 3 Versuche pro 15min (SEC-01)
    const superLoginLimiter = rateLimit({
        windowMs: 15 * 60 * 1000,
        max: 3,
        standardHeaders: 'draft-7',
        legacyHeaders: false,
        message: { error: 'Zu viele Super-Admin-Anmeldeversuche, bitte versuchen Sie es in 15 Minuten erneut.' }
    });

    module.exports = { loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter };
    ```

    **Datei B: backend/middleware/validation.js**

    Validierungsketten für alle HIGH-Risk-Endpunkte (aus RESEARCH.md Endpoint-Inventur Priorität 1):

    ```javascript
    const { body, validationResult } = require('express-validator');

    // Fehler-Handler — einheitlich für alle validierten Endpunkte
    const handleValidationErrors = (req, res, next) => {
        const errors = validationResult(req);
        if (!errors.isEmpty()) {
            return res.status(400).json({ errors: errors.array() });
        }
        next();
    };

    // POST /login
    const loginValidation = [
        body('usernameOrEmail').trim().notEmpty().withMessage('Benutzername oder E-Mail erforderlich'),
        body('password').notEmpty().withMessage('Passwort erforderlich')
    ];

    // POST /request-password-reset, /request-email-verification
    const emailValidation = [
        body('email').trim().isEmail().withMessage('Gültige E-Mail-Adresse erforderlich').normalizeEmail()
    ];

    // POST /reset-password, /set-password, PUT /users/change-password (SEC-06)
    const passwordValidation = [
        body('newPassword')
            .isLength({ min: 8 }).withMessage('Passwort muss mindestens 8 Zeichen haben')
            .matches(/[A-Z]/).withMessage('Passwort muss mindestens einen Großbuchstaben enthalten')
            .matches(/[0-9]/).withMessage('Passwort muss mindestens eine Zahl enthalten')
            .matches(/[^A-Za-z0-9]/).withMessage('Passwort muss mindestens ein Sonderzeichen enthalten')
    ];

    // POST /admin/user (User anlegen)
    const userCreateValidation = [
        body('username').trim().isLength({ min: 2, max: 50 }).withMessage('Benutzername: 2-50 Zeichen'),
        body('email').trim().isEmail().withMessage('Gültige E-Mail-Adresse erforderlich').normalizeEmail(),
        body('role').isIn(['admin', 'user']).withMessage('Ungültige Rolle (erlaubt: admin, user)'),
        body('institution_id').isInt({ min: 1 }).withMessage('Ungültige Institution-ID')
    ];

    // PUT /admin/users/:id (User aktualisieren)
    const userUpdateValidation = [
        body('username').optional().trim().isLength({ min: 2, max: 50 }).withMessage('Benutzername: 2-50 Zeichen'),
        body('email').optional().trim().isEmail().withMessage('Gültige E-Mail-Adresse').normalizeEmail(),
        body('role').optional().isIn(['admin', 'user']).withMessage('Ungültige Rolle')
    ];

    // PUT /user/change-email
    const changeEmailValidation = [
        body('newEmail').trim().isEmail().withMessage('Gültige E-Mail-Adresse erforderlich').normalizeEmail(),
        body('password').notEmpty().withMessage('Aktuelles Passwort erforderlich')
    ];

    module.exports = {
        handleValidationErrors,
        loginValidation,
        emailValidation,
        passwordValidation,
        userCreateValidation,
        userUpdateValidation,
        changeEmailValidation
    };
    ```

    Keine Änderungen an server.js in diesem Task.
  </action>
  <verify>
    <automated>node -e "const r = require('/Users/simonluthe/Documents/HymnoScribe/backend/middleware/rateLimits.js'); const v = require('/Users/simonluthe/Documents/HymnoScribe/backend/middleware/validation.js'); console.log('rateLimits:', Object.keys(r).join(',')); console.log('validation:', Object.keys(v).join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - backend/middleware/rateLimits.js existiert
    - backend/middleware/validation.js existiert
    - `node -e "const r = require('./backend/middleware/rateLimits.js'); console.log(typeof r.loginLimiter)"` gibt "function" aus
    - `node -e "const v = require('./backend/middleware/validation.js'); console.log(typeof v.handleValidationErrors)"` gibt "function" aus
    - `grep -c "superLoginLimiter" backend/middleware/rateLimits.js` gibt mindestens 2 (Definition + Export)
    - `grep -c "passwordValidation" backend/middleware/validation.js` gibt mindestens 2 (Definition + Export)
    - `grep -c "min: 8" backend/middleware/validation.js` gibt 1 (Mindestlänge 8 Zeichen)
    - `grep -c "matches.*[A-Z]" backend/middleware/validation.js` gibt 1 (Großbuchstabe-Regel)
    - `grep -c "matches.*[0-9]" backend/middleware/validation.js` gibt 1 (Zahlen-Regel)
    - `grep -c "skip.*127.0.0.1" backend/middleware/rateLimits.js` gibt 1 (Healthcheck-Ausnahme)
    - node -c auf beide Dateien gibt keinen Syntax-Fehler
  </acceptance_criteria>
  <done>rateLimits.js mit vier Limitern (loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter), validation.js mit allen HIGH-Priority Validierungsketten und handleValidationErrors — alle exportiert und loadable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Auth-Endpoints | Brute-Force auf Login, Password-Reset, Verification ohne Rate Limiting |
| HTTP Body → Validierung | Unkontrollierter Input auf Auth- und User-Management-Endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Denial of Service | POST /login, /super-login, /request-password-reset | mitigate | loginLimiter (5/15min), superLoginLimiter (3/15min), resetLimiter (5/15min) — standardHeaders: 'draft-7' für RFC-konforme Client-Behandlung |
| T-01-09 | Tampering | POST /login mit leerem usernameOrEmail | mitigate | loginValidation: notEmpty().trim() — 400 mit Fehlermeldung vor Auth-Logik |
| T-01-10 | Elevation of Privilege | POST /admin/user mit ungültiger Rolle | mitigate | userCreateValidation: role.isIn(['admin', 'user']) — 'super-admin' kann nicht per API gesetzt werden |
| T-01-11 | Tampering | Schwaches Passwort per /reset-password | mitigate | passwordValidation: min 8, Großbuchstabe, Zahl, Sonderzeichen (SEC-06) |
</threat_model>

<verification>
Nach Abschluss beider Tasks:
1. `node -e "require('./backend/middleware/rateLimits.js'); console.log('OK')"` (aus backend/) — kein Fehler
2. `node -e "require('./backend/middleware/validation.js'); console.log('OK')"` (aus backend/) — kein Fehler
3. `grep -c "superLoginLimiter" backend/middleware/rateLimits.js` — >= 2
4. `grep -c "min: 8" backend/middleware/validation.js` — 1
5. `ls backend/middleware/` — zeigt auth.js, rateLimits.js, validation.js
</verification>

<success_criteria>
- backend/middleware/ Verzeichnis existiert mit auth.js, rateLimits.js, validation.js
- Alle vier Rate-Limiter exportiert (loginLimiter, resetLimiter, verificationLimiter, superLoginLimiter)
- passwordValidation implementiert Mindestlänge 8, Großbuchstabe, Zahl, Sonderzeichen
- handleValidationErrors als zentraler Fehler-Handler exportiert
- Alle Module per node require() ohne Fehler ladbar (auth.js wirft Fehler wegen fehlendem pool.js — das ist erwartet und ok)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/01-security-backend/01-03-SUMMARY.md` erstellen mit:
- Liste aller erstellten Middleware-Dateien
- Exportierte Funktionen je Datei
- Rate-Limiter-Konfiguration (max, windowMs, skip-Bedingung)
- Passwort-Validierungsregeln (exakt wie implementiert)
- Welche Endpunkte noch keine Validierung haben (Priorität 2+3 für Plan 05)
</output>
